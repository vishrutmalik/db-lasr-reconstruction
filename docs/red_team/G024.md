# Red-team audit — G024 (N-LASR 2012 kernel + shared boosting loop)

- **Auditor:** red-team (adversarial, per `.claude/agents/red-team.md`,
  `skills/leakage-survivorship-red-teaming/SKILL.md`,
  `skills/quantitative-test-design/SKILL.md`)
- **Audited:** branch `agent/implementer/G024-nlasr-kernel` @ `99b4a7f`
  (PR #70, issue #24); verifier commit `a6d04a8` landed in parallel and is
  addressed in the relation section.
- **Baseline gates re-run:** `uv sync --locked`
  (`UV_PROJECT_ENVIRONMENT=$HOME/.venvs/lasr-redteam-g024`);
  `pytest tests/unit/test_models_*.py -q` → 102 passed;
  `pytest tests/unit/test_models_training_smoke.py
  tests/leakage/test_lt005_stable_monotonic.py -q` → 12 passed, 1 skip
  (G025 activation).
- **Scope:** `src/lasr/models/{boosting,selection}.py`,
  `src/lasr/models/nlasr/kernel.py`, their config bindings
  (`tests/fixtures/config/nlasr_2012.yaml`), the LT-005 activation flip,
  the G019→G022→G023→boost integration smoke, and the CI-006 artifact
  seam toward G026. References: P1 formulas.md §§0–7/§10, CI-006, CI-016,
  CI-021, CI-023, CI-024, CI-031..044, CR-007/008/009/030, A-G024-01..07.
- **Method:** 4 executed probe batteries (~25 sub-probes) across the six
  assigned attack surfaces; adversarial fixtures with KNOWN correct
  outcomes; teeth validated for every failing probe (the coverage-bias
  probe passes at equal coverage, so the defect — not the construction —
  drives the failure). Durable attacks promoted to
  `tests/leakage/test_red_team_g024_boost_attacks.py` (9 keepers + 2
  strict-xfail ratchets).
- **Relation to the verification report** (`docs/verification/G024.md`,
  PASS, 3 non-blocking; written in parallel and independently): we
  converge on the A-G024-03 mechanism and on NB-1 (= RT-G024-2 here).
  We diverge on severity: their §8 ruling keeps A-G024-03 non-blocking on
  the premise that "every training path exercised in this PR is
  full-coverage or single-factor". That premise is factually wrong — the
  PR's own integration smoke builds a 3-factor TrainingMatrix with
  70.4%/70.4%/100% coverage columns, and this audit measured its 30-round
  selection to be coverage-inverted (see RT-G024-1 evidence (d)). Hence
  the escalation to BLOCKING here.

## Verdict

**1 BLOCKING finding (RT-G024-1).** 1 non-blocking defect (RT-G024-2),
1 non-blocking note (N-1), 6 recorded observations. Everything else
survived attack: bins are provably frozen at fit, the loop has no side
channel to out-of-matrix data, CI-031 catches weight underflow loudly in
both regimes, exact-Z ties and tie-heavy binning are bit-deterministic
under row shuffles, `stable_sum` is bit-exact under 50 permutations and
agrees with `math.fsum`, the §7/Fig-9 goldens match an independent hand
re-derivation, and the LT-005 activation flip is robust across 4 seeds
(not seed-lucky). But the min-Z selection objective, as shipped, ranks
factors by coverage before it ranks them by signal — a pure-noise factor
missing 2% of its values beats a genuine full-coverage signal, and the
PR's own integration smoke already trains a model whose factor selection
is a coverage artifact. That is a fabricated-alpha channel at the §4.3
surface and must be guarded before merge, not after.

---

## Attack log

| # | Surface | Probe | Outcome |
|---|---------|-------|---------|
| 1a | Training leakage | Frozen bins: fit on uniform ranks, predict a shifted cross-section (all values ~0.999); payload hashed before/after two predicts | HELD — all shifted values map into the STORED top bin; payload bit-identical (CI-023) |
| 1b | Training leakage | TrainingMatrix boundary: temporal fields absent by design; `FittedModel` CI-006 stamps validated when present, optional when absent | HELD with note N-1 — contract seam verified on the G026 branch (`lasr.validation.errors.LeakageRefusalError`; `runner.py` asserts `train_max_knowledge_time is not None` before accepting a fold fit) |
| 1c | Training leakage | Label recoding inside fit: kernel/loop never see returns; label domain {−1,+1} enforced at matrix AND kernel level; label 0 rejected | HELD — no path derives labels from returns inside `fit_factor`/`boost` (CI-016) |
| 1d | Training leakage | Out-of-window side channel: rebuilt-identical matrix ⇒ byte-identical artifact (dev test re-run + probe) | HELD |
| 2a | Selection bias | 50%-coverage pure noise vs 100%-coverage weak real signal (n=1000, 50 seeds, shipped config Q=5/raw Z) | **FAILED → RT-G024-1** — noise wins 50/50 (mean Z 0.2490 vs 0.4965) |
| 2b | Selection bias | Coverage sweep 0.5→1.0 (25 seeds each) | **FAILED** — Z(noise) ≈ coverage/2 exactly; noise wins 25/25 at ≤0.98 coverage, 21/25 at 0.99 |
| 2c | Selection bias | Full 30-round boost, {signal, noise@50%} pool; OOS scoring on a fresh draw | **FAILED** — noise selected 30/30; OOS corr(H, label) 0.0021 vs 0.0715 for the signal-only model |
| 2d | Selection bias | The PR's own smoke matrix (G022 features, WORLD_SEED=1729): per-column coverage + 30-round selection + coverage-honest re-ranking | **FAILED** — momentum_12_1/reversal_1m at 70.4% coverage selected 30/30, size_neg_log_mcap (100%) 0/30; coverage-honest normalizer INVERTS the order (size 0.9760 best; momentum 0.9972 worst) |
| 2e | Selection bias (teeth) | Same construction at EQUAL full coverage; and coverage-honest normalizer `Z_true = 2Z + uncovered_mass` re-scored on 2a | HELD — signal wins 50/50 both ways; the deficit alone drives 2a–2d |
| 3a | Numerics | ε=1/N with N=2, N=3; Q>n_covered (degenerate duplicate edges, empty bins) | HELD — finite h, eps exact, deterministic assignment |
| 3b | Numerics | Weight underflow, partial (2 rows → exact 0.0, total > 0) via |h|=800 test double | HELD — loud `CI-031 violated ... non-positive weight` |
| 3c | Numerics | Weight underflow, total (all rows → 0.0) | HELD — loud `weight mass must be finite and positive ... got 0.0` |
| 3d | Numerics | 1000-round run, real kernel (10× production L) | HELD — min weight 8.1e-3 (equilibrium, no drift), Z→0.500000, simplex ≤4.4e-16 every round |
| 3e | Numerics | `stable_sum` on adversarial magnitudes (500×1e-16 + 3×0.33 + 200×~1e-9), 50 permutations; vs `math.fsum` | HELD — one bit-exact sum; delta vs fsum = 0.0 |
| 3f | Numerics | `propagate_nan` policy + NaN rank inside the TRAINING loop | Loud error, misleading message → **RT-G024-2** (non-blocking) |
| 4a | Determinism | EXACT Z tie between two DIFFERENT columns (same-bin same-label value swap ⇒ bit-equal masses) | HELD — registry-first wins; invariant across 20 row shuffles |
| 4b | Determinism | Tie-heavy panel (3 distinct values, Q=5, empty bins), 30 row permutations | HELD — bit-identical payloads |
| 4c | Determinism | Serialization round-trips (kernel payload + FittedModel), dev tests re-run + payload-hash probes | HELD — bit-identical predictions after JSON round-trip |
| 5 | Fixture independence | §7 micro-fixture and Fig-9 goldens re-derived by hand from P1 formulas §§1–4 WITHOUT reading the tests, then compared | MATCH — W±=(0.4,0.1/0.1,0.4), Z=0.4, h=±½ln2.5=±0.458145, w→1/13 & 2.5/13, round-2 Z=2√10/13≈0.48650, round-2 h=½ln(53/38); Fig 9: +0.16065/−0.20153, 0.0556·e^−0.49=0.03404 |
| 6a | Suspicious ease | LT-005 walk-forward re-run under seeds 20260723/914/7/424242 | HELD — mean IC 0.0873/0.0969/0.1020/0.0910, all in [0.07, 0.13]; FMONO 100% of rounds each time; flip not seed-lucky |
| 6b | Suspicious ease | Integration smoke A-003 discipline: any hidden performance claim? | HELD — assertions are structural only (round count, Z domain, finiteness, CI-006 stamps, bit-identity); but see RT-G024-1(d): its silent selection outcome is a coverage artifact |

## Findings

### RT-G024-1 — BLOCKING: min-Z selection ranks factors by coverage before signal (A-G024-03 quantified)

**Mechanism.** `z_statistic` (P1 formulas §3) sums `√(W⁺ⱼW⁻ⱼ)` over the
fitted masses; `fit_factor` drops missing-rank rows from the bins
(CI-021), so `Σⱼ(W⁺ⱼ+W⁻ⱼ) = covered_mass < 1` under partial coverage and
`Z ≤ covered_mass/2` by AM-GM **regardless of content**. The paper's own
precondition ("Since weights sum to 1, Σⱼ(W⁺ⱼ+W⁻ⱼ)=1", formulas §1)
silently fails, so paper-faithfulness cannot be claimed for this regime
either. The proper AdaBoost round normalizer for the fitted h with h=0 on
uncovered rows is `Z_true = 2·Σⱼ√(W⁺ⱼW⁻ⱼ) + uncovered_mass`, which is
coverage-honest.

**Quantification** (shipped config: Q=5, raw Z, h_zero, ε=1/N; n=1000):

| probe | result |
|---|---|
| noise@50% cov vs weak signal@100% (50 seeds) | noise wins **50/50** (mean Z 0.2490 vs 0.4965) |
| coverage sweep | Z(noise) ≈ cov/2; wins 25/25 at cov ≤ 0.98; **21/25 at cov = 0.99** |
| 30-round boost {signal, noise@50%} | noise selected **30/30**; OOS corr(H,label) 0.0021 vs 0.0715 signal-only |
| coverage-honest `Z_true` on the same panels | signal wins 50/50 (fix restores correct selection) |
| teeth: equal coverage | signal wins 50/50 (defect, not construction, drives the flips) |
| survival threshold | a full-coverage factor needs raw Z < 0.25 to beat 50%-coverage noise — near-perfect class separation, far beyond any realistic factor (real monthly factors sit ~0.49+; the paper's own worked example's best factor is 0.4 with Q=2) |

**In-PR exposure (why BLOCKING, not a future-goal hazard).** The G022
library ships `DEFAULT_MIN_COVERAGE = 0.5`, and the PR's own integration
smoke (`tests/unit/test_models_training_smoke.py`, WORLD_SEED=1729)
builds a TrainingMatrix whose columns have coverage
momentum_12_1 = 100/142 (70.4%), reversal_1m = 100/142 (70.4%),
size_neg_log_mcap = 142/142 (100%). Measured on that exact matrix:
round-1 raw Z = 0.3507/0.3452/0.4880 → the boost selects
momentum 16 + reversal 14 = 30/30 rounds and size **0/30**, while the
coverage-honest normalizer says size is the MOST discriminative
(Z_true 0.9760) and the two selected factors are nearly useless
(0.9972/0.9862). The first multi-factor model this repo ever trains has
coverage-artifact selection; every downstream consumer of
`boost`+`MinZObjective` (G025 pooled windows, G029/G030, ensembles)
inherits it, and the recorded `selection_scores` (implausibly low Z)
present the artifact as strong discrimination — the exact
strong-results-as-bugs channel of MP §10.8.

**Required to clear** (any one, plus a decisions.md entry — an
assumptions-register line + "G037 audit item" is not containment for a
defect this PR already triggers):
1. coverage-honest objective (score `2Z + uncovered_mass`, or
   equivalently penalize by missing mass) behind the existing config
   surface, with an A/B test; or
2. a hard guard: raw min-Z refuses (or hard-warns via a typed event)
   TrainingMatrix inputs with unequal/partial column coverage; or
3. a full-coverage precondition enforced at TrainingMatrix construction
   for the min_z-raw configuration, with missing-data policy resolved
   upstream.

**Reproduction.**
```bash
UV_PROJECT_ENVIRONMENT=$HOME/.venvs/lasr uv run pytest \
  "tests/leakage/test_red_team_g024_boost_attacks.py::TestCoverageBiasRatchet" -v
# 2 strict XFAIL (the ratchets) + 2 PASS (teeth + mechanism pin)
```
Inline (mechanism, 30 s):
```python
import numpy as np
from lasr.models.nlasr.kernel import PiecewiseConstantBinKernel
from lasr.models.selection import z_statistic
rng = np.random.default_rng(7); n = 1000
y = np.array([1,-1]*(n//2), dtype=np.int8); rng.shuffle(y)
def rk(v):
    o = np.full(v.shape, np.nan); m = np.isfinite(v)
    o[m] = (np.argsort(np.argsort(v[m]))+1)/m.sum(); return o
sig = rk(0.10*y + rng.standard_normal(n))          # real signal, 100% cov
noi = rng.standard_normal(n); noi[rng.choice(n, n//2, False)] = np.nan
noi = rk(noi)                                      # pure noise, 50% cov
k = PiecewiseConstantBinKernel(n_bins=5); w = np.full(n, 1/n)
for name, col in (("signal", sig), ("noise50", noi)):
    m = k.fit_factor(col, y, w, factor_id=name).masses()
    print(name, round(z_statistic(m.w_pos, m.w_neg), 4))
# signal ~0.49..., noise50 ~0.24...  -> argmin picks the noise
```

### RT-G024-2 — NON-BLOCKING: `propagate_nan` + missing training value fails with a message that hides the cause (= verifier NB-1)

With `missing_policy="propagate_nan"` (the OQ-P1-05 declared alternative)
and ANY NaN rank in the training pool, the selected factor's h is NaN,
`exp_reweight` poisons the weights, and the loop dies with
`BoostingError: weight mass must be finite and positive to renormalize,
got nan` — loud (good: never silent), but the message names neither the
missing rank, the factor, nor the policy; the config leaf is named
`missing_at_predict` yet governs the TRAINING update (A-G024-04 documents
the h_zero extension only). Under this policy the model cannot train on
partial coverage at all — undocumented. Keeper pins the never-silent
invariant: `TestMissingPolicyInTraining`.

### N-1 — NON-BLOCKING note: unstamped `FittedModel` carries no PIT guarantee outside G026

The CI-006 fields are optional-but-validated (documented: "formula-level
tests may omit them"). Verified on the G026 branch that the walk-forward
runner both stamps them from fold data and refuses violations
(`LeakageRefusalError`; `runner.py` asserts the stamp is present). The
residual risk is any FUTURE non-G026 production caller constructing
`FittedModel` directly without stamps — no G024-side gate exists. Fine as
a two-sided contract today; G037 should check that every production
artifact writer stamps.

## Observations (no action required)

- **O-1** LT-005 activation flip is seed-robust: 4 seeds → mean IC
  0.0873–0.1020, all inside the documented [0.07, 0.13] band; FMONO
  selected 3900/3900 rounds each seed.
- **O-2** Weight dynamics self-stabilize: at 1000 rounds min weight is
  8.1e-3 and the selected factor's Z sits at 0.500000 (absorbed) —
  renormalization does not mask underflow on realistic panels; underflow
  requires |h| ≈ 700+, unreachable for the real kernel
  (|h| ≤ ½ln((1+ε)/ε) ≈ 6.9 at N=10⁶).
- **O-3** §7 micro-fixture and Fig-9 goldens match this audit's
  independent hand re-derivation exactly (including round-2
  h = ½ln(53/38): 4/13+1/10 = 53/130, 2.5/13+1/10 = 38/130).
- **O-4** The integration smoke keeps A-003 discipline — structural
  assertions only, no efficacy claims (its selection OUTCOME is however
  the RT-G024-1(d) exhibit).
- **O-5** Exact-Z ties, tie-heavy equal-count binning, and serialization
  round-trips are bit-deterministic under row shuffles and double runs;
  the registry-order tie rule is honored in both column orders.
- **O-6** `equal_count_edges` degenerate cases (Q > n_covered, all-tied
  values) produce deterministic value-based assignment with empty bins;
  empty bins contribute h=0 and Z=0 — content-neutral per bin, but note
  the Z=0 contribution is the same mass-vanishing mechanism as
  RT-G024-1 (bounded here by covered mass staying in SOME bin, so no
  additional bias beyond coverage).

## Keepers

`tests/leakage/test_red_team_g024_boost_attacks.py` — 11 tests:
2 strict-xfail ratchets (RT-G024-1: half-coverage noise, 2%-deficit
flip), teeth companion + mechanism pin, 2 underflow-teeth (partial/total),
propagate-nan-never-silent, 3 tie/permutation determinism attacks,
300-round stability. Runtime ~0.3 s.

## Exit state

- Probes executed: 25 (all six assigned surfaces probed with recorded
  outcomes); every failing probe teeth-validated.
- `pytest tests/leakage/test_red_team_g024_boost_attacks.py` →
  9 passed, 2 xfailed.
- Production code untouched; only this report and the keeper module
  added.

---

# Round 2 — adversarial verification of the RT-G024-1/RT-G024-2 remediation

- **Audited:** remediation range `3306262..6595a23` (same branch/PR).
- **The fix under attack:** `MinZObjective.coverage_adjustment` default
  `coverage_honest` — `Z' = Z + U/2`, `U` = current weight mass of the
  candidate's NaN-rank rows, measured per round from the training column;
  paper-literal `raw_covered_only` arm config-expressible with a loud
  warning; RT-G024-2 typed refusal before the weight update.
- **Status: IN PROGRESS** (skeleton committed early per interruption
  discipline; findings-so-far below, full attack log to follow).

## Findings so far (round 2)

- Affine-equivalence check of the fix design: `Z' = Z + U/2 = (2Z + U)/2`
  is a positive affine transform of this audit's exact AdaBoost
  normalizer `Z_true = 2Z + U` — argmin-identical by construction. PASS
  at design level; empirical re-quantification below.
- Suite state at `6595a23`: keeper + model unit files = 132 passed,
  0 xfailed — my two round-1 strict-xfail ratchets now PASS as teeth.
- Keeper-edit integrity: audit in progress (see §"Keeper-edit integrity"
  when complete).

