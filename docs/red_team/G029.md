# Red-team report — G029 (end-to-end synthetic vertical slice, PR #74)

Auditor: red-team agent (adversarial). Implementation audited: `agent/implementer/G029-vertical-slice`
@ `0c89a08`. Method per red-team charter (§34): assume every strong result is a bug until
disproven; every attack that finds live surface becomes a permanent test in
`tests/leakage/test_red_team_g029_slice_attacks.py`.

STATUS: IN PROGRESS — skeleton; attack log lands with the evidence.

## Attack surfaces

1. Ratchet-flip integrity (10 flips: RT-G026-2/3, RT-G027-5, RT-G028-1/2a/2b,
   RT-G025-1/2/5, RT-G035-3) — line-by-line diff audit + edge-case re-attack.
2. Reproducibility theater: verify-run hash-tree coverage per artifact class;
   hash16 identity/collision; idempotent-rerun cache poisoning; hidden
   nondeterminism (PYTHONHASHSEED, path independence, filesystem order).
3. Composition leaks: N8 stamp/fold straddle; purge/embargo through the real
   config surface; adapter under composition; end-of-history truncation.
4. CLI/config attacks: unknown keys, YAML hazards, L3 refusal, banner integrity.
5. Merger-successor seed failures: typed refusal, no partial-but-blessed run.
6. ci.yml: paths-ignore/rename skip surface.

## Verdict

PENDING.
