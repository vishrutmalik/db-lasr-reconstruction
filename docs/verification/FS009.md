# Verification report — FS009

- **Goal ID:** FS009 — Manifest reconciliation + independent verification of
  the seven FactSet documentation deliverables + binding design rulings
- **Branches reviewed:** `agent/fs-architect/FS002-integration` (3fda393),
  `agent/fs-researcher/FS003-symbology` (6952354),
  `agent/fs-researcher/FS004-fundamentals` (023c84e),
  `agent/fs-researcher/FS005-globalprices` (3ea4fd8),
  `agent/fs-researcher/FS006-estimates` (1ac8530),
  `agent/fs-researcher/FS007-rbics` (26a055b),
  `agent/fs-researcher/FS008-benchmarks` (7b1e0fa),
  `agent/fs-researcher/FS021-pit-estimates-spec` (0903677)
- **Pull requests gated:** #75, #76, #77, #78, #79, #80, #81, #82 (docs-only)
- **Verifier branch:** `agent/fs-verifier/FS009-reconciliation` off
  origin/main (0a74049); env `UV_PROJECT_ENVIRONMENT=$HOME/.venvs/lasr-fs009`
- **Verdict:** **PASS — all seven documents; all six families; overall PASS.**
  0 blocking findings; 6 non-blocking findings (NB-1..NB-6).
- **Deliverables produced:** `docs/factset/capability/MANIFEST.md` (human
  matrix + NORMATIVE rulings N1–N3 + FS-VQ-01..75),
  `docs/factset/capability/manifest.json` (fs002-1, 6 families, 95 op rows).

Read order honored: fs_goals.md (HARD RULES, FS009 row) →
fs_review_adjudication.md → decisions.md D-018/D-019/D-020 →
factset_integration.md (fs002-1 + §7.4) → the seven deliverables → specs/PDFs.
No credential file was read; no live API call was made; no production code
touched.

## Acceptance criteria reviewed

FS009 row (fs_goals.md): manifest reconciliation + verification + BINDING
design rulings (PIT mode-mapping table; canonical Estimates representation;
findings consolidation); adjudication §4.2 (mode table binding), §7
(estimates representation binding), §10 (findings consolidation binding);
D-020(e) (CE-10 + inferred supersession), D-020(f) (interpolated-membership
basis), D-020(g) (effective≠knowledge policy). FS002 §7 defines the fs002-1
schema this reconciliation must emit; §7.4 defines the FS003 re-key.

## Commands executed (all re-run by the verifier; trust nothing)

| Command | Result |
|---|---|
| `_extract_symbology.py` (re-run, FS009 venv) | summary reproduces symbology.json completeness exactly: 4 ops / 6 params / 16 schemas / 10 responses / 32 examples / 1 scheme / 8 enum sites / 4 distinct sets |
| `_extract_fundamentals.py` (re-run, regenerates fundamentals.json IN PLACE) | `git status` clean after run — committed JSON reproduced **byte-identically**; completeness_proof stdout matches §9 of fundamentals.md (12 ops / 22 params / 54 schemas / 0 orphans / 23 enum sites / 17 sets) |
| `_extract_global_prices.py <spec> <scratch>` | output **byte-identical** to committed global_prices.json (`diff -q`); counts 17 paths / 24 ops / 73 schemas / 59 params / 17 responses / 69 examples / 9 enum sites |
| `_extract_estimates.py` (re-run in place) | `git status` clean — estimates.json reproduced byte-identically; 30 ops / 81 schemas / 33 params / 15 enum sets |
| `_extract_rbics.py` (re-run) | summary matches rbics.json completeness_proof: 6 paths / 11 ops / 11 params / 44 schemas / 10 responses / 28 examples / 2 schemes / 0 enum sites |
| `_extract_benchmarks.py <spec> <scratch>` | output **byte-identical** to committed benchmarks.json; 7 paths / 14 ops / 38 schemas / 18 params / 5 responses / 25 examples / 7+7 enum sites / orphans {schemas:1, parameters:1} |
| `grep -c operationId` on all six specs (extractor-independent) | 4 / 12 / 24 / 30 / 11 / 14 — matches every claimed operation count |
| `shasum -a 256` on both PDFs (extractor-independent) | `320549e9…` (UG) and `ea420ea5…` (CM) — match FS021's cited sha256s exactly |
| independent pyyaml parses (NOT via any extractor) of all six specs | securitySchemes recomputed for all six (CFC-1 divergence confirmed); targeted schema/enum/parameter probes below |
| pypdf extraction of UG pp.6–13, CM pp.7–13 + keyword sweeps | FS021 claim sampling below |
| manifest.json validation script | 95/95 operation rows carry every FS002 §7.3 key; 0 missing |

## Spot-checks per family — direct against the vendor spec, NOT via extractors (≥3 per family)

**Symbology:** (1) rate limit "10 requests per second and 10 concurrent"
found at spec L14–15; (2) `ids` prose "ids limit = 100" (L631) vs
`maxItems: 3000` (L625/L1045) — D-1 conflict real; 8192-byte cap L633; 29s
timeout examples L1608/L1766; (3) enums recomputed: 31/28/30/4 values —
match §3.1–3.4 exactly; (4) `identifierResolutionHistorical` fields =
9-field list of §2.4 exactly; (5) securitySchemes = BasicAuth only.

**Fundamentals:** (1) `FundamentalsPITRequestBody`: exact 10 properties,
required `[ids, metrics, fiscalPeriodStart]`, NO currency/batch — matches
§1.1; (2) `IdentifierList.maxItems=1000`, `MetricList.maxItems=1600`;
(3) `FundamentalsPITData.pitStart/pitEnd` descriptions verified verbatim
(inclusive UTC; null pitEnd = current; **W/M snapshot rows: pitStart null,
pitEnd = stamp**) — the §N1 hazard is real; (4) `PeriodInfo.pitStart` =
"first published and became available" verbatim; (5) D5 verified: min/
maxItems misplaced INSIDE the items string subschema on `IdsBatchMax30000`
(no array bound schema-enforced); GET=2000-batch vs POST=5000-batch prose
confirmed at L69/L133; (6) "primary securities" restriction at L341;
(7) securitySchemes = FactSetOAuth2 + FactSetApiKey.

**Global Prices:** (1) `Adjust` enum `[SPLIT, SPLIT_SPINOFF,
DIV_SPIN_SPLITS, UNSPLIT]` default **SPLIT** — the refuse-worthy default is
real; (2) `DividendAdjust` 5 values default EXDATE_C; `Frequency` 11 values;
`Calendar` 4 values default FIVEDAY; (3) GP-DISC-06 verified: `startDateSS`
parameter carries literal schema default `2021-08-27`; (4) shares fields =
the 15-field list of §7 exactly (incl. `publicationDate`);
(5) `Price.additionalProperties: true`; `BatchStatus.status` 4-value enum.

**Estimates:** (1) E-D2 verified verbatim: `fixedDetailRequest.
fiscalPeriodEnd` `$ref`s `#/components/schemas/fiscalPeriodStart`;
(2) `consensusEstimate` = exactly 19 fields incl. up/down/estimateCount;
E-D1 verified (`down` description reads "Number of Up Revisions");
(3) `detailEstimate` = 27 fields; `inputDateTime` `format: string` with
timezone-less example (E-D13); (4) `consensusRatings` 11 fields incl. the
five counts + ratingsNote; (5) 4M-datapoints/min 429 + Retry-After present
in spec text; "100 days" appears 6×; securitySchemes = BasicAuth only.

**RBICS:** (1) `EntityFocus` schema: only 4 declared props (requestId,
fsymId, firstDate, lastDate), all required, `additionalProperties: true` —
D-2 (undeclared level payload) verified; (2) `Ids.maxItems=2500` vs
`TradeNamesIds.maxItems=500`; (3) D-3 verified verbatim: the
`rbicsStructureLevel` example returns `startDate`/`endDate`/`name` against
schema fields `rbicsId`/`firstDate`/`lastDate`; (4) info rate-limit prose =
"10 requests per second" with NO concurrency companion (D-11).

**Benchmarks:** (1) `constituentIds`/`fIConstituentIds` maxItems **1**
(param AND POST schema — no POST capacity advantage), `indexIds` 500,
`metrics` 38; (2) BM-DISC-04 verified: frequency param default `D` vs
schema default `CY`, identical 7-value enum; (3) BM-DISC-05 verified
verbatim: `indexReturns.totalReturnPercent` description carries the
inverted "((startDate level / endDate level) - 1) * 100" formula;
(4) securitySchemes = BasicAuth only (BM-DISC-01 real).

**FS021 vs the PDFs** (load-bearing claim sampling, pypdf + keyword sweeps):
12-item table (UG p.11) matches exactly; "not available before December
2009" (p.12) verbatim; 2017-09-09 frozen-methodology paragraph (p.12)
verbatim; local-midnight snapshot + three-times-daily update + intraday-FX
settle (pp.6/11-12) verbatim; not-adjusted-for list (p.6) verbatim with
splits indeed absent (PE-Q13 justified); redundant-record paragraph (p.12)
verbatim; PK `fsym_id,fe_item,pit_start_date,fe_per_label` + field
dictionary incl. "first/last day that the estimate was valid" (p.13)
verbatim; fsym_id regional example `B00CQC-R` (p.13); CM sampling: V4
estimates-currency + millions + GBP/ZAR major units (p.7); split-adjusted
default + historical per-share restatement + "estimates are never
backfilled" (p.8); T-1 methodology + 100-day window + Variable Window
2014-12-12/150d + fact windows SALES 180d / EPS 360d (p.10); revision
window 75d + class 0 (p.11); 1–3 scale + Without/Dropping/Restricted
(p.13). Both PDF sha256s recomputed and match.

## Evidence-tag honesty audit

Every DOCUMENTED_OPENAPI claim sampled above was found in the cited spec
(0 phantom citations in ~45 targeted probes across 7 documents). The
manifests' most damaging self-findings (spec-internal bugs D-1/D5/D7,
E-D1/E-D2, D-3/D-4, BM-DISC-04/05/06, GP-DISC-06/07/09) all reproduce —
these are honest documents, not advocacy. INFERRED/UNRESOLVED tags spot-
checked for honesty: FS003 U-7c (open-interval convention), FS007 §2.1
half-open-interval reading ("INFERRED ONLY"), FS008 §5.1 percent-basis
(INFERRED with the CY example cited) — all correctly refuse to claim
documentation they don't have.

## Edge cases attempted

- Extractor determinism: FS004/FS006 regenerate IN PLACE — byte-identical
  (git-clean); FS005/FS008 regenerate to scratch — `diff -q` identical.
- Extractor blind-spot control: operation counts, sha256s, securitySchemes,
  and every per-family probe above used independent parses/greps, not the
  committed extractors.
- Count cross-foots: symbology 16 schemas vs 17 SDK models reconciliation
  (§8 row) re-checked arithmetically; estimates 30 ops by-tag sum = 30;
  benchmarks orphan components (calendar param+schema) confirmed via the
  extractor's `unreferenced_components` AND an independent $ref sweep.
- Hostile reading of FS021 vs UG: found the p.12 "only window sizes …
  100 … and 45-day" sentence in tension with the Sharp bundles (NB-1) and
  the p.13 `CFPS` example item outside the 12-item list (NB-2) — both
  folded into the FS-VQ register (FS-VQ-67, FS-VQ-64).

## Leakage risks checked (documentation-phase form)

- Fundamentals snapshot-mode field overloading (pitEnd = snapshot date) —
  confirmed real in the spec; killed structurally by the §N1 normative
  table (knowledge_valid_to NEVER populated from snapshot pitEnd).
- Estimates API: no manifest claim anywhere treats perspective
  reconstruction as PIT (FS006 §7 verdict is correctly brutal); PB-08
  refusal path referenced; CE-1/CE-5 provenance is in the ruling.
- RBICS/Benchmarks effective-vs-knowledge conflation — §N3 policy rows
  implement D-020(g): effective dates never become knowledge_time;
  strict-PIT-headline exclusion until FS-VQ-52 (benchmarks) / FS-VQ-47
  (RBICS) resolve.
- Benchmarks membership interpolation — vendor-observed snapshots vs
  inferred intervals kept ontologically separate (D-020(f) basis).
- GP vendor default `adjust=SPLIT` — refuse-worthy default confirmed;
  UNSPLIT pin (FS002 §4.4/FT-08) re-affirmed in the matrix.
- Feed PE-05/PE-04 (no future knowledge; capture invariance) carried into
  the Phase-2 battery unchanged.
- HARD-RULE hygiene: no credential file read by FS009; FS006 E-D17
  correctly flags that the vendor demo falls back to READING api_keys.txt —
  recorded as a repo-must-never-copy warning.

## Quantitative invariants checked

- 95 = 4+12+24+30+11+14 operation rows; 95/95 rows carry all 33 §7.3 keys.
- All six completeness proofs: documented == defined counts (verified by
  re-run + independent grep).
- Both PDF sha256s and all six spec sha256s recomputed; manifest.json
  embeds the FS009-recomputed values.
- Entitlement = UNKNOWN on 95/95 rows; implementation NOT_STARTED,
  test NONE on 95/95 (no phantom lifecycle claims).

## Blocking findings

**None.** No claim in any of the seven documents misstates the vendor spec
in a way that would corrupt adapter design.

## Non-blocking findings

- **NB-1 (FS021):** UG p.12 states "the only window sizes available will be
  100 days and the 45-day post-event window" while Sharp bundles exist in
  UG Table 1 — internal PDF tension not recorded in the FS021 spec. Folded
  into FS-VQ-67 (with PE-Q9).
- **NB-2 (FS021):** UG p.13 data-dictionary example `fe_item = CFPS` is not
  in the documented 12-item list — suggests the item dictionary may be
  wider than p.11 documents. Folded into FS-VQ-64 (with PE-Q6).
- **NB-3 (FS004):** fundamentals.json is not fs002-1-shaped (no
  `manifest_version`; `completeness_proof` vs `completeness`; ops lack the
  §3.3 dimension keys). Handled: FS009 normalized it (as §7.4 anticipated
  for FS003); no content loss found.
- **NB-4 (FS005/FS008):** global_prices.json / benchmarks.json are
  extractor-inventory-shaped with a `curated` block rather than fs002-1.
  Same handling as NB-3; the curated blocks carry all checklist constants.
- **NB-5 (FS006):** estimates.md §12 says regeneration is byte-for-byte
  "apart from generation date" — actual re-run was fully byte-identical
  (the generated date is pinned, not dynamic). Claim is conservative, not
  wrong.
- **NB-6 (cross-family):** three families' specs declare BasicAuth only
  while SDK+demos use OAuth2 (CFC-1) — recorded normatively in MANIFEST.md
  §3; FS010 must probe the Basic path once per family at smoke rather than
  assume platform uniformity.

## Recommendations (routed, not fixed here)

1. FS010: implement the CFC-3 timeout-body parser and CFC-4 dual-envelope +
   per-row error splitting before any adapter lands (both are now
   six-family-evidenced requirements).
2. FS024: drive the entitlement matrix directly off FS-VQ section A; the
   probe checklist is FS-VQ routes SMOKE/PROBE.
3. Orchestrator: the three ratification items surfaced by §N2 (stat enum
   up/down counts; CE-5 `vendor_pit` value; window-in-metric-dictionary
   keying) and §N1's `retrieval_mode` raw column should enter the D-019
   queue before FS012/FS014 start.
4. FS012/FS017: adopt §N1 rows 1–5 verbatim as acceptance criteria (they
   are written to be testable: PB-09/PB-11 bindings included).

## Evidence

- Re-run logs + extracted PDF pages: FS009 scratchpad (session-local);
  every substantive probe result is restated inline above with spec line
  numbers where cited.
- Reconciled outputs: `docs/factset/capability/MANIFEST.md`,
  `docs/factset/capability/manifest.json` (this branch).
- Verified-input SHAs: branch heads listed in the header; spec/PDF sha256s
  in manifest.json + MANIFEST.md §1.
