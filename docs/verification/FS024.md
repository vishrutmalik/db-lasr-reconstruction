# Verification report — FS024 fresh remediation review

- **Goal:** FS024 — entitlement discovery, metric catalogs, and notebook
  health-check scaffold
- **Branch / worktree:** `agent/fs-implementer/FS024-discovery` /
  `.worktrees/FS024`
- **Exact checkpoint reviewed:**
  `45eae8dcc9f95f33a9a4812321de1b8a8117eaa6`
- **Remediation code checkpoints:**
  `55cf788` (VF-FS024-1..3) and `8c4c917` (bounded refresh)
- **Original verifier checkpoint:**
  `0c0ae8670cab681a0dedab155abc566a260d91e2`
- **Merge base:** `dfc53c9f49e67e5f124fc556e64e1556d4c49268`
- **Pull request:** #87, exact head `45eae8d`, OPEN / MERGEABLE, 8/8 CI
  checks successful when inspected
- **Verdict:** **PASS** — VF-FS024-1, VF-FS024-2, and VF-FS024-3 are closed;
  no blocking finding remains.

No live FactSet call was made and no credential source or value was read during
re-verification. External manifests, capture metadata, and the append-only
ledger were inspected read-only. Notebook execution used an isolated
`/private/tmp` copy of the capture store because replay emits telemetry.

## Scope and governing evidence

Read the permanent bootstrap, current state, FS024 durable charter, original
verification report/checkpoint, remediation checkpoint, committed entitlement
matrix, human and machine capability manifests, and notebook sections 1–4.
Inspected the complete remediation delta from `0c0ae86` to `45eae8d`: 10 files,
509 insertions / 95 deletions. The review focused on the three failed evidence
boundaries and then reran the charter's full/static/notebook gates.

## Re-verification result

| Requirement | Verdict | Independent evidence |
|---|---|---|
| Separately answer FS-VQ-02 for CUSIP, ISIN, and SEDOL | PASS | Final plan has three one-output requests and three distinct full SHA-256 request identities. External metadata shows one independently captured HTTP 403 for each output type. |
| Prevent sibling entitlement overclaim | PASS | CUSIP, ISIN, and SEDOL have separate probe ids, request bodies, result rows, capture ids, and manifest entries. A unit keeper makes one synthetic CUSIP denial coexist with successful ISIN/SEDOL results. |
| Treat cached HTTP 401 as account authentication | PASS | Replay raises `FactSetAuthError` before returning a report. An independent write-enabled synthetic probe produced `ACCOUNT_ABORT` and created no run directory or entitlement artifact. |
| Preserve malformed-auth evidence honestly | PASS | CUSIP metadata retains the earlier 401 capture with no entitlement result, followed by the corrected 403 capture. Ledger completion statuses are 401, 403, 403, 403 for the remediation identities. |
| Immutable/distinct acquisition and replay runs | PASS | Run ids, directories, inodes, manifest digests, modes, and metrics are distinct. Code preflights run ids before transport and uses an exclusive final directory creation. Reuse is a typed refusal. |
| Complete per-probe lineage | PASS | Acquisition has 3/3 probe rows and capture hashes; replay has 17/17. Every row carries probe/family/endpoint/verb/classification/status/cache flag/request hash/capture id/retrieval time and matches the final deterministic plan. |
| Honest handling of the overwritten original manifest | PASS | Documentation explicitly says the initial 14-call acquisition manifest was overwritten and is not reconstructed or relabeled. New manifests are accurately limited to the remediation acquisition and complete replay. |
| Catalogs and notebook scaffold | PASS | Isolated real-capture replay ran 17 probes with 0 live calls, 14 success-cache hits, 0 errors, and catalog totals 2,246 / 439 / 710. Sections 1–4 remain thin module consumers with `LIVE_PULL=False`. |
| Lifecycle fold-in scope | PASS | Machine manifest parses with 95 operation rows. The sampled Symbology POST wording now cites three separate probes; unprobed operations remain `UNKNOWN`. |

## VF-FS024-1 — closed: independent gated-output evidence

The final request plan produces these one-type identities:

| Output | Request hash | Corrected capture id | Result |
|---|---|---|---|
| CUSIP | `791688632500a4f5e54e569003b2a8e06fc739a26697a84744e8fa4a3f3d97f7` | `9e7ab3c417acd15beaaa21bff0d3bed2c68a7f7c5739ad9ee517305a0c0c3948` | HTTP 403 |
| ISIN | `cf45c8be0448501e3acd697ba34c936d30635c8476f4520c545f21ff7e5dd84c` | `69195dd010e582625b65230c2410d0fe7ca7f0a1d7e5540bd23f2a1c16681dc7` | HTTP 403 |
| SEDOL | `3cbe3d6f19dcea4d7fdc0c7797177e9271bf9ef3ee66e3d239776b82204fdaa6` | `d25f84248284e0f28979d9b3070695159880b2cd4a70dbf4680a0fba4442d534` | HTTP 403 |

The request metadata contains exactly `["CUSIP"]`, `["ISIN"]`, and
`["SEDOL"]`, respectively. Each conclusion is therefore supported by its own
request/capture pair and cannot label a sibling output type.

## VF-FS024-2 — closed: cached 401 aborts at account scope

`_execute_probe` distinguishes cached 401 from endpoint authorization: it
raises `FactSetAuthError` and explicitly suppresses endpoint and family claims.
The independent probe exercised `write_outputs=True`, not only the existing
unit-test seam, and verified that the proposed run directory did not exist
after the abort. The actual external CUSIP request retains both historical
captures: malformed-auth 401 with `entitlement_result=null`, then corrected
403 with `FORBIDDEN`. Only the latter appears in the entitlement matrix.

## VF-FS024-3 — closed: immutable manifests and complete lineage

The external evidence is coherent:

| Manifest | Mode / metrics | Evidence rows | File SHA-256 |
|---|---|---:|---|
| `fs024-remediation-acquisition-20260818-8c4c917` | `live_cache_first`; 3 live, 0 cache, 3 errors, 0 retries | 3 | `a3f6fe9383efeda687c18d3c6c232f7288604aca0489c3cf52d5feccfd997c86` |
| `fs024-remediation-replay-20260818-8c4c917` | `replay`; 0 live, 14 cache, 0 errors, 0 retries | 17 | `dc4b8747ec30febe7a417ab77dc8c1d81c4b5af7fd5e4a6edb3b52b450a6c5c7` |

The acquisition rows are all `from_cache=false`, HTTP 403, and refer to the
three identities above. Replay has 17 unique request hashes and 17 capture
ids, and every request hash equals the final plan's computed identity. The
two files have distinct directories, inodes, creation times, and digests.
Synthetic reuse tests prove an existing run id is refused before quota can be
spent and its bytes remain unchanged.

The old `fs024-live-discovery-20260818` manifest still records the overwritten
zero-live fold-in and is not misrepresented as an acquisition record. The new
acquisition manifest covers only the three corrected calls; the docs also
retain the one preceding malformed-auth 401, for four remediation live calls
in the append-only ledger. This is honest loss accounting rather than a
fabricated recovery of the original 14-call run.

## Fresh gates

| Check | Fresh result |
|---|---|
| Focused FS024 config/manifest/discovery/catalog/request/notebook tests | 83 passed |
| Independent cached-401 write-enabled probe | account abort; no run directory |
| Independent external lineage audit | plan 17; acquisition 3; replay 17; ledger 401/403/403/403 |
| Notebook top-to-bottom isolated real-cache replay | 17 probes; 0 live; 14 hits; 0 errors; catalogs 2,246 / 439 / 710 |
| `ruff format --check .` | 332 files already formatted |
| `ruff check .` | all checks passed |
| `mypy --strict src/lasr` | success, 171 source files |
| `CI=1 pytest -q` | 2,891 passed / 23 skipped / 22 xfailed |
| Machine manifest / notebook JSON | parse clean; 95 operation rows |
| `git diff --check 0c0ae86..45eae8d` | clean |
| PR #87 CI | 8/8 successful at exact head `45eae8d` |

## Non-blocking observations

- **VF-FS024-NB1 — run envelope times are invocation stamps.** Both external
  manifests have identical `started` and `finished` values because
  `run_discovery` passes its `now` argument for both. In the acquisition file,
  `finished` precedes the three live retrieval timestamps by roughly one
  second. The immutable per-probe retrieval times, ledger timestamps, capture
  ids, and metrics keep the evidence auditable, so this does not reopen
  VF-FS024-3; future manifests should record an actual completion timestamp.
- **VF-FS024-NB2 — replay still needs a writable telemetry sink.** Cache hits
  emit telemetry below `raw/_telemetry`, so verification used an isolated
  writable copy rather than the shared read-only evidence root. The replay
  computation remained deterministic and network-free.
- The two remediation manifests identify producing code `8c4c917`, while the
  final reviewed checkpoint is `45eae8d`. The intervening production changes
  are documentation/rendering wording only; no evidence is relabeled as if it
  had been acquired by the final checkpoint.

## Disposition

FS024 satisfies its durable charter and is ready for the orchestrator's
VERIFIED / MERGE_READY adjudication. This verifier did not merge PR #87.
