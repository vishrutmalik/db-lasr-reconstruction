# Verification Report — G009 (P3 LASR 2014 evidence extraction)

- **Goal ID:** G009
- **GitHub issue:** #9
- **Branch:** `agent/paper-researcher/G009-p3-lasr2014-evidence`
- **Commit reviewed:** `bc1125317dd53ee52022c395fa6baafd2824fe92` (bc11253; prior content commit cc9dc4b also inspected)
- **Pull request:** #43
- **Verdict:** **PASS**

## Acceptance criteria reviewed

1. goals.md G007–G010 block: all §13.1 fields present or explicitly NOT_DISCLOSED; ≥1 citation
   per material claim; formulas transcribed; contradiction candidates flagged for G011;
   evidence-matrix rows not prose; owned path `docs/evidence/p3_lasr_2014/` only.
2. `skills/paper-evidence-extraction/SKILL.md` exit criteria: deliverables complete
   (extraction.md, formulas.md, evidence_rows.md, contradiction_candidates.md,
   open_questions.md); worktree clean; no full page dumps committed; quotes ≤15 words;
   quantitative invariants (label fractions sum to 1; dimensional coherence);
   title/date verified against input_manifest.md; no later-paper imports.
3. Additional assignment requirements: three variants (LASR / LASR-HC / LASR-HF) cleanly
   separated; publication date used consistently as 2014-12-01 (filename `20140101` wrong).

## Commands executed

- `git log --oneline` / `git status --porcelain` / `git branch --show-current` in the worktree
  — HEAD = bc11253, clean tree, correct branch.
- `git ls-files | grep -i "page\|_pages"` — no page dumps in the index; `git log --all
  --name-only -- 'docs/evidence/p3_lasr_2014/_pages*'` — never committed. Both content
  commits (cc9dc4b, bc11253) touch only the five deliverable files.
- Full-text re-extraction of all 80 PDF pages with pypdf into the session scratchpad
  (`inputs/papers/20140101_Rise of the Machines III.pdf`); page-numbering alignment
  confirmed (paper p.N = PDF page N); title page confirms "Date 1 December 2014".
- Independent recomputation of the formulas.md §4 micro-example (memberships, class
  masses, log-odds, mass check, predictions) — see invariants below.
- Regex scan of all deliverables for verbatim quotes >15 words.
- sha256 of the PDF matches `input_manifest.md` (`abab648b…fbceb8`).

## Tests passed / failed

Research goal — no code tests. Verifier spot-checks below stand in (skill requires ≥10;
24 performed). **Failed: none.**

## Citation spot-checks (claim → page → verdict)

1. Title-page date "1 December 2014"; filename date wrong → p.1 → **PASS** (2014-12-01 used
   consistently across extraction.md header, evidence_rows.md, formulas.md).
2. Hard-bin weak learner h(x)=½·ln((W₊ʲ+ε)/(W₋ʲ+ε)); ε "a small value" role; Q "equals to
   five in our case"; y=±1 for top/bottom-30% → p.16 → **PASS** (all verbatim on p.16).
3. Linearized learner: bin log-odds × max(0, 1−dist(f(x),j)); dist = distance to quintile
   center "normalized by the width of the quintile"; "linear interpolation for two connected
   quintiles"; "the new weak classifier is a continuous function" → p.17 → **PASS**.
4. Worked example: 45th-percentile stock → "25% of its weight to the second quintile and 75%
   to the third"; centers of quintiles 2/3 at 30th/50th percentiles → p.17 → **PASS**
   (formulas.md check m₂=1−15/20=0.25, m₃=1−5/20=0.75 recomputed, correct).
5. Fractional memberships ALSO used for training class masses ("in the training process …
   each stock would contribute the weight based on the distance from the center"; W±ʲ
   equation; "will only be influenced by the consecutive two quintiles") → pp.17–18 → **PASS**.
6. Tail-leakage micro-example (formulas.md §4) → constructed by researcher, arithmetic
   verified independently → **PASS** (all 10 stocks' memberships, all five W₊ʲ/W₋ʲ pairs,
   h₁…h₅ = −0.760/0.000/+0.490/−0.833/−0.129, mass total 0.95 with 0.05 tail leakage from
   s1/s10, and predictions +0.368 (p=45), +0.159 (p=55), −0.456 (p=2) all recomputed exactly).
7. LASR-HC: labels = "next quarter's return"; look-ahead guard "we use the data up to three
   months prior to the rebalance date"; precursor re-trained quarterly; HC refit cadence not
   stated → p.58 → **PASS** (incl. the AMBIGUOUS classification — p.58 indeed never states
   HC's own refit frequency).
8. LASR-HF/Weekly: "We re-fit the model weekly"; 1-week forward-return labels; four
   components incl. hedge weeks "in the previous three years" → p.66 → **PASS**.
9. LASR-Technical "only with around 40 technical factors" (p.68); LASR-Weekly = weekly model
   on the fundamental factor set; blend "combine our LASR-Weekly and LASR-Technical models"
   → LASR-HF (p.74); correlations 69/21/30 (Figure 164, p.71) → **PASS**.
10. HF costs: 10bps per trade (p.71); LATAM fn.17 "around (or above) 50bps"; 0/5/10bps
    sensitivity (Figures 170–171, p.74); turnover ~1,200%/max ~1,600% monthly two-way (p.70)
    → **PASS**.
11. Execution timing: fn.18 "Unrealistic assumption" (close-to-close) vs fn.19 "A more
    realistic assumption" (next-day open), p.72; Technical Sharpe "plunge by over half"
    (p.72); trained open-to-close recovers (Figure 169, p.73); HF trained AND evaluated on
    next-day open → pp.71–74 → **PASS**.
12. p.27 portfolio spec: L/S market neutral, 2x leverage, 4% target vol, 1.5% max stock,
    beta ≤0.1, sector/country ≤10%, 30% one-way (60% two-way) monthly turnover, 20bps
    one-way, Axioma daily MH fundamental risk models + R/Java API, backtests from 1999
    (LATAM end-2006) → **PASS** (every bullet verbatim on p.27).
13. Figure 44 (p.27) per-region table → **PASS** — all 54 numbers in extraction.md match the
    PDF exactly (US 8.26%/1.56/14.0%/5.60%/2.50/12.00% … ANZ 8.68%/0.72/6.0%/4.00%/1.50/6.23%).
14. Realistic cost tiers 30/40/50bps (US small-cap / Em EMEA / LATAM) → p.63 → **PASS**;
    capacity: "can't trade (either buy or sell) more than 10% of 20-day ADV", AUM held at
    US$100M and US$5B → p.64 → **PASS**.
15. Quintile-vs-tercile discrepancy: Figure 121 axes "Quintile 1…5" (US, p.52) vs Figures
    122–124 axes "Tercile 1…3" (EUxUK p.53, Japan p.53, AxJ p.54) → **PASS** (C-3 is real;
    INFERRED classification appropriate — axis labels only, no stated rule).
16. Monthly training windows: 12M baseline / 12-yr seasonal / 1M short-term (p.9); hedge =
    bottom-half months of "the past 10 years" (p.14) → **PASS**. fn.5 (p.9) does read
    "January-2000 to January-2011" while building on 2012-12-31 → C-4 confirmed real.
17. Ensemble weighting: ex-US "simply equally weighted"; US dynamic by recent performance;
    fn.6 "not very different from equal weights"; N-LASR2 "essentially an average of our
    strong classifiers" (p.14) → p.9, p.14 → **PASS**.
18. Universe: "Russell 3000 for US, S&P/TSX composite index for Canada, and S&P BMI" for the
    rest; ">10,000 stocks", MSCI ACWI coverage; EM split into LATAM + Emerging EMEA, emerging
    Asia merged into AxJ → p.22 → **PASS** (C-2 confirmed).
19. Serial-correlation definition (p.25); LASR autocorrelation "43% and 16%" higher than
    N-LASR1/N-LASR2 (p.26) → **PASS**.
20. C-1: P3 references list "The rise of the machines II … 24 February 2013" (p.77) vs
    input_manifest.md P2 title-page date 23 January 2013; p.15 live performance "after
    January 2013" → **PASS** (genuine internal/cross-source discrepancy, correctly flagged).
21. Validation split: US design sample "1987 to mid-2012"; mid-2012+ and non-US "as true
    out-of-sample" → p.4 → **PASS**.
22. Labels 30/40/30 with fn.4 ("somewhat arbitrary", all stocks scored in prediction);
    Figure 2 lists 70 named factors, styles Value 1–19 / Growth 20–26 / Mom-Rev 27–32 /
    Sentiment 33–45 / Quality 46–63 / Technical-Exotic 64–70 → p.6 → **PASS**.
23. Neutralization: labels top/bottom "30% in each sector" (pp.10–11); country neutralization
    + fn.7 regional grouping (p.11); size split at "median float-adjusted market cap" (p.12);
    beta "performed for high and low beta universes, broken down by the median" (p.13);
    size/beta US-only ("sample size is not big enough in other regions", p.13) → **PASS**.
24. Qualitative boosting (p.8): misclassified weights increased / correct decreased; factor
    weights decline exponentially; "beyond 10 or 20 are essentially minimal"; strong
    classifier = sum of weak classifiers, real-value confidence score; global blend Sharpe
    5.5x / maxDD −0.6% (p.40); horse race CART/RF/ANN/SVM, AdaBoost best balance (pp.50–55);
    N-LASR live June 2012 with US/Canada "significant performance downgrade" (p.10) → **PASS**.

## Code paths inspected

- `docs/evidence/p3_lasr_2014/extraction.md` — all 40 §13.1 items present, each with
  citation + classification + consequence; NOT_DISCLOSED items (eligibility, outlier method,
  borrow, stopping conditions, ε value, signal vol-scaling) all carry searched-where notes.
- `docs/evidence/p3_lasr_2014/formulas.md` — hard-bin + linearized learner, fractional
  masses, tail-leakage analysis, micro-example, 10 technical formulas, explicit
  not-in-P3 table.
- `docs/evidence/p3_lasr_2014/evidence_rows.md` — **recounted: exactly 38 rows**
  (P3-01…P3-38), evidence-matrix format, source=P3, date 2014-12-01.
- `docs/evidence/p3_lasr_2014/contradiction_candidates.md` — **recounted: exactly 9**
  (C-1…C-9), flag-only, resolution deferred to G011.
- `docs/evidence/p3_lasr_2014/open_questions.md` — 12 questions (Q1–Q12).
- Commit diffs cc9dc4b and bc11253 — only the five deliverables under the owned path.

## Edge cases attempted (missed-topic probes)

Adversarial grep of the full 80-page extracted text, attempting to falsify NOT_DISCLOSED:
- **ε numeric value:** searched `epsilon / 0.0001 / 1e- / small value` — no numeric value
  anywhere. NOT_DISCLOSED holds.
- **Weight-update equation:** searched `exp( / alpha / misclassif / error rate / weighted
  error / iteration` — only the qualitative p.8 prose exists; the equations live in the
  image-only Figures 6/21. NOT_DISCLOSED holds.
- **Eligibility filters:** searched `eligib / exclude / screen / filter / minimum price /
  ADR / REIT / liquid` — nothing beyond index membership. NOT_DISCLOSED holds.
- **Borrow assumptions:** searched `borrow / short avail / stock loan / lending` — zero hits.
  NOT_DISCLOSED holds.
- **Stopping conditions / round count:** searched `iteration / rounds / stopping / converge /
  terminat` — nothing beyond "beyond 10 or 20 … minimal" (p.8). NOT_DISCLOSED holds.
No missed disclosures found.

## Image-only exhibit verification

- **Figure 6 (p.9)** and **Figure 21 (p.18)**: confirmed — extracted page text contains only
  the figure caption and source line; no algorithm text extractable. The
  UNREADABLE_EXHIBIT flags and the decision to defer the weight-update/selection-objective
  formulas to P1 (rather than guess) are correct and match the skill's rules.

## Technical-formula transcription check

All 10 Figure 160 (p.69) formulas compared token-by-token: W%R, CLV, AD, PPO, PVO,
SO (n=39, SMA(SO), SMA(SMA(SO))), MACD (12/26/9), BB width, CMF (N=20), RSI (14) — all
match, including the as-printed `Fast_EMA` denominator for PPO/PVO (correctly transcribed
as printed and flagged as Q6 rather than silently "fixed").

## Leakage risks checked

- PIT refit claim: p.6 verbatim "only using data available as of that given point-in-time" ✓.
- LASR-HC 3-month training-data lag correctly extracted as a look-ahead guard equal to the
  label horizon (p.58) ✓.
- HF next-day-open training AND evaluation correctly captured (pp.71–74); extraction's
  consequence (HF pipeline must be open-to-close end-to-end) is the right leakage-aware
  reading ✓.
- Seasonal-window fn.5 one-year-lag anomaly caught (C-4) rather than papered over ✓.
- No later-paper (P1/P2/P4) details imported as P3 facts; P1-deferred items are explicitly
  marked "do not import silently" ✓.

## Quantitative invariants checked

- Label fractions 30/40/30 sum to 1 — stated and checked in extraction.md ✓.
- Micro-example arithmetic — independently recomputed in full; every membership, class
  mass, log-odds value, the 0.95 total-mass/tail-leakage figure, and all three predictions
  are correct ✓.
- Interior mass conservation (adjacent memberships sum to 1 for c₁≤p≤c_Q) and tail
  leakage (<1 outside outer centers) — verified algebraically from the p.17 dist
  definition; the tail-leakage property is a genuine consequence of the literal formula
  and is correctly raised as Q1 ✓.
- Dimensional coherence of h (log-odds, dimensionless memberships) ✓.
- Bin centers c=(10,30,50,70,90), width 20 for Q=5 — consistent with the paper's stated
  30th/50th-percentile centers ✓.

## Blocking findings

**None.**

## Non-blocking recommendations

1. **Quote length (copyright):** six quoted passages exceed 15 words (16–22 words incl.
   ellipsed spans; longest contiguous verbatim run 18 words): extraction.md — rank-IC
   definition (Return definition, p.10 quote, 16w), observation-weight-update sentence
   (p.8 quote, 18w), serial-correlation definition (p.25 quote, 17w), strong-classifier
   output sentence (p.8 quote, 22w incl. ellipsis), low-turnover-LASR rationale (p.58
   quote, 16w incl. ellipsis); formulas.md — dist definition (p.17 quote, 16w). Trim or
   paraphrase to ≤15 words each.
2. **C-4 wording:** "mislabels the count-window" is itself imprecise — Jan-2000…Jan-2011
   inclusive IS 12 Januaries; the only anomaly is the one-year lag (skipping Jan-2012).
   Tighten the description before G011 consumes it.
3. **Figure 164 gloss:** extraction.md calls it "Model score correlations"; the paper labels
   it only "Correlation matrix" and context (after-cost performance discussion) suggests
   return correlations. Drop the word "score" or mark INFERRED.
4. **RSI transcription policy inconsistency:** the exhibit prints `RSI = 100 - (100/1+RS)`;
   formulas.md silently normalizes to `100 − 100/(1+RS)`. Correct reading, but PPO/PVO got
   the "as printed + flag" treatment — add a one-line note for consistency.
5. **Global-blend citation:** the −0.6% maxDD sentence in the paper cites "Figure 96"
   (apparent paper typo for Figure 101); extraction.md cites "Figures 99–101". Harmless,
   but a footnote would inoculate G011 against a false discrepancy.

## Evidence for the verdict

- Branch/SHA/cleanliness verified directly in the worktree; no `_pages/` dumps anywhere in
  history for this path.
- 24 citation spot-checks (list above) against a fresh pypdf re-extraction of the source
  PDF — zero factual mismatches; all quoted fragments located verbatim on the cited pages.
- All 40 §13.1 items present with per-variant (LASR / LASR-HC / LASR-HF) separation where
  the paper differentiates them; all absences are NOT_DISCLOSED with searched-where notes,
  and five adversarial probes failed to find any missed disclosure.
- Counts re-verified: 38 evidence rows, 9 contradiction candidates, 12 open questions.
- The single most implementation-critical artifact — the linearized weak-learner spec and
  its worked example — was recomputed end-to-end and is exact.
