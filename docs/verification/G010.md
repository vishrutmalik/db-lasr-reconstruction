# Verification report — G010

- **Goal ID:** G010 — Paper 4 (N-LASR 2020) evidence extraction
- **GitHub issue:** #10
- **Branch:** `agent/paper-researcher/G010-p4-nlasr2020-evidence`
- **Commit reviewed:** `b2e20987f2580e32e26b2ba44748517889c3ccf9`
- **Pull request:** #44 (metadata not independently confirmable — `gh` unavailable in verifier environment; branch and SHA verified locally)
- **Verdict:** **PASS**

## Acceptance criteria reviewed

From goals.md detail block G007–G010 and `skills/paper-evidence-extraction/SKILL.md`:

1. All MASTER_PROMPT §13.1 items present in `docs/evidence/p4_nlasr_2020/` or
   explicitly NOT_DISCLOSED with searched-where.
2. ≥1 citation per material claim; classification EXPLICIT / INFERRED /
   ASSUMED / MODERNIZED disciplined.
3. Formulas transcribed with variable definitions and worked micro-example.
4. Contradiction candidates flagged for G011 (not resolved).
5. Quantitative invariants: label fractions sum to 1; weight normalization
   preserved; formulas dimensionally coherent.
6. Copyright discipline: minimal quotes, no bulk page dumps committed.
7. Challenger configurations captured (special-attention item in the G010 brief).
8. Deliverables complete; worktree clean; verifier can trace every claim.

## Commands executed

- `git log --oneline` / `git status --short` in the worktree → HEAD = b2e2098,
  branch correct; only untracked item is the sanctioned
  `docs/evidence/p4_nlasr_2020/_pages/` cache (repo-ignored on main via
  fbc0b07; not yet in this branch's .gitignore — see non-blocking #3).
- `git ls-files | grep _pages` → empty; `git log --stat` over both G010
  commits (58a67d4, b2e2098) → only the five deliverable .md files. **No page
  dumps in any commit.** ✓
- `python3` + `hashlib`: SHA-256 of
  `inputs/papers/20200423_Return of the Machines.pdf` =
  `e9577378…336e6f`, matches `input_manifest.md` row P4; pypdf reports 25
  pages, matches. ✓
- `python3` + `pypdf`: independently extracted all 25 pages to the verifier
  scratchpad; every spot-check below performed against this fresh extraction,
  not the implementer's cache.
- `python3` recomputation of the bin-membership micro-example and the Step 4
  arithmetic (see invariants).
- Regex scan of all five deliverables for verbatim quotes >15 words.
- Row/classification counts recomputed with `grep -c` / `awk` (see below).

## Tests passed / failed

n/a — documentation goal; no code or test suite. All verifier probes passed;
findings limited to the non-blocking items listed at the end.

## Citation spot-checks (claim → page → verdict)

All checked against my own pypdf extraction of the PDF. 20 material claims
(assignment minimum 12), prioritizing the assignment's list.

| # | Claim (deliverable) | Page | Verdict |
|---|---|---|---|
| 1 | Weak learner: K=5 bins, centers [0.1,0.3,0.5,0.7,0.9], inverse-distance membership over the 2 closest bins normalized to sum 1; log-ratio bin scores; OLS straight-line fit of the 5 bin scores on bin centers with printed design matrix [1 c_k] (extraction §22, formulas F5/F8/F9) | pp.17–18 Steps 5, 8, 9 | ✓ verbatim ("retaining only the 2 closest bins", "log of the ratio of weighted sums", design matrix rows 1 0.1 … 1 0.9 all present) |
| 2 | Monotonic gate: "If β < 0, exit the algorithm as at the current iteration we predict a non-monotonic relationship"; forecast φ̂ = γ + β·s if β ≥ 0; fn 48 forecast can be negative (extraction §27, formulas F10) | p.18 Step 10 + fn 48 | ✓ verbatim; "exit the algorithm" wording confirmed, stop-vs-skip ambiguity is real |
| 3 | Factor selection = alpha with highest weighted correlation between ranked scores (Step 1) and rank-adjusted returns (Step 2), weights = boosting weights (extraction §23, formulas F6) | p.18 Step 6 | ✓ verbatim; no error-rate objective appears anywhere in Steps 6–12 |
| 4 | Weight update w_{i+1,j} = w_{i,j}·e^{−l_j·φ̂_j}, then normalized to sum 1; inflates incorrect forecasts; no learner weight α anywhere (extraction §24, formulas F11) | p.18 Step 11; p.17 Step 4 (init 1/N) | ✓ verbatim; confirmed no α_i term exists in the appendix |
| 5 | 114 features from FactSet, 6 categories; Figure 3 split Profitability 32 / BSS 28 / Efficiency 21 / Value 17 / Growth 12 / Technical 4 (extraction §6, row E-P4-03) | p.3 §2.1; p.4 Fig 3 | ✓ all six counts verbatim; 32+28+21+17+12+4 = 114 ✓ |
| 6 | Dual clock: alphas updated weekly; "each model is re-calibrated every 4 weeks in order to reduce turnover" (fn 16 parameters fixed between); weekly portfolio rebalance, daily mark-to-market (fn 17) (extraction §4–5, row E-P4-13) | p.3 §2.1; p.5 §2.1 + fn 16–17 | ✓ verbatim |
| 7 | Target: sector-region-neutral 4-week forward returns, divided by 5-year weekly-return vol (fn 12), then ranked; **§2.1 order (neutralize→vol-scale) vs Appendix Step 2 order (vol-scale→neutralize) conflict** (extraction §12/14, CC-P4-06) | p.3 §2.1 + fn 11–12 vs p.17 Step 2 | ✓ both passages read exactly as documented; the order conflict is genuine |
| 8 | Fundamentals "lagged by 3 months" before alpha computation; technical factors (Momentum, Volatility, Beta, Market Cap) exempt from sector-region neutralization; 11 × 3 = 33 couples (fn 9); N-LASR re-ranks, others z-score (extraction §7–8, rows E-P4-04/05/06/32) | p.3 §2.1 + fn 9–10 | ✓ verbatim |
| 9 | Hyperparameters trained 1996–2002, all posterior results 2003–2020 out-of-sample; all 4 sub-portfolios share one configuration; N-LASR hyperparameters "kept as per original research reports" (extraction §37, rows E-P4-14/15) | p.5 §2.2 | ✓ verbatim |
| 10 | Figure 7 headline: NLASR Sharpe 1.64, CAGR 5.48, vol 3.34, maxDD 5.95, daily turnover 3.87; challenger order XGB[C] 1.47 … EW 0.94; paired t-tests, 36 pairs, fn 26 (extraction extras, row E-P4-29) | p.7 Fig 7 + fn 26 | ✓ entire table matches, all 9 Sharpe values verbatim |
| 11 | Costs 5bp per dollar (10bp spread), 50bp borrow; regional 10bp/100bp (fn 28); sensitivity sweep one-way cost 5→20bp (Fig 13 axis 5–20) at 50/100bp borrow; delay t+2→t+20 business days (Fig 14 axis 2–20), linear decay, no market impact, worst-case Sharpe >1.0 (extraction §34–36, rows E-P4-25/26/27) | p.6 §2.2; p.9 §4.2 + fn 28; p.10 Figs 13–14 | ✓ verbatim, chart axes confirmed from extracted tick labels |
| 12 | Step 4 arithmetic typo: paper prints "N = 0.6 × 1,200 × 52 = 37,740"; true product 37,440 (formulas F4, OQ-P4-08) | p.17 Step 4 | ✓ paper prints 37,740; recomputed 0.6×1200×52 = 37,440 — typo confirmed, correctly flagged not silently corrected |
| 13 | I = 30 boosting rounds is INFERRED only: Step 12 says "until the maximum number of iterations is reached" with no number; sole numeric source is XGB fn 19 "We set that as 30 for consistency with N-LASR" (extraction §26, row E-P4-20) | p.18 Step 12; p.5 fn 19 | ✓ grepped all 25 pages: no explicit N-LASR iteration count exists anywhere; INFERRED classification is exactly right |
| 14 | Challenger configs: RF N=10 bootstrap, √k features/split, depth {2,3,4}→3 (fn 18); XGB 30 trees, depth 2, learning rate validated (fn 19); NN grid h{1,2,3}/u{8,16,32}/ReLU-tanh/d{0,0.1,0.3,0.5}/e{10,20,100}, bottleneck halving, selected h=1,u=8,d=0.3,e=20,ReLU (fn 20); NNLS no hyperparameters; monotonicity via non-neg coefs (NNLS/NN) and explicit fitting parameter (XGB/RF) (extraction extras, row E-P4-31) | p.5 §2.2 + fn 18–20 | ✓ every value verbatim |
| 15 | Labels: top 30% → +1, bottom 30% → −1, rest discarded; Appendix retains ranks <0.3 or >0.7; 360/360 example at 1,200 stocks (extraction §17, formulas F3) | p.4 §2.1; p.17 Step 3 | ✓ verbatim |
| 16 | 4 training models (5y, 1y, seasonal same-month 10y fn 13, hedge = worst 50% of weeks in prior 10y by 3-model aggregate P&L); final signal = equal-weight average of the 4 (extraction §18–21, §28, formulas F14) | p.4 §2.1 + fn 13 | ✓ verbatim |
| 17 | Universe: "80% most liquid stocks in the MSCI World universe - roughly 1,200 stocks"; 8 regional S&P BMI universes fn 25 (extraction §2) | p.2 §1; p.7 fn 25 | ✓ verbatim |
| 18 | Portfolio: signal-weighted quintile L/S (fn 21 top/bottom 20%); positions = residuals of signal-on-beta regression; betas 3y weekly; post-adjustment market corr in [−0.15,0.15]; fn 22 garbled "top and quintile stocks" as flagged (extraction §11/30, rows E-P4-23/24) | p.6 §2.2 + fn 21–22 | ✓ verbatim incl. the garbling |
| 19 | §6 record: not "paper trading" (p.13) vs §1 "paper trading performance since" (p.2) tension; monotonicity constraints April 2019 tracked to end-March 2020; post-Jan'15 Sharpes 0.76/0.46/0.78 (HFR/Eurekahedge/N-LASR; fn 43 daily-vol basis 0.76); Apr'19–Mar'20: −0.69/−0.85/−0.34; PoD AR(p≤5, AIC)+GMM, 10,000 paths (extraction §38, OQ-P4-10) | p.2; pp.13–15 + fn 39–43 | ✓ all numbers and both conflicting wordings verbatim |
| 20 | Breadth: f ∈ [0.01,0.05,0.1,0.2,0.3,…,1], semi-annual random redraw, 10 repeats per f, gross of costs; ~240 stocks (20%) for notable edge; 100-run 50-of-114 alpha exercise: ranked 1st 65%, top-3 96%; turnover "3.8% daily, or 19% weekly" / "~20%" weekly; Figure 11 correlations (0.44/0.51/0.50/0.67/0.58/0.89) and Figure 12 regional aggregate Sharpes incl. World 1.68 vs Fig 7's 1.64 (OQ-P4-09) (extraction §33/39, extras) | p.6 §3; p.8 §3; p.9 §4.1–4.2 Figs 11–12; p.10 §4.3 | ✓ every value verbatim; the 1.68-vs-1.64 discrepancy is real and correctly left open |

20/20 spot-checks passed. No fabricated citation, page error, or misquoted
number found. Long-only (leg Sharpes 1.06/0.41/0.53, fn 30–32), traditional-
factor comparison (1.28 vs 1.47, fn 33–34, Quality 38%/Momentum 14%), and the
"EBIT/EV, EBITDA/EV and FCV/EV" garble (p.12) were additionally confirmed in
passing while reading pp.11–12.

## Numeric recomputation (required checks)

Independently recomputed in python3:

- **Bin membership, s = 0.15:** distances to [0.1,0.3,0.5,0.7,0.9] =
  [0.05,0.15,0.35,0.55,0.75]; two closest = bins 1–2; inverse distances
  [20, 6.667]; normalized → **[0.75, 0.25, 0, 0, 0]** — matches the paper's
  own example (p.17 Step 5) and formulas.md F5 exactly. ✓
- **Step 4 arithmetic:** 0.6 × 1,200 × 52 = **37,440** ≠ paper's 37,740 —
  typo confirmed exactly as flagged (F4, OQ-P4-08). ✓
- **F11 worked example:** e^{0.35} = 1.4191 → 10⁻⁴·e^{0.35} ≈ 1.419×10⁻⁴,
  matches formulas.md; wrong-sign datapoint is upweighted as required. ✓

## Code paths inspected

n/a (documentation-only goal). Files reviewed in full:
`docs/evidence/p4_nlasr_2020/{extraction,formulas,evidence_rows,contradiction_candidates,open_questions}.md`.

## Edge cases attempted (adversarial missed-topic probes)

Tried to prove the extraction missed §13.1 content by grepping my own
full-text extraction of all 25 pages:

- **Smoothing constants:** "smooth" — 0 hits anywhere → item 25 / E-P4-21
  NOT_DISCLOSED correct; only fn 46–47 acknowledge the ψ=0 corner case
  without a rule, exactly as documented.
- **Prediction normalization:** "normali" — only Step 11 weight
  renormalization and Step 5 bin-value normalization; no composite-signal
  z-scoring/re-ranking anywhere → item 29 NOT_DISCLOSED correct.
- **Liquidity metric:** "liquid" — 9 substantive hits, none defining the
  measure, window, or refresh of the "80% most liquid" screen → item 3
  NOT_DISCLOSED-with-searched-where correct (OQ-P4-01).
- **Rebalance weekday:** "Friday/Monday/weekday" — 0 hits → OQ-P4-07 correct.
- **Missing-data handling / currency / survivorship / delistings:** 0
  methodological hits → OQ-P4-11/13 correctly treated as open, nothing missed.
- **Explicit iteration count:** all "iteration"/"30" occurrences reviewed —
  the only 30 tied to rounds is XGB fn 19 → INFERRED status of I=30 stands.

§13.1 coverage recount: all 40 §13.1 bullets map 1:1 to extraction.md items
1–40 (items 25 fully NOT_DISCLOSED; 3, 7, 9, 13, 29, 32 explicitly split
EXPLICIT/NOT_DISCLOSED with searched-where). Six special-attention extras
(challenger configs, training-model diagnostics, category weights, long-only,
traditional factors, UNREADABLE_EXHIBIT register) present. No gaps.

## Leakage risks checked

n/a for code; documentation-level: validation/test split (1996–2002 vs
2003–2020) captured with the correct implementation consequence (HP search
confined to validation, row E-P4-14 tagged "leakage test"); the 3-month
fundamental lag and t+2 execution lag are extracted with PIT consequences
(E-P4-04, E-P4-26); the hedge model's dependency on the other 3 models'
P&L history — an easy place to leak — is explicitly flagged (item 21).

## Quantitative invariants checked

- Label fractions 30/40/30 sum to 1 ✓ (p.4 and p.17 verified; F3).
- Bin memberships normalized to sum 1 per datapoint ✓ (p.17 Step 5; F5,
  recomputed).
- Boosting weights renormalized to Σ=1 each iteration, stated by the paper
  and preserved in the transcription ✓ (p.18 Step 11; F11).
- Dimensional coherence: θ log-ratio dimensionless; φ̂ = γ+β·s dimensionless;
  update exponent dimensionless ✓ (formulas.md check section re-verified).
- Feature counts 32+28+21+17+12+4 = 114 ✓.
- Evidence row count: **34 rows** E-P4-01…34 ✓; classification split
  32 EXPLICIT / 1 INFERRED (E-P4-20) / 1 NOT_DISCLOSED (E-P4-21) recomputed ✓.
- **5 CR candidates** (CR-P4-a…e) in evidence_rows.md ✓; **10 contradiction
  candidates** (CC-P4-01…10) in contradiction_candidates.md ✓; 17 open
  questions (OQ-P4-01…17) ✓.
- Classification discipline: the single INFERRED row states its inference
  basis (fn 19); NOT_DISCLOSED items name where the implementer searched;
  proposed defaults live in open_questions.md tagged ASSUMED; no ASSUMED
  content presented as fact; UNREADABLE_EXHIBIT used for chart-only values
  instead of guessed numbers ✓.
- Manifest integrity: PDF SHA-256 recomputed, matches input_manifest.md P4
  row; 25 pages; title/date/author check on p.1 confirmed (incl. the
  cosmetic "The Return of the Machines" vs manifest title, CC-P4-10) ✓.
- Cross-check of paper-internal anomalies claimed by the extraction: the
  §7-vs-§6 section-numbering slip (p.2 vs pp.13/15) and the in-text vs
  caption figure-number off-by-one on pp.9–10 both verified real; the
  extraction consistently uses printed captions ✓.

## Blocking findings

None.

## Non-blocking recommendations

1. **Six verbatim quotes exceed the 15-word discipline** (16–25 words;
   extraction.md lines 70, 125, 144, 184, 240, 246 — the worst are the
   hedge-sample quote at 24 words and the validation-split quote at 25
   words). extraction.md's own header claim "Quotes ≤15 words" is therefore
   inaccurate. No bulk page dumps are committed and total quoted volume is
   minimal, so — consistent with the G007 verification precedent — not
   blocking, but trim/paraphrase these six and correct the header in a
   follow-up.
2. PR #44 could not be checked from this environment (`gh` missing);
   orchestrator should confirm the PR head equals b2e2098 before merge.
3. This branch predates main's fbc0b07 (`.gitignore` rule for `_pages/`), so
   `git status` shows the cache as untracked here. Harmless (never staged,
   ignored after merge), but worth knowing when eyeballing worktree
   cleanliness pre-merge.
4. contradiction_candidates.md CC-P4-01/02/04 lean on preliminary reads of
   the P1–P3 sibling caches; they are properly labeled as preliminary with
   resolution deferred to G011 — G011 should re-derive the P1–P3 side from
   the merged G007–G009 extractions, not from this file.
5. formulas.md F5 states the s = c_k zero-distance edge case is
   NOT_DISCLOSED; when implementing, also decide the s exactly midway
   between two centers tie case (e.g. s=0.2) — worth adding to OQ-P4-02's
   scope.

## Evidence for verdict

Fresh-context review at SHA b2e2098. PDF hash re-derived and matched to the
manifest; all 25 pages re-extracted independently. 20 citation spot-checks —
covering every priority item in the verification brief (linear-fit weak
learner, β≥0 "exit" gate, weighted-correlation selection, α-free weight
update, 114-feature split, dual clock, target-order conflict, 3-month lag,
1996–2002/2003–2020 split, Sharpe 1.64 / turnover 3.87%, cost and delay
sweeps, 37,740 typo, I=30 inference, challenger configs) — all passed with
zero citation errors. The paper's own micro-example ([0.75,0.25,0,0,0]) and
arithmetic typo were reproduced by independent computation. Coverage of all
40 §13.1 items confirmed by 1:1 recount; six adversarial missed-topic probes
failed to find anything the extraction missed; 34+5 and 10 counts confirmed.
No page dumps exist in any commit. Findings are limited to quote-length
trimming and process notes — none affect correctness, classification
integrity, or implementability.
