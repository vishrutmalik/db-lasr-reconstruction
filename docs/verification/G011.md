# Verification report — G011

- **Goal ID:** G011 — Cross-paper contradiction register + 7 model-version specs
- **GitHub issue:** #11
- **Branch:** `agent/quant-reviewer/G011-contradictions-model-specs`
- **Commit reviewed:** `bea427d75d88bb6eff736122b4adc8b84e5a150e`
- **Pull request:** #50 (metadata not independently confirmable — `gh` unavailable
  in the verifier environment; branch and SHA verified locally)
- **Verdict:** **PASS**

## Acceptance criteria reviewed

From the goals.md G011 row, issue #11 assignment brief, MASTER_PROMPT §13.2
(resolve versions independently, never merge silently), §13.3 (contradiction
register: both sides, exact sources, default only when necessary, alternatives
configurable, ablation/sensitivity test each), §8 (terminology discipline),
and the verifier/pr-verification skill:

1. Diff confined to owned paths; worktree clean at the stated SHA.
2. All 42 contradiction candidates (P1 14, P2 9, P3 9, P4 10) accounted for
   in the candidate→CR mapping.
3. Register entries: accurate two-sided citations, defensible triage class,
   real config parameter, sensitivity/ablation test.
4. Spec provenance: EXPLICIT traces to a real evidence row; IMPORTED_FROM_P1
   genuinely absent from the importing paper's evidence; ASSUMED carries an
   A-G011-nn candidate with a config knob.
5. No silent version merging in any spec; modernized deltas labeled.
6. Executability: an implementer can code a version from its spec without
   inventing parameters.
7. Internal consistency: CR IDs contiguous; class counts; tallies match
   tables; quotes ≤15 words; no fabricated evidence-row IDs.

## Commands executed

- `git status` / `git log --oneline` / `git rev-parse HEAD` in the worktree →
  HEAD = bea427d, tree clean, branch correct.
- `git fetch origin main`; `git log origin/main..HEAD` → exactly the three
  G011 commits (e07cac5, 3cf5e4e, bea427d); `git diff --name-only
  origin/main...HEAD` → **only** `docs/methodology/contradiction_register.md`
  and the seven files under `docs/methodology/versions/`. No evidence files,
  no G014 files, nothing else. ✓
- Candidate-ID extraction (`grep -oE` over all four
  `contradiction_candidates.md` files) → exactly CC-P1-01..14 (14),
  CC-01..09 (9), C-1..9 (9), CC-P4-01..10 (10) = 42. ✓
- `grep -rhoE "A-G011-[0-9]+"` over the specs → IDs 01..66 all present, no
  gaps; shared IDs (A-G011-08 return type, -19 borrow, -27 P1-import bundle,
  -38 overlap_mode) verified semantically consistent across files. ✓
- Quote-length scan (`grep -oE '"[^"]+"' | awk` word count) over the register
  and all seven specs → zero quotes over 15 words. ✓
- Silent-merge grep (`monoton|linear_fit|OLS|inverse.distance|weighted
  correlation|triangular|neutralization cell`) over nlasr_2012.md,
  nlasr2_2013.md, lasr_2014.md → no later-generation mechanism appears in an
  earlier spec (single benign hit: "Monotone risk-adjusted improvement"
  acceptance pattern in nlasr2_2013 §9, unrelated to P4's monotonic gate). ✓

No test suite applies (documentation-only goal); all claims re-derived from
the evidence files directly, not from the implementer's summaries.

## Tests passed / failed

- Not applicable (no code); the checks above stand in for tests. None failed.

## Code paths inspected

- `docs/methodology/contradiction_register.md` — full read, all 31 entries.
- All seven `docs/methodology/versions/*.md` — full reads.
- Cross-checked against: `docs/evidence/p{1..4}_*/contradiction_candidates.md`,
  `evidence_rows.md`, `formulas.md`, `open_questions.md`, and targeted
  sections of each `extraction.md` (P2 §§22–26 disclosure probes).

## Candidate completeness (criterion 2) — PASS

All 42 candidates appear in the mapping table; independently cross-listed
both directions. Every CR-002..031 traces back to ≥1 candidate; multi-mapped
rows (CC-03→CR-010/011/012/016; shared CRs like CR-005 ← CC-P1-03 + CC-07 +
C-7) are semantically correct merges of the same underlying conflict, not
drops. The CR-P4-a..e aliases in P4's evidence_rows.md map identically to
CC-P4-01/02/03/04/06 as the register note claims. No candidate dropped.

## Register-entry audit (criterion 3) — 13 entries audited, all four
sub-criteria pass on each

REAL: **CR-002** (roster 3 vs 4 — P1-19/20/21, P1-44 searched-absence,
E-P2-19/21, P3-17, E-P4-10 all verified; config `ensemble.components`;
hedge-ablation + must-fail-to-build test), **CR-003** (hedge rules —
E-P2-19/20 IC<7.5%/144m, P3-17 bottom-half/10y, P3-18 3y weekly, E-P4-11
worst-50%/10y aggregate P&L; the INFERRED P3 metric is flagged as such;
per-rule configs + cross-rule sensitivity), **CR-005** (dynamic-IC vs equal —
P1-24/25, E-P2-21/22, P3-19, E-P4-12; F-P2-8 25% algebra verified in P2
formulas), **CR-007** (kernel lineage — P1-12, E-P2-23, P3-11/12, E-P4-17;
the register's extra catch that P3 triangular ≠ P4 inverse-distance
membership is correct per P3 formulas §3.1 vs P4 F5; three golden tests
named and all three worked examples verified present), **CR-008** (argmin-Z
vs argmax weighted corr — P1-14 vs E-P4-18; dual REAL/APPARENT classing is
honest; P1 formulas §7 Z=0.4-vs-0.5 example verified), **CR-013** (costs —
P1-38, E-P2-24/25, P3-28, E-P4-25; P1-39/P3-36 borrow NOT_DISCLOSED
confirmed).

ERRATUM: **CR-019** (7.56 vs 6.54 — CC-P1-11/OQ-P1-08; both numbers retained,
Fig 14 normative), **CR-027** (seasonal footnote off-by-one — C-4 vs P1-20
"trailing 12 years"; `seasonal.lag_years` exists solely for the sensitivity
run — correct §13.3 shape), **CR-029** (target order — special scrutiny:
E-P4-07 and P4 formulas F2 confirm §2.1 says neutralize→scale while Appendix
Step 2 says scale→neutralize; the register does NOT resolve it away — no
canonical reading claimed, default `volscale_first` explicitly ASSUMED with
weak rationale stated, both orders runnable, label-flip A/B + full backtest
both orders + assumptions-register entry required. Exactly what the
assignment demanded), **CR-030** (β<0 — special scrutiny: E-P4-16/F10 confirm
the one-sentence ambiguity; literal `stop_training` default flagged,
`skip_alpha` required alternative, synthetic β<0 test; the note that
iterations 1..i−1 remain usable under stop is a correct reading of the
appendix flow).

APPARENT: **CR-009** (special scrutiny — I checked both formulas files
myself: P1 formulas §4 gives `w←w·exp(−y·h)` + renormalize with real-valued
h and no per-round α (Fig 9 spot value 0.0556·e^−0.49=0.034 verified
present); P4 F11 gives `w←w·e^{−l·φ̂}` + renormalize, explicitly "no learner
weight α_i". These ARE the same primitive; the candidate's suspected
divergence was against textbook discrete AdaBoost, which neither paper uses.
Declining to create a `weight_update` enum is the right call — an enum would
fabricate an undocumented difference; the real difference (forecast shape)
is correctly owned by CR-007), **CR-010** (rounds — P1-17 explicit 30,
P2 §26 NOT_DISCLOSED verified, P3-15 INFERRED, E-P4-20 fn 19; the
repeats-allowed reading of P3's "top 20" exhibits is defensible and is
backed by a distinct-factor-count diagnostic rather than hand-waving),
**CR-011** (ε — P1-13 explicit 1/N, P2 extraction §25 searched-absence
verified verbatim, P3-14 role-only, E-P4-21 NOT_DISCLOSED; P4's zero-bin
default correctly ASSUMED not imported-as-fact).

## Spec provenance audit (criterion 4) — PASS

**nlasr_2012.md — 10 sampled:** train_universe (P1-31 ✓ EXPLICIT),
ε=1/N (P1-13 ✓), n_rounds=30 (P1-17 ✓), selection objective (P1-14 ✓),
weight update (P1-15 ✓), label fractions (P1-04 ✓), rank normalization
(P1-08 ✓), training windows (P1-19/20/21 ✓), US IC weighting (P1-25 ✓),
cost grid (P1-38 ✓) — every sampled EXPLICIT row traces to a real evidence
row classed EXPLICIT saying what the spec says. ASSUMED spot-checks
(technical deviation transform A-G011-04/OQ-P1-07; rank direction
A-G011-05/OQ-P1-02) carry candidates and OQ-backed defaults. Tally 39 =
22E/0I/4INF/13A re-counted by hand: matches.

**nlasr_2020.md — 10 sampled:** universe (E-P4-02 ✓), clocks (E-P4-13 ✓),
fundamental 3m lag (E-P4-04 ✓), vol scaling (E-P4-08 ✓), kernel (E-P4-17 ✓),
selection objective (E-P4-18 ✓), weight update (E-P4-19 ✓), equal-¼ ensemble
(E-P4-12 ✓), costs/borrow (E-P4-25 ✓), t+2 MOC (E-P4-26 ✓). n_rounds
correctly INFERRED not EXPLICIT (E-P4-20); zero-mass rule correctly ASSUMED
against E-P4-21 NOT_DISCLOSED with A-G011-56. All 17 cited OQ-P4 IDs exist.
Tally 38 = 18E/0I/2INF/18A re-counted: matches (the dual-class beta-
residualization row counted under EXPLICIT, transparently).

**IMPORTED_FROM_P1 probes — 5+ sampled in nlasr2_2013.md** (kernel, Q, ε,
selection objective, weight update, n_rounds, feature list): P2 extraction
§22 (quantile count NOT_DISCLOSED, Figure 7 UNREADABLE, "For details see
Wang, et al. [2012]"), §23 (objective NOT_DISCLOSED), §24 (update formula
NOT_DISCLOSED), §25 (no smoothing parameter anywhere, searched locations
listed), §26 (rounds NOT_DISCLOSED) — every import is genuinely absent from
P2's evidence. **5 sampled in lasr_2014.md** (ε P3-14 value-undisclosed ✓;
weight update P3-16 "formula must come from P1" ✓; n_rounds P3-15
undisclosed ✓; preprocessing pipeline ✓; component z-scoring P3-35 "no
re-scaling disclosed" ✓). No import is presented as stated by the importing
paper. Tallies: nlasr2_2013 33 = 13/7/5/8 ✓; lasr_2014 29 = 19/6/0/4 ✓;
lasr_hc 8 = 4/0/1/3 ✓; lasr_hf 13 = 9/0/1/3 ✓; modernized 17 MODERNIZED ✓ —
all re-counted against their own tables.

## No silent merging (criterion 5) — PASS

- nlasr_2012: neutralization "NONE at signal level", hedge component "MUST
  NOT exist" with config rejection, kernel piecewise_constant, no monotonic
  gate, no P2 cells, grep-clean.
- lasr_2014: kernel = piecewise_linear_interp with triangular membership;
  P4's OLS line and inverse-distance membership absent; CR-007's membership
  distinction respected.
- nlasr2_2013: hard-bin kernel imported with IMPORTED_FROM_P1 flags, never
  attributed to P2.
- modernized.md: all 17 deltas labeled M-01..M-17 MODERNIZED with
  rationales; explicitly barred from leaking into historical specs; the one
  place a CR default flips (M-08 skip_alpha) is called out as such.
- The register's closing "version-boundary rule" section correctly encodes
  §13.2 both directions (silence→import-with-flag; later feature→reject,
  not default-off).

## Executability test (criterion 6) — nlasr2_2013 walked as G030 implementer

Every core parameter is either valued or config-defaulted with provenance:
universe (R3000/Fig 54), features (P1 70-list import, A-G011-23), cell-wise
rank/N with stated rank direction, cell definitions (GICS-10 A-G011-24,
size-median A-G011-25, beta A-G011-26, Fig 55 per-region scheme), 30/40/30
within-cell labels, full engine (Q=5, ε=1/N, argmin-Z unsmoothed, w-update,
L=30, init 1/N — all imported with values), 4-sample ensemble incl. hedge
(144m backcast, 7.5% threshold, backcast object A-G011-28, recursion
A-G011-29, pooling A-G011-13), z-score + IC weighting (window A-G011-16),
hedge weight rule (F-P2-8), portfolios, costs, validation windows, numeric
acceptance targets. **No core-math parameter must be invented.** Edge-policy
gaps an implementer would still have to decide (all minor, see
non-blocking #3): hedge-backcast behavior when <144 months of history exist
(regional models near their start dates), the empty-hedge-set corner,
minimum-cell-size handling for the 40-cell US scheme in thin months, and the
z-score universe at scoring time (covered in nlasr_2012 as A-G011-15 but not
restated in the P2 spec's import list).

## Edge cases attempted

- Zero-mass bin / ±∞ score corner: covered (CR-011 test; A-G011-56).
- β<0 mid-loop: covered (CR-030 synthetic test, both modes).
- Exact-center membership s=c_k: covered (A-G011-55).
- P3 tail leakage below/above outer bin centers: covered (`tail_mode`,
  A-G011-31, sensitivity required).
- Thin universes / Q=3 interaction: covered (CR-012 test).
- Insufficient hedge history / empty hedge set: NOT covered (non-blocking #3).
- Seasonal start-up with <2 years history: covered in nlasr_2012
  (A-G011-14); inherited elsewhere.

## Leakage risks checked

- Overlapping 4-week labels sampled weekly (nlasr_2020 §4) — faithful
  `pooled_as_paper` vs `purged` mode both present; training window must end
  4 weeks before calibration; G014 hook named. ✓
- LASR-HC 3-month labels — explicit purge/embargo section; P3-23 data-lag
  guard distinguished from overlap correlation (correct distinction). ✓
- P1/P2 same-close look-ahead — kept as faithful default with the paper's
  own acknowledgment, lag variants configurable (CR-018); matched-target
  retraining requirement carried. ✓
- HF open-price alignment (train/eval price basis) — load-bearing note kept
  (P3-30). ✓

## Quantitative invariants checked

- 42 = 14+9+9+10 candidates, all mapped (re-derived). ✓
- CR IDs contiguous 001..031; 30 new entries; class counts REAL 14 (CR-002..
  008 + 012..018), ERRATUM 9 (019..023, 026, 027, 029, 030), APPARENT 7
  (009..011, 024, 025, 028, 031) — re-counted from headings. ✓
- All seven per-version provenance tallies re-counted against their tables:
  match. ✓
- A-G011-01..66 contiguous, cross-file reuses consistent. ✓
- Quotes ≤15 words everywhere (scripted scan). ✓
- Evidence-row ID probes (well beyond 6): P1-04/08/12/13/14/15/17/19/20/21/
  25/31/33/38/39/41/44, E-P2-19/20/21/22/23/24/29, P3-11/12/13/14/15/16/17/
  18/19/28/35/36, E-P4-02/07/10/11/16/17/18/19/20/21/25/26 — every cited row
  exists and says what the register/specs claim. F-P2-8, P1 formulas §5/§7,
  P3 formulas §3.1/§3.2, P4 formulas F2/F5/F10/F11, P2 extraction §§22–26,
  OQ-P1-01..17 and OQ-P4-01..17 references all resolve. Zero fabricated IDs. ✓
- Register worked-example spot values re-verified against the formulas files:
  P1 Fig 9 h=+0.1607/−0.2016 and 0.0556·e^−0.49=0.034; P3 45th-percentile
  0.25/0.75; P4 ψ=[0.75,0.25,0,0,0] and 10⁻⁴·e^{+0.35}. ✓

## Blocking findings

None.

## Non-blocking recommendations

1. **Stale inherited-tally note in lasr_hc_2014.md §3:** the parenthetical
   "(plus inherited lasr_2014 provenance: 17 EXPLICIT · 6 IMPORTED · 3
   INFERRED · 6 ASSUMED)" no longer matches lasr_2014.md's corrected tally
   (19/6/0/4; bea427d fixed the base file but not this echo). The delta
   table itself is correct. One-line fix.
2. **Base training-window evolution lacks a dedicated register home:**
   CC-P4-09 maps to CR-003, which covers only the hedge rule; the base
   component windows also changed across generations (P1/P2/P3 12m / 12y-
   seasonal / 1m vs P4 5y / 1y / 10y-seasonal). The version specs encode
   this correctly per version (no merging occurred), but §13.3's
   "sample-window differences" item would be more completely served by a
   sentence in CR-002 or CR-003 recording the base-window change with its
   citations (P1-19/20/21, E-P2-18, P3-17/18, E-P4-10).
3. **nlasr2_2013 edge-policy gaps** (from the executability walk): add
   defaults + A-G011 candidates for (a) hedge backcast when history <144
   months, (b) empty hedge set, (c) minimum cell size / cell collapse for
   the 40-cell scheme, (d) restate the scoring-universe z-score convention
   (A-G011-15) in the P2 spec's import list. None require new evidence —
   pure assumption-register work.
4. **Sensitivity coverage of non-CR ASSUMED rows:** CR-linked assumptions
   all carry tests; a few standalone ASSUMED rows (e.g. rank direction
   A-G011-05, GICS vintage A-G011-51) have a config but no named test. When
   the A-G011 candidates are filed into assumptions_register.md, attach a
   sensitivity note per entry.
5. **Pre-existing evidence-file typo (not in this diff):** P2 extraction §26
   cross-references "contradiction_candidates CC-06" where the boosting-
   parameter candidate is CC-03. Owned by G008's files; flag for a cleanup
   pass.

## Evidence for the verdict

The two hardest judgment calls both survive independent re-derivation from
primary evidence: CR-009's APPARENT classification is exactly right (both
papers print the same real-AdaBoost update; the candidate's foil was a
textbook algorithm neither paper uses), and CR-029/CR-030 resolve internal
P4 contradictions the §13.3 way — flagged ASSUMED defaults, both readings
runnable, tests specified — rather than by silent canonization. All 42
candidates are accounted for, every sampled citation is real and accurate,
imports are honest about their provenance, the seven specs are mutually
non-contaminating, and the nlasr2_2013 spec is codeable end-to-end without
inventing core parameters. The five findings above are documentation polish,
not correctness defects.
