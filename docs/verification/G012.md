# Verification report — G012 Workbook schema extraction + data dictionary

* **Goal ID:** G012
* **GitHub issue:** #12
* **Branch:** `agent/data-researcher/G012-workbook-schema`
* **Commit reviewed:** `5de02d9aba39a8b971d7580ee04ede3d4e7d92e8`
* **Pull request:** #45
* **Verdict:** **FAIL** at `5de02d9` (1 blocking finding) — **superseded by Round 2: PASS** at `585aeb5` (see "Round 2 re-verification" below)

## Acceptance criteria reviewed

1. goals.md G012 detail block: every sheet and column inventoried (incl. all
   54 Trading Multiples columns); no fabricated fields; PIT caveats explicit.
2. `skills/excel-schema-mapping/SKILL.md` exit criteria and quantitative
   invariants: field counts in dictionary == counts observed in sheets (state
   both); no mapping without a workbook citation; verifier re-runs the
   inventory script and diffs counts.

## Commands executed

All from fresh context, before reading any implementer explanation beyond the
deliverables themselves:

* `git log/status/branch` in `.worktrees/G012-data-researcher` — confirmed
  HEAD `5de02d9`, branch `agent/data-researcher/G012-workbook-schema`,
  worktree clean. Diff `6ebe98f..5de02d9` touches only the six files under
  `docs/data/` (2,836 insertions); no workbook binaries, no bulk data dumps
  committed (structure + small example values only).
* Independent recount script (verifier-authored, openpyxl 3.1.5,
  `data_only=True` and `data_only=False` passes) against
  `inputs/data_templates/AlphaSense Financial Data Available Metrics with
  Consensus_v3.xlsx` (SHA-256 `9bf1cdeb...ccf744` — matches documented) and
  `inputs/data_templates/ASQ_Comprehensive_Financial_Data_NVDA_v3.xlsx`
  (SHA-256 `40973092...f83b22` — matches documented).
* Full-table re-verification of `data_dictionary.md`: parsed all dictionary
  table rows and machine-checked every one against the workbooks — Section 1
  (513 rows: name, frequency, consensus flag, tab mapping, excel_code),
  Section 2 (16 rows: name, code, category vs Available Consensus), Section 4
  (258 rows vs W1 col G), Section 5 (35 rows vs W1 col I).
* Direct probes of W2 `Data`, `Trading Multiples`, `Front Page`, `Ratios`
  (PIT evidence, gap dates, duplicate pair identity, error cell).
* Case-insensitive substring sweep of every cell in both workbooks for
  gap-list terms (ISIN, CUSIP, SEDOL, FIGI, split, ex-date, bid, ask, spread,
  short interest, borrow, delist, index, publication, knowledge, VWAP,
  total return).

## Tests passed

n/a (research goal — no code/test suite in scope). The skill-mandated
verifier re-run of the inventory is recorded below.

## Tests failed

n/a.

## Code paths inspected

Deliverables inspected in full: `docs/data/workbook_schema/w1_metrics_catalog.md`,
`docs/data/workbook_schema/w2_nvda_template.md`, `docs/data/data_dictionary.md`,
`docs/data/pit_assessment.md`, `docs/data/gap_list.md`, `docs/data/evidence_rows.md`.

## Independent recount — claimed vs verifier

| Item | Claimed | Verifier recount | Match |
|---|---|---|---|
| W1 Financial Metrics metric rows (col A, r2-514) | 513 | 513 | YES |
| W1 distinct metric names | 508 | 508 | YES |
| W1 duplicated names | 5 (listed) | 5, identical list | YES |
| W1 frequency distribution | Q=398, D/M=60, N/A=30, M=15, W=6, LTM=4 | Q=398, D/M=60, N/A=30, M=15, W=6, LTM=4 (sum 513) | YES |
| W1 col B `Yes` rows | 164 | 164 | YES |
| W1 Available Consensus metric rows | 176 (13 categories) | 176 (13 categories, counts match) | YES |
| Consensus cross-match | 164 = 160 exact + 4 duplicate rows; 16 AC names unmatched | 160 exact-matched AC names; sum of col-A multiplicities 164; extra Yes from duplicates 4; 16 unmatched — identical name list | YES |
| W1 col G M&A fields | 258 (rows 2-259) | 258 | YES |
| W1 col I Funding fields | 35 (rows 2-36) | 35 | YES |
| W2 FS coded rows (r8-337) | 306 (+14 headers +10 blank = 330) | 306 coded, 14 header-only, 10 blank (330 total) | YES |
| W2 Ratios metric rows | 118 = 104 coded + 14 uncoded | 104 coded; 22 label-only rows of which 8 are section headers → 14 uncoded metrics | YES |
| W2 Ratios duplicated codes | 4 (FIXED_ASSET_TURNOVER, TOTAL_ASSET_TURNOVER, TOT_ASSET_TO_EQUITY, P_TO_FFO) | same 4, rows (37,72),(38,73),(58,65),(120,137) | YES |
| W2 Trading Multiples columns | 54 = 2 empty (A,B) + 26 (Date,Value) pairs | 54 columns A..BB; A,B empty; 26 code-bearing pairs C..BB | YES |
| TM pairs empty for NVDA | 6 (P_TO_SALES_ADJ, EV_TO_SALES_ADJ, P_TO_BV, P_TO_TBV, P_TO_AFFO, PEG) | same 6 | YES |
| TM duplicate pair | P_TO_FCF (AI,AJ)=(AW,AX) cell-identical | confirmed cell-identical over all rows | YES |
| **TM distinct codes** | **24** | **25** (26 code cells − 1 duplicated `P_TO_FCF`) | **NO** |
| TM obs counts / window | 375 full series, 254 unadjusted LTM; 2025-01-01..2026-06-21, data to 2026-06-18 (r384) | 375 (CLOSE/MCAP/EV/`_ADJ`), 254 (unadjusted LTM); window and last row confirmed | YES |
| Dictionary total | 823 = 513+16+1+258+35 | arithmetic and each addend independently confirmed | YES |
| W2 FS non-null per period | 92,102,103,102,102,100,63,63 | identical | YES |
| W2 Ratios non-null per period | 21×6, 15, 15 | identical | YES |
| Front Page coded fields | 34, of which 24 empty | 34 coded rows (7-44), 24 with empty D | YES |

## Edge cases attempted

* Blank/duplicate handling: recount treats blanks and whitespace explicitly;
  duplicate names counted by multiplicity — reconciliation 160+4=164 holds
  exactly.
* Ratios label-only rows: distinguished section headers (8, all-caps) from
  uncoded metric rows (14) independently; matches the documented split.
* TM date columns are strings, not Excel dates — verified gap computation on
  parsed ISO strings.
* Duplicate-pair identity checked cell-by-cell over the full row range
  (10..1678), not just headers.

## Leakage risks checked (PIT evidence spot-check)

* **`Data!N2:O3`** opened directly: `Version Type` / `Latest restatement` →
  `latest_filing` is the ONLY version option in the sheet (full Data-sheet
  dump inspected: only currency list, period-type list, and this pair).
  The pit_assessment.md headline claim is supported.
* **No publication/knowledge timestamps anywhere in W2**: swept every string
  cell in all 5 sheets for publi|report date|filing|knowledge|as-of|
  timestamp|vintage|restat|version — only hits are the `Data!N2:O3` version
  cells and a false positive (`Cash Conversion Cycle`). Confirmed: the only
  date semantics are fiscal `FINANCIAL_PERIOD_END_DATE` rows and TM trading
  dates.
* **TM window and holes**: window controls C4/C5 = 2025-01-01/2026-06-21;
  unadjusted LTM multiples (PE, EV_TO_EBITDA probed) have exactly two gaps:
  2025-07-30 → 2025-10-26 (88 days) and 2026-01-30 → 2026-04-26 (86 days);
  `_ADJ` variants and CLOSE/MCAP/EV are gap-free. Matches pit_assessment.md
  and E-G012-10 precisely.
* pit_assessment.md makes no unsupported PIT claim: every ESTABLISHED item
  was re-verified against a cell; NOT_ESTABLISHED defaults are consistent
  with A-001.

## Quantitative invariants checked

* Dictionary counts == sheet counts (skill invariant): holds for Sections
  1-5 and the 823 total; VIOLATED for the TM distinct-code figure (24 stated
  vs 25 observed) — see Blocking findings.
* No mapping without a workbook citation: full Section-1 machine check — all
  513 rows cite tab+row; tab/row mappings re-derived from W1's own
  `MATCH` formulas (offsets +7/+7/+6) and matched for 510/513 rows; the 3
  deviations map to cell-identical duplicate Ratios rows (non-blocking, below).
* No fabricated fields: every dictionary name/code/category re-found in the
  workbooks (Sections 1, 2, 4, 5 checked row-by-row; zero fabrications);
  W2-coverage claim verified — exactly one W2 coded label
  (`SHARES_OUTSTANDING`, FP r20) lacks a case-insensitive W1 match, and it is
  exactly the single Section-3 entry.
* Known-invalid code: `Front Page!D37` contains the literal cached string
  `VL40: Unknown metric "RATING_NUM_RECOMMENDATIONS"` — confirmed verbatim.
* P/AFFO conflict: confirmed — W2 `Ratios!B120` = `P_TO_FFO` with label
  `P/AFFO` (and `Ratios!B137` = `P_TO_FFO` labeled `P/FFO`), while W1
  Available Consensus maps `P/AFFO` → `P_TO_AFFO` and the TM `AZ` column uses
  `P_TO_AFFO` (empty for NVDA). The conflict is real and documented; its AC
  row citation is off by one (non-blocking, below).
* Gap-list absences independently confirmed (both workbooks, every cell):
  ISIN/CUSIP/SEDOL/FIGI — absent; split — absent; dividend ex-date — absent;
  bid/ask/spread — absent; short interest — absent; delisting — absent;
  publication/knowledge timestamps — absent; index membership — absent (the
  only "index" hits are W1 `Total Return Index`/`Stock Price Return Index`,
  which are return-index fields, and the only "borrow" hits are the
  `Security Borrowed` balance-sheet line FS r218/W1 A209 — neither
  contradicts the corresponding gap-list rows).

## Blocking findings

1. **Wrong count: Trading Multiples distinct excel_codes stated as 24; the
   workbook has 25.** 26 code cells in `Trading Multiples!` row 8 minus the
   one duplicated `P_TO_FCF` = 25 distinct codes (the stated parenthetical
   "26 pairs - 1 duplicated ... = 24" is arithmetically wrong on its face).
   Stated in two places:
   * `docs/data/workbook_schema/w2_nvda_template.md` line 615 ("Distinct
     codes: 24 ..."); the same sentence's claim "all 24 also exist on the
     Ratios tab / W1 list" is also inaccurate as written — `CLOSE`, `PEG`,
     `P_TO_AFFO` do not appear as codes on the Ratios tab (`PEG` is a
     label-only Ratios row; Ratios pairs the `P/AFFO` label with `P_TO_FFO`).
   * `docs/data/data_dictionary.md` line 886 ("Trading Multiples 26 pairs
     (24 distinct codes)").
   This violates the skill's quantitative invariant (dictionary counts ==
   sheet counts) and would mis-size any downstream adapter field list built
   from the summary. **Remediation:** change 24 → 25 in both files and
   correct the "all ... exist on the Ratios tab / W1 list" sentence to state
   the actual overlap (22 of 25 as Ratios codes; `CLOSE` on Front Page/TM,
   `PEG` label-only on Ratios, `P_TO_AFFO` only in W1 AC + TM header).

## Non-blocking recommendations

1. **Off-by-one AC citation for `P_TO_AFFO`:** `P/AFFO` → `P_TO_AFFO` is
   `Available Consensus` row **167**, not 166 (row 166 is `P/FFO` →
   `P_TO_FFO`). Occurs in `docs/data/data_dictionary.md` line 535 footnote
   and `docs/data/evidence_rows.md` E-G012-05.
2. **Duplicate-row mapping inconsistency (3 rows):** for W1 rows 372, 387,
   388 the dictionary cites the second occurrence of duplicated Ratios rows
   (RA r65, r72, r73) while W1's provider crosswalk — and
   `w1_metrics_catalog.md`, correctly — resolves MATCH to the first
   occurrence (RA r58, r37, r38). The rows are cell-identical (same code,
   label, 0/8 fill), so nothing substantive is wrong, but the two
   deliverables should cite the same row (prefer the crosswalk-resolved
   first occurrence, with a note that an identical duplicate exists).

## Evidence for the verdict

* Verifier recount output (openpyxl 3.1.5, system python3) reproduced every
  claimed count except TM distinct codes: script reported
  `TM: #codes row8: 26 distinct: 25`, duplicates `['P_TO_FCF']`.
* Full-table machine verification of `data_dictionary.md`: 513 + 16 + 258 +
  35 rows checked, findings limited to the two items above (the 10 `(dup)`
  suffix flags are documented convention, not mismatches).
* `Data!N2:O3` dump, FP D37 literal error string, TM gap dates
  (2025-07-30→2025-10-26, 2026-01-30→2026-04-26), and the duplicate-pair
  cell-identity all confirmed by direct probes recorded above.
* Workbook SHA-256 hashes match those documented in both catalogs.

The deliverables are exhaustive, honestly classified, and PIT-disciplined;
the single blocking item is a two-file summary-count correction. Return to
implementer for the fix; re-verification after remediation should be quick
(re-run the recount, confirm lines changed).

Verified by: independent verifier (fresh context), 2026-07-19/20.

## Round 2 re-verification

* **Goal ID:** G012
* **GitHub issue:** #12
* **Branch:** `agent/data-researcher/G012-workbook-schema`
* **Commit reviewed:** `585aeb5` (remediation of round-1 findings; round-1 FAIL was at `5de02d9`)
* **Pull request:** #45
* **Verdict:** **PASS**

### Scope

Narrow re-verification after remediation commit `585aeb5`. Round 1 already
machine-verified the full dictionary at `5de02d9`; this round checks (a) the
remediation diff is exactly what round 1 prescribed and nothing else moved,
(b) every new/changed statement matches a fresh independent recount, (c) no
stale references to the superseded figures remain.

### Commands executed

* `git log/status/branch` in the worktree — HEAD `585aeb5` on
  `agent/data-researcher/G012-workbook-schema`, worktree clean.
* `git diff --stat 604feed..585aeb5` and `git diff --stat 5de02d9..585aeb5
  -- docs/data` — identical: only `docs/data/data_dictionary.md` (+7/−5),
  `docs/data/evidence_rows.md` (+1/−1),
  `docs/data/workbook_schema/w2_nvda_template.md` (+1/−1); 9 insertions,
  7 deletions total. `git diff 5de02d9..604feed` adds only this verification
  report. Everything outside these hunks is byte-identical to the
  round-1-verified content (git content addressing is authoritative).
* Workbook SHA-256 re-hashed: W2 `40973092...f83b22`, W1 `9bf1cdeb...ccf744`
  — unchanged since round 1.
* Fresh verifier-authored recount script (openpyxl, `data_only=True`)
  against both workbooks — results below.
* Stale-reference sweep: `grep -rn -E "24 distinct|all 24|r166|RA r65|RA
  r72|RA r73"` and TM-context "24" patterns over all of `docs/data/`.

### Independent recount — remediated claims vs verifier

| Item | Remediated claim | Verifier recount | Match |
|---|---|---|---|
| TM row-8 code cells | 26 code-bearing pairs | 26 | YES |
| TM distinct codes | 25 (− 1 duplicated `P_TO_FCF`) | 25; duplicates = `['P_TO_FCF']` only | YES |
| TM∩Ratios code overlap | 22 of 25 | 22 of 25; exceptions exactly `CLOSE`, `PEG`, `P_TO_AFFO` | YES |
| `CLOSE` exception detail | coded only on Front Page r17 + TM | `Front Page!B17` = `CLOSE` (label `Share Price`); not a Ratios code | YES |
| `PEG` exception detail | label-only Ratios r134; W1 FM r414 / AC r168 | `Ratios!C134` = `PEG`, `B134` empty; `PEG` not a Ratios code; `Financial Metrics!A414` = `PEG`; AC r168 = `PEG`→`PEG` | YES |
| `P_TO_AFFO` exception detail | only W1 AC r167 + TM header; Ratios pairs `P/AFFO` with `P_TO_FFO` (r120) | AC r167 = `P/AFFO`→`P_TO_AFFO`; `P_TO_AFFO` in TM row 8, not in Ratios codes; `Ratios!B120` = `P_TO_FFO` / `C120` = `P/AFFO` (r137 = `P_TO_FFO` / `P/FFO`) | YES |
| AC citation fix (dict footnote + E-G012-05) | `P/AFFO`→`P_TO_AFFO` at AC row 167 | row 167 confirmed (row 166 is `P/FFO`→`P_TO_FFO`) | YES |
| Dictionary summary line | TM 26 pairs (25 distinct codes; `P_TO_FCF` queried twice) | 26 cells, 25 distinct, dup `P_TO_FCF` | YES |
| Duplicate-row remap: W1 r372 → RA r58 | `TOT_ASSET_TO_EQUITY` | `Ratios!B58` = `TOT_ASSET_TO_EQUITY` (dup at r65) | YES |
| Duplicate-row remap: W1 r387 → RA r37 | `FIXED_ASSET_TURNOVER` | `Ratios!B37` = `FIXED_ASSET_TURNOVER` (dup at r72) | YES |
| Duplicate-row remap: W1 r388 → RA r38 | `TOTAL_ASSET_TURNOVER` | `Ratios!B38` = `TOTAL_ASSET_TURNOVER` (dup at r73) | YES |
| New duplicate-row footnote | pairs (37,72),(38,73),(58,65) verbatim-identical, 0/8 fill | each pair cell-identical across the full row on cached values; 0/8 data cells filled | YES |

### Stale-reference sweep

No remaining occurrence in `docs/data/` of "24 distinct", "all 24", an
`AC r166` consensus citation for `P/AFFO`, or `RA r65`/`RA r72`/`RA r73`
mappings. The only grep hits are false positives: `FS r166` (Financial
Statements row citation, data_dictionary.md L169) and `RA r124`
(substring "24", L422) — both verified correct in round 1.

### Round-1 findings disposition

1. **Blocking — TM distinct codes 24 vs 25:** fixed in both files
   (`w2_nvda_template.md` L615, `data_dictionary.md` L888); the corrected
   "22 of 25 on Ratios" overlap sentence with its three named exceptions is
   exactly what the workbook shows.
2. **Non-blocking — AC r166→r167 for `P_TO_AFFO`:** fixed in both
   occurrences (`data_dictionary.md` footnote, `evidence_rows.md`
   E-G012-05).
3. **Non-blocking — duplicate-row mapping preference:** fixed; rows 372,
   387, 388 now cite the first occurrences (RA r58/r37/r38), consistent
   with W1's MATCH crosswalk and `w1_metrics_catalog.md`, with an accurate
   explanatory footnote added.

### Evidence for the verdict

Recount script output: `TM row8 code cells: 26 distinct: 25 dups:
['P_TO_FCF']`; `TM distinct in Ratios codes: 22 of 25; exceptions:
['CLOSE', 'PEG', 'P_TO_AFFO']`; AC r166/167/168 = `P/FFO`/`P/AFFO`/`PEG` →
`P_TO_FFO`/`P_TO_AFFO`/`PEG`; all three duplicate Ratios pairs
`identical=True`, `0/8` fill. No deviations. All acceptance criteria and
skill invariants now hold; no new errors introduced.

Verified by: independent verifier (fresh context, round 2), 2026-07-20.
