# Verification report — G013 Field mapping: provider fields → model requirements

* **Goal ID:** G013
* **GitHub issue:** #13
* **Branch:** `agent/data-researcher/G013-field-mapping`
* **Commit reviewed:** `98b4400ff5ec7545ed1c945e1b59b170261ed006`
* **Pull request:** #52
* **Verdict:** **PASS** (0 blocking findings; 6 non-blocking)

## Acceptance criteria reviewed

1. goals.md table row G013 ("Field mapping: provider fields → model
   requirements"; deps G011, G012) — goals.md carries no detail block for
   G013, so the controlling criteria are MASTER_PROMPT §10.3.
2. MASTER_PROMPT §10.3: map provider fields to model features; classify each
   required input direct / renamed / derivable / ambiguously derivable /
   unavailable / needs additional data; record PIT limitations; never assume
   presence implies point-in-time access.
3. `skills/pr-verification/SKILL.md` procedure; assignment-specific checks:
   demand-side completeness vs the seven version specs, ≥15-row mapping
   spot-check, coverage-arithmetic recount, anti-fabrication probes.

## Commands executed

All from fresh context; deliverables read only after the goal definition,
§10.3, both walked version specs, and the G012 verification inputs.

* `git status` / `git log` / `git rev-parse HEAD` in
  `.worktrees/G013-data-researcher` — HEAD `98b4400`, branch
  `agent/data-researcher/G013-field-mapping`, worktree clean.
* `git diff --stat main...HEAD` and `--name-status` — exactly two added
  files: `docs/data/field_mapping.md` (239 lines),
  `docs/data/feature_coverage.md` (263 lines); nothing else touched.
* Full read of both deliverables; full read of
  `docs/methodology/versions/nlasr_2012.md`, `nlasr_2020.md`,
  `nlasr2_2013.md`, `lasr_2014.md`, `lasr_hc_2014.md`, `lasr_hf_2014.md`,
  `modernized.md`; `docs/data/gap_list.md`, `pit_assessment.md`,
  `data_dictionary.md` (cited rows), `workbook_schema/w2_nvda_template.md`
  (TM pair table), `evidence_rows.md` (cited E-G012 rows); targeted greps of
  `docs/evidence/p1_nlasr_2012/`, `p3_lasr_2014/`, `p4_nlasr_2020/`.
* Verifier-authored openpyxl 3.1.5 probes against
  `inputs/data_templates/ASQ_Comprehensive_Financial_Data_NVDA_v3.xlsx` and
  `AlphaSense Financial Data Available Metrics with Consensus_v3.xlsx`
  (scripts in session scratchpad; results transcribed below).
* Quote-length sweep: no quoted string in either deliverable exceeds 12
  words (awk over all `"..."` spans); the MASTER_PROMPT quote in
  feature_coverage §8 matches the source verbatim.

## Tests passed / failed

n/a — research/documentation goal, no code or test suite in scope. The
adversarial probes below stand in for tests; none failed against the
deliverables' claims.

## Code paths inspected

`docs/data/field_mapping.md` (all 7 sections, all 46 FM rows + §5 family
tables) and `docs/data/feature_coverage.md` (all 9 sections) in full.

## Check 1 — scope

SHA `98b4400` confirmed; worktree clean; diff vs main is exactly the two new
files. PASS.

## Check 2 — demand-side completeness (independent walk of P1 + P4 specs)

I enumerated every model-required input family from `nlasr_2012.md` and
`nlasr_2020.md` myself, then located each in field_mapping.md:

| Demand (spec §) | Found in mapping |
|---|---|
| P1 universe: Russell 3000, S&P 500 score screen, S&P BMI global (§1) | FM-27, FM-28 |
| P1 explicit absence of screens (§1, P1-32) | FM-32 |
| P1 monthly clock; P4 weekly/4-week clocks (§2) | FM-08 |
| P1 execution modes incl. next_open (§2) | FM-11, FM-12 |
| P1 70 factors / six styles incl. Fig 74 technical (§3) | §5.1–5.6 |
| P1 global: country demeaning, USD conversion (§3) | FM-35, FM-24 |
| P1 target: 1M USD total return (§4) | FM-18 |
| P1 optimizer: beta neutrality, risk model (§10) | FM-22*, FM-43 |
| P1 costs grid / no borrow (§10) | FM-42 |
| P1 benchmarks S&P 500 TR; windows to 1988 (§11) | FM-23; global depth caveat |
| P4 universe: MSCI World 80% liquid; liquidity screen (§1) | FM-27, FM-29 |
| P4 114 features, six families (§3) | §5 + coverage §6 dedicated section |
| P4 3-month fundamental lag (§3) | FM-10 |
| P4 GICS L1 × 3 regions, 1996–2018 vintage (§3) | FM-33, FM-36 |
| P4 4w vol-scaled neutralized target; 260w vol (§4) | FM-20, FM-21 |
| P4 beta residualization 3y weekly (§10) | FM-22 |
| P4 costs 5bp / borrow 50bp as config (§10) | FM-40, FM-42 |
| P4 t+2 MOC execution, daily mark-to-market (§10, §2) | FM-11 |
| P4 MSCI World TR reference; data from ~1991 (§11) | FM-23; depth caveat |
| Both: identifiers, delistings, corporate actions, fiscal calendar | FM-01..07, 09, 16, 17 |

Cross-checked the other five specs: P2 cells (sector/country/size/beta →
FM-33/35/37/38), ADV constraint (FM-30), Russell 1000 (FM-23); P3
float-adjusted size split (FM-31), Merton DD risk-free (FM-45), short
interest (FM-44); HF daily OHLCV (FM-12/13/14/19); HC no new inputs
(verified against `lasr_hc_2014.md` — 3M target reuses FM-18, P3-23 lag is a
pipeline rule, $5B reuses ADV); modernized M-05/06/12/13/14 (coverage §7).
**No missing input family found.** Two nits: (*) FM-22's "Needed by" cell
omits P1's optimizer beta-neutrality (P1-36) though coverage §1 includes
beta for nlasr_2012 (NB-4); P4 long-only "dividend taxes" is not listed
(config-like, not provider data — NB-6).

## Check 3 — mapping spot-check (18 rows, all six classes)

Row → citation → verdict (dict = data_dictionary.md Section 1; workbook
probes marked WB):

1. FM-01 direct — `NAME` dict r485 = FP r7; WB: FP r7 = `NAME`/"NVIDIA Corp". OK.
2. FM-05 direct/LISTED_ONLY — dict r511 `IPO Date`, W1 list only; WB: W1 row 511 col B empty, col D `#N/A`. OK.
3. FM-09 direct — dict r503 = FP r15; WB: FP r15 `FINANCIAL_PERIOD_END_DATE` = 2026-04-26. OK.
4. FM-11 direct — dict r427 `CLOSE` FP r17 + TM; WB: TM pair C,D 375 obs 2025-01-02..2026-06-18; FP r17 empty as documented. OK.
5. FM-12 renamed — dict r424 `OPEN` RA r144; WB: RA r144 = OPEN, 6/8 fiscal-period values, no TM pair. OK (caveat accurate).
6. FM-13 renamed — dict r425/r426 RA r145/r146; WB: confirmed, 6/8 each. OK.
7. FM-14 renamed — dict r438 `VOLUME` FP r19; WB: FP r19 present, value empty. OK.
8. FM-15 direct — dict r42 `DPS` FS r77; WB: FS r77 = DPS, 7/8 filled. OK.
9. FM-18 ambiguous — dict r451–r454 TR family; WB: W1 rows 451–454 names only, no codes (col D `#N/A`); derivation (b) uses only CLOSE (r427) + DPS (r42), both exist. OK.
10. FM-25 direct — dict r418 `MCAP` TM E,F; WB: 375 obs. OK.
11. FM-27 unavailable — gap §8: "Nothing index-related in either workbook". Consistent. OK.
12. FM-29 derivable — CLOSE×VOLUME both exist as fields; dict r440–r445 liquidity variants listed-only confirmed. OK.
13. FM-30 ambiguous — dict r462 + r442 both listed-only (WB: codes absent). OK.
14. FM-31 needs-data — "no float/free-float field among the 823"; WB: case-insensitive sweep of every W1 cell for "float": 0 hits. OK.
15. FM-33 direct/SNAPSHOT — dict r486 `SECTOR_GICS` FP r10; WB: FP r10 = "Information Technology"; no history surface (pit §static reference). OK.
16. FM-40 unavailable — gap §7 borrow; dict r209 `SECURITY_BORROWED` = FS r218 balance-sheet row (WB confirmed, 0/8), so the note that it is not stock-borrow data is correct. OK.
17. FM-45 needs-data — WB: sweep for risk-free/treasury: only `TREASURY_STOCK` (balance sheet). OK.
18. §5.1 EBITDA-to-EV direct — dict r404 = RA r125 + TM; WB: TM pair Y,Z (`EV_TO_EBITDA`) 254 obs; P1 p.19 naming confirmed in P1 extraction. OK.

PIT tags on all 18 match pit_assessment.md's family table (NOT_PIT for Q
fundamentals, RETRO_DAILY for TM panel, SNAPSHOT for FP statics/consensus,
LISTED_ONLY for W1-only). All `unavailable` rows cross-reference gap_list
sections that say what is claimed. 18/18 verdicts OK.

## Check 4 — coverage-arithmetic recount

* **nlasr_2012 (16 groups):** my row-by-row recount of coverage §1 by
  leading status: D = {reference block, close/mcap, Value, Quality} = 4;
  V = {Growth, Momentum, Technical, beta/risk-model} = 4; A = {calendar,
  target} = 2; U = {IDs/delistings/report-dates, corp actions, universe,
  benchmark, Sentiment} = 5; N = {global FX/country} = 1. **4/4/2/5/1 over
  16 — matches the scorecard exactly.**
* **nlasr2_2013 (8 new groups):** D=1 (GICS), V=2 (size, beta cells), A=2
  (country, ADV), U=2 (Russell 1000, regional universes), N=1 (FX) —
  **matches 1/2/2/2/1.**
* **lasr_2014 sentiment claim:** P3 extraction "Feature categories": Value
  1–19, Growth 20–26, Mom/Rev 27–32, Sentiment 33–45, Quality 46–63,
  Tech/Exotic 64–70 → 19+7+6+13+18+7 = 70; Sentiment = 13. "≥13/70
  (the sentiment style)" is grounded; Merton DD and short interest/float are
  verbatim Fig 2 names per the extraction, both in Quality → 70−15 = 55
  upper bound consistent with "~50–55".
* **nlasr_2020 named features:** table (a) recount: direct = ROA, ROE, cash
  ratio, asset turnover, EBIT/EV, dividend yield, EBITDA/EV, FCF/EV,
  −Market Cap = 9; derivable = percent accruals, CAPEX/assets, asset
  growth, 1Y EPS growth, momentum, volatility, beta = 7; total 16.
  **"16/16 (9 direct, 7 derivable)" reconciles**, and the 16 names match
  the P4 extraction's Figure 3 / p.3 / p.12 enumeration exactly (including
  the FCV/EV typo flag). Family counts 32+28+21+17+12+4 = 114 ✓.
* **nlasr_2020 group score:** "1 D · 3 V · 0 A · 3 U · 2 N" sums to the 9
  table rows and D/V/U assignments are consistent for 7 rows, but the two
  N counts must fall on the "114 features" row and the "borrow/costs"
  config row, whose own status cells do not say N (see NB-1).
* **field_mapping §7 summary:** class lists recounted — direct 12 rows,
  renamed 3, derivable 8, ambiguous 5, unavailable 11, needs-data 4 = 43,
  plus the three n/a/cross-ref rows (FM-32, FM-39, FM-42) = 46 = FM-01..46.
  Every listed FM number matches that row's own Class cell. Reconciles.

## Check 5 — anti-fabrication probes (refutation attempts)

Probes run directly against the workbooks (openpyxl), attempting to refute
"direct"/"derivable" claims:

1. **Daily OPEN/HIGH/LOW/VOLUME exists somewhere?** Enumerated all 26
   Trading Multiples pair headers (rows 7–8, cols C..BB): only CLOSE, MCAP,
   EV and multiples — no OPEN/HIGH/LOW/VOLUME series anywhere; RA r144–146
   carry only 6/8 fiscal-period values; FP r19 VOLUME is empty. The
   mapping's refusal to claim daily OHLV (FM-12/13/14 LISTED_ONLY, FM-19
   needs-data, HF "synthetic-only" verdict) is CORRECT, not optimistic.
2. **Total-return field usable?** W1 rows 451–454 have names only (no
   excel_code, `#N/A` match col); absent from W2. FM-18's `ambiguously
   derivable` (not direct) classification is correct.
3. **Growth fields really absent?** Case-insensitive sweep of every cell of
   W1 Financial Metrics for "growth": 0 hits. The "zero precomputed
   growth-rate fields" claim stands. Same sweeps: "float" 0 hits, "short
   interest" 0 hits, risk-free/treasury only `TREASURY_STOCK`.
4. **Claimed daily series real?** TM window C4/C5 = 2025-01-01/2026-06-21;
   obs recounted: CLOSE 375, MCAP 375, EV 375, PE 254, EV_TO_EBITDA 254,
   EV_TO_EBIT 254, EV_TO_FCF 254, P_TO_BV 0, P_TO_TBV 0 — all match the
   mapping's citations including the §5.1 zero-obs caveat.
5. **PIT anchor real?** Data!N2:O3 = `Version Type` / `Latest restatement` →
   `latest_filing`; FS D8 REV = 16675 (the pit_assessment FY-5 example);
   selector Data!D2:E5 = FY/FQ/FH. Confirms A-001 usage throughout.
6. **Modernized un-runnable claim:** M-05 (modernized.md) mandates PIT
   membership/GICS/as-reported fundamentals with publication lags;
   pit_assessment verdict establishes no PIT capability and mandates
   `supports_pit=false`. "Definitionally un-runnable on AlphaSense alone"
   follows. Confirmed.

No fabricated availability found; every error found runs in the
conservative direction.

## Edge cases attempted

Single-company nullability generalization (mapping consistently labels NVDA
fill counts as one-example observations); duplicate W1 names (dup-flagged
rows not double-counted in the margin-family characterization — see NB-5);
empty TM pairs (P_TO_BV/P_TO_TBV/PEG correctly excluded from "demonstrated"
claims); the FM-42/FM-40 config-vs-data boundary (borrow 50bp correctly
kept as parameter, not data).

## Leakage risks checked

This is a documentation goal; the leakage-relevant claims were verified:
presence ≠ PIT enforced on every row (no row claims backtest-usable history;
the global depth caveat is stated to apply to all rows); `latest_filing`
restatement risk propagated to every fundamental-derived feature (NOT_PIT);
adjustment-basis risk (FM-17) propagated into momentum/return rows; the
3-month-lag rule correctly separated from unavailable report dates (FM-10).

## Quantitative invariants checked

46 = count of FM rows (verified); §7 class partition covers all 46 with no
overlap (verified); per-version tallies internally consistent (verified,
one labeling imprecision — NB-1); 70 = Σ P3 family counts (verified);
114 = Σ P4 family counts (verified); 16 named = 9+7 (verified);
823 = 513+16+1+258+35 per the dictionary's own reconciliation table
(recomputed from the table; underlying counts double-verified under G012).

## Blocking findings

None.

## Non-blocking recommendations

1. **nlasr_2020 scorecard N-bucket labeling** (feature_coverage §6): the
   "2 N" in "1 D · 3 V · 0 A · 3 U · 2 N" can only be the "114 features"
   row and the "Borrow 50 bp / costs 5 bp" row; the latter's own cell says
   "config parameters, no data needed". Relabel the tally (e.g. 7 scored
   groups + 2 n/a) or mark the two rows' statuses explicitly.
2. **"8 with demonstrated daily TM series" understates** (field_mapping
   §5.1; feature_coverage §6(b)): recount from the TM pair table gives 16
   of the 27 fields r391–r417 with a daily series (12 unadjusted at 254
   obs + 4 `_ADJ` at 375 obs). Conservative direction; no classification
   changes.
3. **"1Y–4Y changes per pull"** (§5.2): FY-5..FY0 = 6 historical periods
   supports up to 5Y changes. Off-by-one, conservative direction.
4. **FM-22 "Needed by" omits nlasr_2012** (optimizer beta-neutrality,
   P1-36); coverage §1 already includes it, so demand coverage is intact —
   add P1 to the FM-22 row for consistency.
5. **"23 Margins + 7 Adjusted"** (§5.5): reproducible only as a
   distinct-name count over dict rows 319–351 (the dictionary's category
   labels give 16 "Margins" + 4 "Adjusted Margins" because several margin
   fields carry category "Fundamentals"). State the counting basis.
6. **P4 long-only "dividend taxes"** (nlasr_2020.md §10) is absent from the
   inventory; recommend one FM row or a note classing it as a config
   parameter (like FM-42), since no provider data is implicated.

## Evidence for the verdict

Diff scope exact (two new files at `98b4400`). Independent demand-side walk
of both assigned specs plus the other five found no missing input family.
18/18 spot-checked rows have accurate citations, existing referenced fields,
derivations over existing fields only, gap_list-consistent unavailability,
and pit_assessment-consistent PIT tags. All refutation probes against the
raw workbooks confirmed the deliverables' claims; the only arithmetic
defects found (NB-1..NB-3, NB-5) are conservative — they understate, never
overstate, availability. The controlling conclusions (nothing PIT, no
universe source, HF blocked on undemonstrated daily OHLV, modernized
contradicts provider semantics) each trace to verified workbook cells or
double-verified G012 deliverables.
