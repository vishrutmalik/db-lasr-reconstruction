# Verification report — G014

- **Goal ID:** G014 — Quantitative correctness & leakage criteria
- **GitHub issue:** #14
- **Branch:** `agent/quant-reviewer/G014-correctness-criteria`
- **Commit reviewed:** `3aa490c7ca3cd8b14c7c3f0113884341a6b7abbb`
- **Pull request:** #49 (metadata not independently confirmable — `gh`
  unavailable in verifier environment; branch and SHA verified locally)
- **Verdict:** **PASS**

## Acceptance criteria reviewed

From issue #14 / goals.md row G014, MASTER_PROMPT §10.4 (quant-reviewer
charter), §30 "Quantitative correctness", §17 (synthetic scenario list),
§23 (timestamps and metrics), and the verifier assignment:

1. Deliverables are exactly the two owned files
   `docs/methodology/correctness_criteria.md` (CI-001..055) and
   `docs/methodology/leakage_tests.md` (LT-001..021); no G011-owned paths
   (`versions/**`, contradiction register) touched.
2. Every MASTER_PROMPT §30 "Quantitative correctness" bullet and every §17
   example scenario maps to at least one CI/LT.
3. Paper-specific criteria cite real evidence rows that actually support the
   stated invariant (spot-check ≥ 12).
4. Invariants are machine-checkable — a deterministic pass/fail test is
   writable from the statement alone.
5. Leakage scenarios each carry construction recipe, leak-free expectation,
   leak symptom, quantified thresholds, and a teeth-check ablation; threshold
   arithmetic internally consistent.
6. Internal consistency: unique contiguous IDs matching claimed counts;
   coverage map references only real goal IDs; CI↔LT cross-references resolve.
7. Copyright discipline: quotes ≤ 15 words; no fabricated evidence-row IDs.

## Commands executed

- `git status` / `git log --oneline` / `git rev-parse HEAD` in the worktree →
  HEAD = `3aa490c`, branch correct, working tree clean.
- `git show --stat c57ddd7` and `git show --stat 3aa490c` → each G014 commit
  touches exactly one deliverable file; `git diff origin/main...HEAD
  --name-only` → only `docs/methodology/correctness_criteria.md` and
  `docs/methodology/leakage_tests.md`. `versions/**` and the contradiction
  register are untouched (they do not appear in the branch diff at all). ✓
- `git merge-base HEAD origin/main` = `8e9d41b` (a merged G011 evidence
  commit) — the branch carries no foreign commits beyond main.
- Python scan (re) extracting every cited ID of the forms `P1-xx`, `P3-xx`,
  `E-P2-xx`, `E-P4-xx`, `OQ-P1-xx`, `OQ-P4-xx`, `CR-P4-[a-e]`, plus
  slash-shorthand expansions (e.g. `E-P2-09/13/15`) and `P2 Qn`/`P3 Qn`, and
  checking each against the merged `docs/evidence/**` corpus → **126 unique
  IDs + 27 slash-expanded IDs, zero missing**; all `P2 Qn`/`P3 Qn` found.
- Python scan of CI/LT headings → uniqueness, contiguity, per-section counts;
  cross-reference resolution both directions; goal-ID existence against
  `goals.md`.
- Regex scan of both deliverables for verbatim quotes > 15 words → none.
- Manual arithmetic re-derivation of the leakage_tests.md threshold header
  (see invariants below).

## Tests passed / failed

n/a — documentation goal; no code or test suite exists yet on this branch.
All verifier probes passed. The catalog itself is the input to future test
suites (G017–G038).

## Coverage against the specification

### MASTER_PROMPT §30 "Quantitative correctness" (12 bullets)

| §30 bullet | Covered by |
|---|---|
| No future data enters features | CI-001, CI-004, CI-005 (all of section A); LT-013, LT-019 |
| Labels align to the intended future horizon | CI-012, CI-013; LT-007 |
| Neutralization occurs at the intended stage | CI-025..CI-030 (section D); LT-003 |
| Training and testing do not overlap improperly | CI-009, CI-015(b); LT-012 |
| Overlapping labels handled deliberately | CI-015(c)(d); LT-012 |
| Portfolio returns reconcile with positions and returns | CI-045; LT-018 |
| Turnover calculated correctly | CI-046; LT-008 |
| Costs and borrow deducted correctly | CI-048 |
| Gross and net exposures reconcile | CI-047 |
| Corporate actions create no false returns | CI-049; LT-018 |
| Delistings handled | CI-003, CI-049; LT-009 |
| Hard-bin vs linearized behave differently in controlled scenarios | CI-038, CI-054; LT-006, LT-008 |

**All 12 bullets covered. No spec item without a criterion.**

### MASTER_PROMPT §17 example scenarios (10 items)

LT-001..LT-010 are a one-to-one, in-order mapping of the ten §17 examples
(regime-dependent value; momentum crisis reversal; sector predictive until
neutralized; deliberately leaked feature; stable monotonic; nonlinear
non-monotonic; horizon-dependent decay; hard-bin vs linearized turnover;
delistings; restatement leakage). **All ten present**, explicitly headed
"Core MASTER_PROMPT §17 scenarios". Additional §17 generator requirements
are also exercised (seasonal effects → LT-015, changing universe membership
→ LT-016, corporate actions → LT-018, deliberate data errors → LT-021).

## Citation spot-checks (claim → evidence row → verdict)

16 checks (assignment minimum 12), all against the merged evidence files in
this worktree, prioritizing the assignment's named items:

| # | Criterion / claim | Evidence cited | Verdict |
|---|---|---|---|
| 1 | CI-025: P2 neutralization = within-cell ranks AND within-cell labels; combined schemes are simultaneous cross-product cells ("40 different categories") | E-P2-09, E-P2-13 | ✓ E-P2-09 (p.15: sector-normalized scores to (0,1] + 30/30 labels within sector); E-P2-13 (p.30: 40 categories, factors and forward returns normalized within each) — both prongs supported |
| 2 | CI-034: P3 triangular masses conserve interior mass; literal tails leak mass; both modes behind config per P3 Q1 | P3-11/12, P3 formulas §3.3, P3 Q1 | ✓ P3-11/12 flag "tail mass < 1 (Q1)"; formulas §3.3 + worked example (ΣW = 0.95 = 1.00 − 0.05 tail leakage) exactly match the invariant's accounting |
| 3 | CI-015(d): P4 within-window training overlap deliberately permitted | OQ-P4-06 | ✓ OQ states the all-52-weekly-rows reading with the Step-4 N-example as support. Note: CI-015 calls it "EXPLICITly implied"; the OQ itself tags it ASSUMED — slight provenance overstatement (non-blocking #2) |
| 4 | CI-008: hedge set from fully realized backcast; P2 trailing 12-year monthly backcast, rank-IC threshold 7.5%; P4 worst 50% of prior 10y weeks | E-P2-19/20/21, P3-17, E-P4-11, P2 Q8/Q9 | ✓ E-P2-19 "144-month backcast each training date"; E-P2-20 threshold 7.5% verbatim; E-P4-11 and P3-17 as stated; Q8/Q9 exist |
| 5 | CI-012: execution-timing families per paper (P1 close / 1-day-lag / open; P4 t+2 market-on-close; HF close-to-close unrealistic) | P1-34, P3-30, E-P4-26 | ✓ all three rows carry exactly these variants incl. P3-30's "Unrealistic" close-to-close and E-P4-26's t+2 |
| 6 | CI-014: training-label timing = evaluation timing; P1 retrained lag variant, P3 HF trained AND evaluated open-to-close | P1-34, P3-30 | ✓ P1-34 inference: "execution-timing enum with matched retraining of targets"; P3-30: "HF trained AND evaluated open-to-close" |
| 7 | CI-032: ε = 1/N in numerator and denominator; N = labeled pooled count (inferred); P3 ε value NOT_DISCLOSED → inherited | P1-13, OQ-P1-15, P3-14 | ✓ P1-13 verbatim + "(verified)"; OQ-P1-15 exactly this reading; P3-14 = EXPLICIT(role)/NOT_DISCLOSED(value) |
| 8 | CI-035: pinned worked-example numbers (+0.1607/−0.2016; 0.0556·exp(−0.49)=0.0340; weights 1/13, 2.5/13; Z 0.4→0.4865) | P1 formulas §5/§7 | ✓ every number reproduced verbatim in formulas.md lines 89–98 and 144–149 |
| 9 | CI-016: 30/40/30 partition, middle absent from training, fractions configurable ("somewhat arbitrary") | P1-04/05, P3-06, E-P2-08, E-P4-09 | ✓ P1-04 middle "disregarded"; P3-06 fn.4 "somewhat arbitrary"; rows exist and say exactly this |
| 10 | CI-046: turnover 2× relation pinned; sanity bands P1 >250%/yr two-way, P4 ≈3.8% daily / 19% weekly | P1-37, P3-27, E-P4-33 | ✓ P3-27 "30% one-way per month (60% two-way turnover)" pins 2×; P1-37 "over 250%" two-way; E-P4-33 "3.8% daily, or 19% weekly" |
| 11 | CI-005: P4 fundamentals lagged 3 months before alpha computation | E-P4-04 | ✓ verbatim "lagged by 3 months" |
| 12 | LT-017: P2 hedge classifier ensemble weight = exactly 25% | E-P2-21 | ✓ "takes the average weight of the other three" ⇒ 25% after normalization, as the row itself derives |
| 13 | CI-055: acceptance targets as bands; dual references for intra-paper discrepancies (7.56% vs 6.54%; 1.64 vs 1.68) | P1-41, OQ-P1-08, E-P4-29, OQ-P4-09, P3-33, E-P2-29 | ✓ OQ-P1-08 documents 7.56 vs 6.54; OQ-P4-09 documents Fig 7 1.64 vs Fig 12 1.68 "keep both"; P3-33 "our data ≠ DB data" |
| 14 | CI-027: P4 target-order contradiction (§2.1 vs Appendix Step 2) → both orders configurable, no hidden default | E-P4-07, CR-P4-e, E-P4-08 | ✓ E-P4-07 flags "§2.1 vs Appendix order conflict"; CR-P4-e row = exactly this contradiction, unresolved, `target_pipeline_order` config; CC-P4-06 detail block concurs |
| 15 | CI-039: P4 β ≥ 0 monotone gate; "exit the algorithm" stop-vs-skip ambiguity as config | E-P4-16, E-P4-17, OQ-P4-03 | ✓ E-P4-16 carries both the constraint ("short a given alpha" barred) and the stop-vs-skip ambiguity |
| 16 | LT-001: adaptation lag is a documented P1 property to reproduce, not patch | P1-45 | ✓ P1-45 lists adaptation lag among known limitations, "must reproduce these behaviors, not patch them" |

**16/16 supported.** Plus the exhaustive automated existence scan: all 126
unique cited IDs (and 27 slash-shorthand expansions) resolve in
`docs/evidence/**` — **zero fabricated evidence-row IDs**.

## Testability audit (10-invariant sample across stages)

| CI | Stage | Machine-checkable? |
|---|---|---|
| CI-001 | A | ✓ deterministic field scan: `knowledge_time <= as_of` on every consumed row |
| CI-004 | A | ✓ metamorphic: truncate-after-`as_of`, assert bit-identical outputs |
| CI-008 | A | ✓ recomputation identity of the hedge set under data truncation (bitwise) |
| CI-013 | B | ✓ calendar-horizon equality per row against the configured grid |
| CI-016 | B | ✓ floor/ceil counts, partition sum, no zero labels in training set |
| CI-023 | C | ✓ artifact hash unchanged across scoring runs |
| CI-031 | E | ✓ `w_i > 0` and `|Σw − 1| < 1e-12` per round |
| CI-034 | E | ✓ exact mass identities with independent tail-leakage recomputation |
| CI-042 | F | ✓ byte-hash equality of all artifacts across seeded reruns |
| CI-045 | G | ✓ reconciliation identity to 1e-10 with per-period residual report |

Also sampled: CI-015 (deterministic purge/embargo rules + family facts —
testable), CI-027 (A/B fixture where orders differ — testable), CI-051
(hand-computed Spearman fixture — testable). One soft spot found: CI-030
uses "≈ 0" / "to tolerance" without a numeric band in its own statement
(non-blocking #1 — the band is quantified in LT-003, which CI-030
cross-references). **No invariant presented as testable is untestable.**

## Scenario audit (6-scenario sample)

All six carry construction recipe, leak-free expectation, leak symptom,
quantified pass/fail thresholds, and a teeth-check ablation:

| LT | Thresholds | Teeth check |
|---|---|---|
| LT-003 | un-neutralized IC > 0.04; neutralized \|IC\| < 0.02; per-sector \|net\| < 2% of gross | dual-config contrast on the SAME dataset |
| LT-009 | ±1 SE of analytic unbiased value; uplift × 0.5 floor; exact −40% realization per delisting | survivorship-biased ablation |
| LT-010 | clean \|IC\| < 0.02 vs ablation IC > 0.07 | latest-vintage flat-table ablation |
| LT-012 | purged \|IC\| < 0.02 vs unpurged IC > 0.05; hard error on unpurged fold spec | unpurged test-harness ablation |
| LT-013 | PIT \|IC\| < 0.02 vs ablation \|IC\| > 0.5; structural lag assertion | observation-date-join ablation |
| LT-016 | PIT \|alpha\| < 1 SE vs ablation > 2 SE; membership-query immutability | current-membership ablation |

**Threshold arithmetic re-derived (the ≥4-sigma claim):** per-period
cross-sectional IC SE ≈ 1/√500 = 0.0447 ✓; mean-IC SE over T ≥ 120 periods
= 0.0447/√120 = 0.0041 ≤ 0.005 ✓; the ±0.03 band = 6σ and the "≈ 0" bound
\|IC\| < 0.02 = 4σ at the stated SE ceiling — the "≥ 4-sigma separations"
claim is internally consistent, and the file correctly requires rescaling
when scenario sizes change and bands derived from sidecar ground truth.

## Edge cases attempted

- Doc-only goal: probed for silent scope creep (foreign files in the branch
  diff; G011-owned paths) — none.
- Probed for dangling cross-references in BOTH directions (CI→LT and LT→CI)
  — all resolve.
- Probed the coverage map for phantom goal IDs — all of G015, G017,
  G019–G035, G037, G038 exist in goals.md.
- Probed for quote-length violations across both files — longest quote is
  10 words.
- Probed slash-shorthand citations (e.g. `E-P2-09/13/15`, `P3-11/12`) by
  expansion — all components exist.

## Leakage risks checked

The catalog itself is the leakage-control artifact. Verified that each §30
leakage-related bullet has an enforcing invariant AND an adversarial
scenario with a leaky ablation proving the detector can fire (LT battery
summary column "Teeth check" — spot-verified for 6 scenarios above).
LT-019 (future-truncation metamorphic probe) correctly subsumes section A
and is gated into CI for G029/G038. LT-020 correctly gates the whole
battery on determinism.

## Quantitative invariants checked

- CI headings: 55, unique, contiguous CI-001..CI-055 ✓ (programmatic).
- Section counts A:11 B:8 C:5 D:6 E:11 F:3 G:6 H:5 — sum 55, matches the
  claimed structure ✓.
- LT headings: 21, unique, contiguous LT-001..LT-021 ✓.
- CI-035 pinned numbers vs P1 formulas.md: exact match ✓.
- CI-046 2×-turnover relation vs P3-27: pinned by the quoted 30%/60% pair ✓.
- LT threshold header arithmetic: consistent (see scenario audit) ✓.
- LT-004 false-positive band: honest ρ ≤ 0.10 ⇒ observed mean per-feature
  IC < 0.15 at the stated SE; flag threshold 0.30 leaves a wide margin ✓.

## Blocking findings

None.

## Non-blocking recommendations

1. **CI-009 lacks a "Tested by:" field** (the only one of the 55; it has
   Scope/Basis/Prevents). The coverage map does assign it (G026, G038), so
   coverage is intact — add the field for format uniformity.
2. **CI-015(d) provenance wording:** "OQ-P4-06 … EXPLICITly implied"
   overstates the source, which tags the all-weekly-rows reading ASSUMED
   (with the Step-4 N-example as supporting evidence). Suggest "ASSUMED,
   supported by the Step-4 N example" to keep provenance discipline exact.
3. **CI-030 (and the `|net| < tolerance` clause of CI-047)** state no
   numeric band inline, deferring to LT-003 / implementer choice. The
   header convention says statistical assertions state their own bands;
   consider inlining the LT-003 numbers (|IC| < 0.02, per-cell |mean| below
   a stated ε) at the CI.

## Evidence for the verdict

- Worktree clean at `3aa490c`; branch diff vs `origin/main` = exactly the
  two owned deliverables (796 + 495 lines added, nothing else).
- 12/12 §30 bullets and 10/10 §17 scenarios mapped (tables above).
- 16/16 citation spot-checks supported; 126/126 cited IDs exist (automated
  exhaustive scan) — no fabricated citations.
- 10/10 sampled invariants machine-checkable; 6/6 sampled scenarios
  complete with quantified thresholds and teeth-check ablations; the
  4-sigma threshold arithmetic re-derives correctly.
- IDs unique/contiguous per claimed counts; all cross-references resolve;
  quotes within the 15-word limit.

Verifier: independent, fresh-context (did not author any G014 content).
