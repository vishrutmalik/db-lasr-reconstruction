# Verification report — G017

- **Goal ID:** G017 — Typed canonical schemas + core time vocabulary + import-rule enforcement
- **GitHub issue:** #17
- **Branch:** `agent/implementer/G017-canonical-schemas`
- **Commit reviewed:** `0daf9c18415bbc2d44d51ed08c18cc2ad0a81d71` (commits `44171e1` + `0daf9c1`)
- **Pull request:** #57 (head `0daf9c1…` confirmed via `gh pr view 57 --json headRefOid`)
- **Verdict:** **PASS**

## Acceptance criteria reviewed

Specs of record: `docs/architecture/canonical_schemas.md`,
`system_design.md` §1 (time vocabulary) / §4 (import rules),
`training_and_artifacts.md` §4.1 (TimingRecord),
`docs/methodology/correctness_criteria.md`; goals.md G017 detail block
(objective, must-resolve N-1/N-2/N-6/N-7, tests, owned paths). Verifier
assignment checks 1–8 all executed.

## Commands executed

All from `.worktrees/G017-implementer` with a fresh verifier environment
`UV_PROJECT_ENVIRONMENT=$HOME/.venvs/lasr-verify-g017` (implementer env
untouched), uv at `~/.local/bin/uv`:

- `git status` → clean; `git rev-parse HEAD` → `0daf9c1…`.
- `git diff --name-status main...HEAD` → 38 files, all inside the owned set:
  6 under `src/lasr/core/`, 15 under `src/lasr/data/schemas/`, 17 test files
  matching `tests/unit/test_schemas*.py` + `tests/unit/test_import_rules.py`.
  Nothing else touched (no pyproject/CI/docs/coordination changes). ✓
- `uv sync --locked` → exit 0, fresh venv. ✓
- `uv run ruff format --check .` → `62 files already formatted`. ✓
- `uv run ruff check .` → `All checks passed!` ✓
- `uv run mypy src/lasr` → `Success: no issues found in 43 source files`
  (strict per pyproject). ✓
- `uv run pytest -q` twice → **374 passed** both runs (4.52s / 1.19s),
  identical results. ✓
- `gh pr checks 57` → lint ✓, typecheck ✓, test 3.11/3.12 ubuntu +
  3.12 macos ✓; determinism/e2e-smoke/leakage-fast skipping (G016
  placeholders, expected). CI green at the reviewed SHA. ✓
- Verifier adversarial probe file (18 tests, written OUTSIDE tests/ in the
  session scratchpad, run via `uv run pytest <abs path>` against the
  installed package) → **18 passed**.
- Greps: `ComponentSpec` in src (docstring mentions only, no symbol);
  `^import|^from pandas` in src (none); `src/lasr/config` contents
  (G016 docstring stub, unchanged by G017).

## Tests passed

- Full suite: 374/374, twice, identical (25 pre-existing G016 tests + 349 new).
- Verifier probes: 18/18 (details under edge cases / invariants below).

## Tests failed

None.

## Code paths inspected

- `src/lasr/core/`: `time_semantics.py` (event/knowledge/vintage/as-of
  vocabulary, `ensure_utc`, `knowable` CI-001/CI-005 predicate,
  `DateInterval` inclusive-endpoint intervals), `timing.py` (frozen
  `TimingRecord` + `ExecutionMode`), `ids.py` (A-ARCH-01 minting,
  `IdScheme`), `enums.py` (PitGrade / RevisionSupport / KnowledgeBasis /
  CR-015 ClassificationScheme), `errors.py`, `__init__.py`.
- `src/lasr/data/schemas/`: `base.py` (SchemaRow `extra=forbid, frozen`;
  `ColumnSpec.derived_from`; `TableSchema` self-checks for PK/sort/U1/U2;
  `validate_rows` batch validator for U1/U2/U4/nullability/PK-uniqueness/
  forbidden columns), all table modules (security_master, market_data,
  fundamentals, estimates, corporate_actions, classifications, universe,
  trading, features, training_examples), `manifest.py` (U5), `ensemble.py`
  (ExpertSpec/SubModelSpec/EnsembleRosterSpec + 4 selector specs),
  `registry.py` (16-table registry), `__init__.py`.
- `tests/unit/test_import_rules.py` (full read — see teeth assessment),
  `test_schemas_base.py`, `test_schemas_registry.py`,
  `test_schemas_ensemble.py`, `test_schemas_roundtrip.py`,
  `test_schemas_property.py` (full reads); test-name-level review of the
  remaining 11 schema test modules.

## Check 3 — schema-vs-architecture completeness: 16/16

Every table family in `canonical_schemas.md` has (a) a pydantic row model,
(b) a `TableSchema` with PK + sort key + per-column nullability, (c) the
knowledge/vintage columns the architecture requires:

| # | Table (arch §) | Row model | PK matches arch/N-6 | knowledge/vintage |
|---|---|---|---|---|
| 1 | securities (§1.1) | SecurityRow | (security_id) | first_knowledge_time (N-5 naming) |
| 2 | identifier_map (§1.2) | IdentifierMapRow | 4-col §1.2 PK | knowledge_time |
| 3 | listing_intervals (§1.3) | ListingIntervalRow | N-6: (security_id, exchange, listing_date) | knowledge_time |
| 4 | prices_daily (§2) | PriceDailyRow | (security_id, event_date) | knowledge_time; FM-17 forbidden cols |
| 5 | adjustment_factors (§2.1) | AdjustmentFactorRow | N-6: (security_id, event_date) | knowledge_time; derived_table |
| 6 | fundamentals (§3) | FundamentalRow | §3 PK incl. vintage_seq | vintaged; knowledge_basis |
| 7 | estimates_consensus (§4) | EstimateConsensusRow | §4 PK incl. vintage_seq | vintaged |
| 8 | corporate_actions (§5) | CorporateActionRow | (action_id) | announcement_time (documented) |
| 9 | classification_intervals (§6.1) | ClassificationIntervalRow | §6.1 PK | knowledge_time |
| 10 | derived_exposures (§6.2) | DerivedExposureRow | N-6: (security_id, measure, event_date) | knowledge_time; derived_table |
| 11 | universe_membership_intervals (§6.3) | UniverseMembershipRow | §6.3 PK | knowledge_time |
| 12 | borrow_daily (§7.1) | BorrowDailyRow | N-6: (security_id, event_date) | knowledge_time |
| 13 | trading_calendars (§7.2) | TradingCalendarRow | N-6: (calendar_id, event_date) | None — documented N-5 exemption |
| 14 | fx_rates (§7.3) | FxRateRow | N-6: (base_ccy, quote_ccy, event_date) | knowledge_time |
| 15 | feature_values (§8) | FeatureValueRow | §8 PK | knowledge_time |
| 16 | training_examples (§10) | TrainingExampleRow | (config_hash, security_id, as_of) = §10 join key | knowledge_cutoff |

Plus the non-table records: `FeatureSpec` (§9, field list verbatim vs the
architecture's literal declaration — checked field-by-field),
`DatasetManifest` (U5: schema_version, provider, pit_grade,
source_snapshot_ids, content_hash), `ExpertSpec` (MP §21 one-to-one),
`TimingRecord` (§4.1 + N-4). `SCHEMAS` registry exposes exactly the 16
names; `test_every_mp14_family_has_a_schema` pins the set; the registry
test also pins row-model ↔ TableSchema column and nullability agreement for
all 16 (no drift possible).

**Invented-field probe (10 fields traced to citations):**
`knowledge_basis` → §3 + A-001/A-002; `membership_basis` → §6.3;
`source_snapshot_id` → §2 raw-layer lineage; `n_contributors` → §4
(FM PRICE_TARGET_CONTRIBUTORS); `successor_security_id` → §5;
`is_primary` → §1.3 FM-07; `market_proxy_id`/`window_spec` → §6.2 FM-22;
`sample_window_tags` → §10 CI-011; `purge_status` incl. overlap_permitted →
§10 CI-015/OQ-P4-06; `derived_from_action_ids` → §2.1. MP §14 field lists
cross-read (MASTER_PROMPT.md lines 1263–1372): no field exists that is not
in the architecture doc or MP §14; the only new structural constructs are
`SubModelSpec`/`EnsembleRosterSpec` (the mandated N-1 mechanism, justified
against P3-03/P1-26 evidence and reducible to the degenerate single-sub-model
case) and `holding_end` (the mandated N-4 resolution).

## Edge cases attempted (verifier's own probes, all passed)

PIT-violating rows **rejected**:
- `PriceDailyRow` with `knowledge_time.date() < event_date` → U3 error.
- `UniverseMembershipRow` with `valid_to < valid_from` → CI-003 error.
- `TrainingExampleRow` missing `knowledge_cutoff` (absent AND explicit
  null) → ValidationError; `max_feature_knowledge_time > knowledge_cutoff`
  → CI-001 error; `knowledge_cutoff != as_of` → error; cutoff > decision →
  CI-012 error; zero-length target window → error; `label=0` → error.
- `FundamentalRow` `report_date < period_end` → U3; `knowledge_time <
  period_end` → LT-021 error.
- `CorporateActionRow` cash_dividend without amount/currency → error;
  `terminal_return` on a split → CI-049 single-home error.
- `ListingIntervalRow` `delisting_return` without `delisting_date` → CI-049
  error.

Valid edge rows **accepted**:
- Open intervals (`valid_to=None`) on universe membership; single-day
  interval; open-interval far-future containment.
- Relisting: two `listing_intervals` rows, same security+exchange,
  different `listing_date` → `validate_rows` passes; identical
  `listing_date` → duplicate-PK rejection.
- Non-UTC tz-aware timestamps normalized to UTC; naive rejected.
- CR-006: `model_fit_time` earlier than `knowledge_cutoff` allowed;
  degenerate same_close TimingRecord (all six stamps equal) allowed.

Batch/order/empty:
- Shuffled `prices_daily` batch → U4/CI-043 rejection; empty batch and
  single-row batch pass; smuggled `adj_close` column → FM-17 rejection;
  vintage pair with non-increasing knowledge_time → U2/CI-002 rejection.

Serialization: my own dict+JSON round-trips on `FundamentalRow`,
`ListingIntervalRow`, `EnsembleRosterSpec` — equality after round-trip; the
suite additionally round-trips one fixture per registry row model with a
completeness guard (`test_every_registry_row_model_covered`).

Determinism: full suite run twice, identical (hypothesis explicitly
derandomized in `test_schemas_property.py`, so this holds outside CI too);
`mint_security_id` deterministic + normalization-invariant, empty inputs
rejected.

## Check 5 — import-rule test teeth: REAL

`tests/unit/test_import_rules.py` implements an AST walk (`ast.Import` +
`ast.ImportFrom` with relative-level resolution and submodule-binding
candidates) over `src/lasr`, with the system_design.md §4 dependency table
as data, longest-prefix rule ownership, unmapped-package fail-loud, and a
challenger-extra (sklearn/xgboost) confinement rule. Negative controls are
genuine mutations: forbidden imports are materialized in `tmp_path` trees
and the scanner must report them (models→providers, models→PIT,
targets→canonical, relative-import evasion, cli import,
providers→canonical, sklearn outside challengers, unmapped package), plus a
positive control of legal edges. Not a no-op: assertions require non-empty
violation lists naming the exact edge.

My own mutations (probe file, all detected): models→providers via
**relative** import (`from ..data.providers import loader`),
models→providers plain deep import, `from lasr.data import canonical`
namespace-hop evasion, and an upward edge schemas→providers. Real tree
scans clean (`scan_violations(SRC_ROOT) == []`), re-run independently.

Known static-analysis boundary (inherent, not a defect): dynamic imports
(`importlib.import_module`) are invisible to an AST walk; the architecture
mandates only an import-statement walk.

## Check 6 — N-resolution audit

- **N-2** (single delisting-return home): `corporate_actions.terminal_return`
  is authoritative (`DELISTING_RETURN_AUTHORITATIVE_HOME` constant with a
  4-point rationale in the module docstring);
  `listing_intervals.delisting_return` carries
  `derived_from="corporate_actions.terminal_return"`.
  `test_ci049_delisting_return_has_exactly_one_home` computes the set of
  non-derived terminal-return columns across ALL schemas and asserts it is
  exactly the one home — structurally future-proof. Rejection semantics:
  terminal_return restricted to delisting/merger rows; delisting_return
  requires a delisting_date; both probed above. Sound.
- **N-6** (7 pinned PKs = the 6 arch-unkeyed tables + training_examples):
  challenged each — `listing_intervals (security_id, exchange,
  listing_date)`: relisting on the same exchange gets a new listing_date;
  simultaneous multi-venue listings differ in exchange; same-day
  delist-and-relist is the only collision and is pathological. Sufficient.
  `fx_rates (base_ccy, quote_ccy, event_date)`: one observation per ordered
  pair per day, not vintaged — right, and the degenerate pair is rejected.
  `derived_exposures (security_id, measure, event_date)`: window is
  embedded in the measure enum (beta_1y_d, vol_260w…), so window_spec need
  not be in the PK. `adjustment_factors`/`borrow_daily
  (security_id, event_date)` and `trading_calendars (calendar_id,
  event_date)`: canonical daily grains. `training_examples` PK = the §10
  feature-join key. All sensible; `test_n6_primary_key_resolutions_pinned`
  freezes them.
- **N-7**: `ExpertSpec` canonical; `ComponentSpec` absent as a symbol from
  src (grep: docstring narrative only); decision + rationale recorded in
  `ensemble.py` docstring; `test_expert_spec_is_the_canonical_name` asserts
  the retirement; MP §21 field list pinned one-to-one.
- **N-1**: `test_schemas_ensemble.py` **constructs** the lasr_hf_2014
  roster (two sub-models over distinct feature lists `p3_fig2_70` /
  `p3_fig160_tech`, each with the P3-18 four pools: TrailingWindow(52),
  seasonal years=12, PreviousPeriod(4), HedgeBackcast bottom_half_model_ic
  lookback 156 weeks) and the nlasr_2012 ultra roster (standard/technical
  sub-models, `p1_fig11_us70`/`p1_fig74_tech`, CR-002 3-expert rosters).
  Structural distinctness vs a flat 8-expert roster is genuinely asserted
  (`blend != flat` AND `model_dump() != model_dump()` with 2 vs 1
  sub-models). Evidence rows opened and matched: P3-18 ("1 yr weekly
  baseline; same-calendar-week seasonal; past-month short-term; 3-yr hedge
  weeks"), P1-26 ("equally-weighted the z-score of the N-LASR and Technical
  N-LASR"), P3-03 (blend), A-G011-46 (equal z-scored blend, ASSUMED) —
  selectors and blend semantics match, with one anchor nuance (finding NB-3).

## Check 7 — TimingRecord audit

All 8 MP §23 concepts present (MASTER_PROMPT §23 list cross-read): feature,
knowledge, model-fit, signal-generation, order-decision, execution
timestamps as fields; holding period as `[execution_time, holding_end]`;
target period as `[target_start, target_end]`. CI-012 ordering enforced in
`__post_init__`: probed that execution before decision fails, execution ≠
target_start fails, signal before model-fit fails, zero/negative holding
fails, naive timestamps fail; CR-006 (fit earlier than cutoff) correctly
allowed. `holding_period` property distinct from `target_horizon` (N-4):
probed with the nlasr_2020 shape (7-day hold vs 28-day target). Frozen
dataclass; frozenness and asdict round-trip covered by the suite.

## Leakage risks checked

- CI-001 substrate: `knowable` boundary (`knowledge_time == as_of` → True,
  +1µs → False); lag only shrinks the knowable set (suite property test +
  my probe); negative lag rejected.
- Training-example audit bound `max_feature_knowledge_time <=
  knowledge_cutoff` enforced per row (probed both directions).
- U3 inverted timestamps rejected on fundamentals (LT-021 seed),
  prices_daily, borrow_daily, fx_rates, derived_exposures (CI-004
  window-end bound), feature_values (CI-005 lag direction).
- U2/CI-002: later vintage with equal-or-earlier knowledge_time rejected
  (restatement/revision leak-substrate).
- CI-003: inverted intervals rejected everywhere (incl. a hypothesis
  property over 200 examples); membership is interval-shaped by
  construction (no snapshot column exists to backfill).
- P1 same_close acknowledged look-ahead expressible via ExecutionMode, not
  via falsified knowledge times (system_design §1 conventions respected).

## Quantitative invariants checked

- U1 (non-null knowledge column) enforced at both TableSchema construction
  and batch validation; exemption set pinned to exactly
  {trading_calendars: None} + documented naming deviations
  (first_knowledge_time, announcement_time, knowledge_cutoff).
- U2 vintaged set = {fundamentals, estimates_consensus}, vintage_seq in PK.
- U4 sort key on all 16; unsorted batch rejected (CI-043 substrate).
- CI-016 label domain: Literal[1,-1] | None; 0 and ±2 rejected.
- CI-018: all required fields non-Optional → missing/null rejected
  (parameterized over every required field in the suite; probed
  independently for knowledge_cutoff).
- CI-049: single home + type-restricted terminal returns; returns < −100%
  rejected (`ge=-1.0`).
- FM-17 guard: 8 adjusted-price column names rejected on prices_daily.
- Bar-consistency: open/close/vwap within [low, high], crossed quotes
  rejected.

## Blocking findings

None.

## Non-blocking recommendations

1. **NB-1 (doc drift, not G017-owned):** `system_design.md` §4 names
   `tests/unit/architecture/test_import_rules.py`; the delivered path is
   `tests/unit/test_import_rules.py`, which matches the goals.md G017 owned
   path. The stale architecture reference (also echoed in G016's
   `test_package_imports.py` docstring) should be reconciled by a
   docs-owning goal.
2. **NB-2:** `canonical_schemas.md` §0 promises "a `validate(frame)`
   function"; delivered as `validate_rows(Sequence[Mapping])` with a clear
   documented rationale (no pandas stubs under the frozen mypy-strict gate;
   frame wrapper explicitly deferred to G020 via `to_dict("records")`).
   Ensure G020 actually lands the frame-level wrapper.
3. **NB-3:** The lasr_hf constructing test uses
   `SeasonalSameMonthSelectorSpec` for the P3-18 "same-calendar-week"
   seasonal pool. This mirrors the architecture (its selector table is
   declared complete with only `SeasonalSameMonth`, anchor deferred to
   config per OQ-P4-14), but the spec type has no anchor field; G025 must
   either generalize the anchor (month|week) or document that the seasonal
   anchor follows the expert's grain — otherwise the HF seasonal pool is
   under-specified at the spec-type level.
4. **NB-4:** The AST import scanner cannot see dynamic imports
   (`importlib.import_module`); acceptable under the architecture's
   "walks import statements" mandate, worth a one-line note when the rule
   is next documented.
5. **NB-5:** U3 daily-bar checks compare `knowledge_time.astimezone(UTC)
   .date()` against a local-exchange `event_date`; for the daily-close
   convention every real exchange lands on the same-or-later UTC date, but
   G020's bar-knowledge convention code should keep this comparison in mind
   for any intraday extension (UTC+13 mornings map to the prior UTC date).

## Limitations honesty check (assignment check 8)

The four stated limitations are accurate: (1) no `lasr.config`
implementation — `src/lasr/config` is the unchanged G016 docstring stub;
config schemas were never in G017's owned paths/acceptance, and the PR
flags the follow-up before G024; (2) no pandas import anywhere in
`src/lasr` (grep confirms; deviation documented in `base.py`, see NB-2);
(3) import-rule test path deviation vs the architecture doc is real and
matches goals.md (NB-1); (4) raw per-family schemas absent — not in G017's
acceptance criteria (provider surface belongs to G018 per
`provider_contract.md`). Nothing else in the goals.md G017 block is
missing: all four must-resolve items resolved, all four named test
categories present, mypy strict clean.

## Evidence for the verdict

Diff scope exact (38 files, owned paths only); PR head = reviewed SHA; all
five gates re-run by the verifier from a fresh locked venv with outputs
matching the PR's claims (374 passed twice, mypy 43 files clean); CI green;
16/16 architecture tables schema'd with pinned PK/sort/nullability and a
drift-proof registry test; 10-field invented-field probe traces every
sampled field to the architecture or MP §14; 18/18 independent adversarial
probes (PIT rejections, valid edges, round-trips, batch semantics,
TimingRecord orderings, scanner mutations) pass against the installed
package; import-rule tests have demonstrated mutation teeth plus my own
four evasion mutations detected; N-1/N-2/N-6/N-7 resolutions verified
against the G015 verification report and opened evidence rows; no blocking
findings.
