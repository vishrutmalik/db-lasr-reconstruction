# Verification report — G019: synthetic data generator + synthetic provider + LT scenario machinery

- **Goal ID:** G019
- **GitHub issue:** [#19](https://github.com/vishrutmalik/db-lasr-reconstruction/issues/19)
- **Branch:** `agent/implementer/G019-synthetic-provider`
- **Commit reviewed:** `036c7423877893dc318139edb1e09c97bebcdf8f` (worktree clean, up to date with origin)
- **Pull request:** [#63](https://github.com/vishrutmalik/db-lasr-reconstruction/pull/63)
- **Verifier:** fresh-context independent verifier (specs read before code: MP §17,
  `docs/methodology/leakage_tests.md` LT-001..021, `docs/architecture/provider_contract.md`
  as amended by D-011/D-012/D-015, D-017 on main, G019 registry entry)
- **Date:** 2026-07-23

## Verdict: **PASS** (0 blocking findings, 6 non-blocking)

---

## Acceptance criteria reviewed

1. MP §17 generator inventory (20-item list) — every item present in code, not just claimed.
2. leakage_tests.md G019 rule: every LT-001..021 scenario is a named generator config;
   every embedded truth machine-readable (sidecar); every teeth-check ablation generated
   alongside the clean dataset.
3. provider_contract.md §4.1/§6: FULL_VINTAGES synthetic provider through the same
   `DataProvider` protocol + CT-01..15 suite; `generate(scenario) -> ScenarioBundle`;
   `scenario_catalog()` covers LT-001..021.
4. NB-1 (G018 verification) resolution: typed refusal for duplicate ProviderIds in BOTH
   adapters (D-017 on main).
5. Determinism: identical config+seed ⇒ byte-identical worlds; label-keyed RNG streams;
   row-shuffle/params-order invariance (LT-020, CI-042/CI-043).
6. Scope: owned paths + two explicit grants + flagged shared-file edits requiring
   per-edit verifier sign-off.

## Commands executed (all re-run by me in a fresh venv, `UV_PROJECT_ENVIRONMENT=$HOME/.venvs/lasr-verify-g019`)

```
uv sync --locked                      -> clean install
uv run ruff format --check .          -> 135 files already formatted
uv run ruff check .                   -> All checks passed!
uv run mypy src/lasr                  -> Success: no issues found in 78 source files
uv run pytest -q          (run 1)     -> 1081 passed, 22 skipped in 58.23s
uv run pytest -q          (run 2)     -> 1081 passed, 22 skipped in 11.25s  (identical skip set)
uv run pytest -m "unit or integration" -q -> 977 passed, 4 skipped, 122 deselected
uv run pytest tests/integration/test_provider_contract.py -q -> 34 passed, 4 skipped
  ... -k local_file                   -> 18 passed, 1 skipped   (identical to G018-verified outcome)
  ... -k synthetic                    -> 16 passed, 3 skipped   (all skip-with-reason)
gh pr checks 63                       -> lint / typecheck / test(3.11, 3.12, macos) all PASS;
                                         e2e-smoke / leakage-fast / determinism SKIPPED (wired
                                         but `if: false` per G016 design — not failures)
```

The 22 skips = 4 CT capability-conditional skips + **18 ACTIVATION placeholders** (counted
via `-rs`: exactly 18 with `ACTIVATION` reasons naming their unblocking goals).

## Tests passed / failed

- Full suite green twice with identical counts; zero failures.
- All six assigned LT modules run verbosely: LT-004 (6 tests), LT-009 (5), LT-010 (5),
  LT-013 (4), LT-016 (4+1 skip), LT-021 (11+1 skip) — **35 passed, 4 ACTIVATION skips**.
- **Un-skip probe:** copied `test_lt021_seeded_errors.py` to scratch with the
  `@activation` decorator neutralized → `test_quality_layer_recall_after_g021_lands`
  **FAILED loudly** (`pytest.fail("activated before G021 landed")`), 1 failed. The
  ACTIVATION mechanism is honest: no fake model assertions hiding behind skips.

## Scope audit (diff vs main, 46 files, +7275/−10)

Owned paths: `src/lasr/data/synthetic/**` (12 new modules), `providers/synthetic_provider.py`,
`tests/unit/test_synthetic*.py` (4), `tests/leakage/**` (23). Grants + flagged shared edits
below. **No out-of-scope files touched.** `pyproject.toml` in the branch is byte-identical
to main 26a3a09 (merge artifact only, verified by diff).

### Flagged shared-file edit sign-offs (per-edit verdicts)

| # | Edit | Verdict | Basis |
|---|------|---------|-------|
| 1a | CT-10: `KNOWLEDGE_COLUMN_BY_TABLE = {"raw_corporate_actions": "announcement_time"}` | **PASS** | Verified against `src/lasr/data/schemas/raw_corporate_actions.py`: the table has NO `knowledge_time` column; its knowledge column is `announcement_time` (canonical `corporate_actions` declares `knowledge_time_column="announcement_time"`). The original literal check was unsatisfiable for ANY PIT-true provider serving the family. The amendment also STRENGTHENS the negative branch: a non-PIT provider is now forbidden from emitting `announcement_time` (previously vacuous). No effect on local_file (family unavailable there). |
| 1b | CT-10 U3: `FORECAST_PERIOD_TABLES = {"raw_estimates"}` exemption | **PASS** | `raw_estimates.period_end` is documented in the G017 schema as "forecast period end" — FY1/FY2 consensus is published BEFORE its period ends, so `knowledge >= period_end` is wrong for estimates by construction, for any PIT-true provider. Non-null knowledge is still enforced. local_file estimates are non-PIT (positive branch never ran); no weakening. |
| 1c | CT-07: structural columns (`knowledge_time`, `announcement_time`, `valid_from`, `valid_to`) exempt from field-coverage comparison | **PASS** | Read the full test: the exemption is scoped ONLY to the "columns outside declared coverage" set-comparison. CT-10 mandates the stamp's presence on PIT-true frames, so it cannot simultaneously be a CT-07 coverage violation; interval bounds are structure like the pre-existing `period_end` exemption. local_file frames never carry these columns (CT-10 forbids), so its behavior is unchanged — confirmed empirically: local_file CT run is 18 passed / 1 skipped with the same single CT-11 skip reason G018's verification recorded. |
| 2 | `tests/unit/test_import_rules.py` + `test_package_imports.py`: register `lasr.data.synthetic` as Level-3 sibling; providers gain the one-way synthetic edge | **PASS** | Matches the shipped layering exactly: grep of `src/lasr/data/synthetic/` shows zero imports from providers/canonical/point_in_time/ingestion; the provider adapter imports synthetic (one direction). Registration is the minimal true statement of the dependency. system_design.md §3 module-map one-liner remains architect-owned doc drift (non-blocking, flagged by implementer). |
| 3 | `providers/__init__.py` (not literally in owned paths) | **PASS** | Additive exports only (`DuplicateProviderIdError`, `require_unique_ids`, `DatasetRef`, `ScenarioBundle`, `SyntheticProvider`); the natural registration point for an owned provider; no existing export changed. |
| 4 | Grant: PROVIDER_CASES append in `test_provider_contract.py` | **PASS** | Exactly the granted append (baseline scenario, seed 1729, 40×6y) replacing the placeholder comment G018 left. |
| 5 | Grant: NB-1 helper in `providers/base.py` + `local_file.py` | **PASS** | `DuplicateProviderIdError` + `require_unique_ids` added to base; `local_file._resolve` applies it; synthetic `_resolve` applies it. Typed refusal, not silent dedupe (D-017 on main ratifies). Grep confirms no dedupe path on ids remains (the only `dict.fromkeys` is on requested price FIELDS, which is legitimate). |

## Code paths inspected

- `src/lasr/data/synthetic/`: `config.py` (validation, params freeze), `periods.py`
  (deterministic grids), `plan.py` (typed plan, FactorSpec constraints), `scenarios.py`
  (all 22 builders), `_stages.py` (RNG discipline, return-generating process, churn,
  actions, LT-010/012/013/003/004/016/017 constructions), `_emission.py` (vintages,
  revision histories, membership intervals, error seeding, all 7 ablation builders),
  `mechanics.py` (split/dividend/ledger arithmetic), `generator.py` (sidecar assembly,
  analytic V-quintiles, survivorship uplift, oracles), `sidecar.py`, `truncation.py`
  (LT-019 harness with typed refusal on un-governed tables), `world.py` (order-insensitive
  content hashing).
- `src/lasr/data/providers/synthetic_provider.py`: capability record (honest CALENDAR
  `supports_pit=false`, N-5), all 11 fetch methods, vintage semantics
  (`latest`=max-knowledge / `as_reported`=min-knowledge / `all`=append-only), window and
  id guards, `generate()` bundles with SYNTHETIC_TRUTH grading + manifests.
- Shared-file diffs (full `git diff origin/main...HEAD` on all 6 shared/granted files).
- `tests/leakage/lt_battery.py` (bands from sidecar, not constants; panels built from
  emitted TABLES, not generator internals — no truth/test circularity) + all 6 assigned
  scenario modules + LT-020.

## Edge cases attempted (my own probes, beyond the shipped tests)

- `n_securities=3` → `ScenarioConfigError`; empty scenario_id → error; unknown scenario
  `LT-999` → `ScenarioConfigError` (typed, never a silent default world).
- Minimum viable world: `n_securities=4, n_years=1, weekly` generates cleanly.
- Caller mutates the params dict after construction → generator behavior isolated (frozen copy).
- Inverted fetch window → `TimeSemanticsError`; out-of-history window →
  `HistoryUnavailableError`; unknown vintage literal → `CapabilityError`; unknown
  id → `UnknownProviderIdError`.
- **NB-1/D-017:** duplicate ProviderIds raise `DuplicateProviderIdError` in BOTH adapters
  (synthetic `fetch_prices`, local_file `fetch_security_master` — re-proved live, plus the
  shipped unit tests cover four surfaces).
- Row-shuffle of every table → content hash unchanged; seed 1 vs seed 2 → world hash differs.

## Leakage risks checked

- **Layering:** `lasr.data.synthetic` imports nothing from providers/canonical/PIT
  (grep-verified); providers→synthetic edge is one-way and registered in the import-rule test.
- **LT-004:** falsified knowledge stamps are timestamp-indistinguishable from honest bars
  (test asserts it); leaked-feature IC measured at 0.9006 on my own seed; control ablation
  drops FLEAK and shows no false positives.
- **LT-010:** both vintages stored with correct knowledge times (initial ≥ period_end+lag;
  restated ≥ initial+180d — verified strictly increasing on 490 keys of a fresh-seed world);
  vintage-respecting join IC ≈ 0; `latest_vintage` ablation verified to carry the RESTATED
  value under the INITIAL stamp and to show IC > 0.07 (teeth genuinely differ from clean).
- **LT-013:** every FHIND row satisfies `knowledge_time ≥ period_end + 90d` (CI-005);
  PIT join IC ≈ 0; observation-date-join ablation |IC| > 0.5.
- **LT-016:** no membership before inclusion period (PIT intervals); backfilled ablation
  shows phantom alpha > 2 SE while PIT universe mean matches mu_market within 5 SE.
- **LT-009:** −40% realized exactly once on the delisting date, no bars after; biased
  ablation drops dead names entirely; measured uplift equals sidecar to 1e-9 (recomputed
  from emitted bars — a genuine cross-check, not circular).
- **LT-021:** every error class seeded exactly 3×, sidecar-registered exactly once each
  (fresh-seed recount: max duplicate sidecar record = 1); corrupted tables trip
  `validate_rows`/row-models; paired CLEAN ablation passes — the detector can fail.
- **LT-019 harness:** refuses to truncate un-governed tables (KeyError), calendar exempt
  per N-5.
- **CT-10/CT-11 positive branches** execute non-skipped for synthetic (node-level -v proof).

## Quantitative invariants checked

| Invariant | Probe | Result |
|---|---|---|
| Embedded IC ≈ ρ | My own panel/IC measurement (never battery seed): LT-005 ρ=0.10, 400×15y, seeds 31337/424242 | 0.0984 (0.4σ), 0.1049 (1.3σ) ✓ |
| Negative control | LT-004 FNOISE (ρ=0) | −0.0045 over 143 periods ✓ |
| Leak embedding | LT-004 FLEAK corr 0.9 | 0.9006 ✓ |
| Seasonal embedding | LT-015 ρ_Jan=0.15: 5 seeds | mean 0.1498 (single-seed spread 0.124–0.162 = sampling noise, 20 Jan obs) ✓ |
| Determinism | same config+seed twice | world_hash equal ✓; different seed ⇒ different hash ✓ |
| **Pinned world hashes** | `default_config(sid, 20260723)` | baseline `4efcece09ca17403` ✓, LT-005 `c244bb3a430d860f` ✓, LT-018 `b69c96d564062ec8` ✓, LT-021 `93a45e3932f3b2da` ✓ — all four reproduced exactly |
| Row-order invariance | shuffle every table, rehash | identical ✓ |
| Membership intervals | fresh-seed baseline, per (ticker,exchange) | 0 overlaps; churn gaps present (8/61) ✓ |
| No price after delisting | all 15 terminal names | 0 bars after termination; terminal return realized exactly (0 mismatches at 1e-9) ✓ |
| Restatement vintages | 490 restated keys | strictly increasing knowledge times; 0 rows with knowledge < period_end (of 8170) ✓ |
| Ledger identity (CI-045) | recomputed `(mcap_t + div·shares_{t-1})/mcap_{t-1} − 1 − r_t` over 2276 sidecar ledger rows incl. 2 splits | worst residual 3.4e-16 ✓; hand-check: 2:1 split halves close, doubles shares, market cap continuous, return 0 ✓ |
| A-003 labelling | bundle sidecar + manifests | banner present; datasets graded SYNTHETIC_TRUTH (CALENDAR: SNAPSHOT_STAMPED, see NB-5) ✓ |
| Catalog coverage | `scenario_catalog()` | all of LT-001..021 + baseline ✓ |

## MP §17 inventory audit (item-by-item, against code)

| §17 item | Where | Verified how |
|---|---|---|
| Multiple securities | `config.n_securities` (≥4 enforced) | probe |
| Multiple countries/sectors | `_build_identities`, 3 countries/6 sectors default, fictional vocabulary | code + CT frames |
| Changing universe membership | `_membership_rows` churn gaps + inclusions | probe (8 gapped members, 0 overlaps) |
| Listings and delistings | `late_listing_fraction`, hazard delisting, terminal events | probe (no bars outside interval — unit test + my recount) |
| Corporate actions | splits, reverse splits, regular+special cash dividends, symbol change, merger, delisting | LT-018/baseline worlds, action rows typed (CI-049 row-model) |
| Fundamental publication lags | `period_end + 75d` publication stamps | 0/8170 violations |
| Restatements | dual vintages, `restated` version_type, +180d | 490 keys strictly increasing |
| Missing values | `missing_fraction` (real absences, not NaN rows) | unit test + code |
| Analyst-estimate revisions | `_estimate_revision_rows` monthly revision histories, FY1/FY2 | unit tests + CT-02 |
| Cross-sectional factor structure | `FactorSpec` exposures → resid embedding | IC probes above |
| Time-varying factor efficacy | regimes (LT-001), crisis (LT-002), halves (LT-014), adverse (LT-017) | rho-path resolution code + sidecar |
| Seasonal effects | `seasonal_month` (LT-015) | 5-seed probe |
| Regime changes | `_two_state_chain`, spells recorded | sidecar |
| Market and sector components | `mu/sigma_market`, AR(1) sector paths | code |
| Idiosyncratic returns | `eps_shocks`, noise-scale normalization keeping Σρ²<1 (typed refusal above 1) | code |
| Liquidity variation | volume=shares·turnover·lognoise, ADV, SPREADBPS | code + CT frames |
| Borrow costs | `_borrow_rows` size-ranked fees, hard-to-borrow flags | CT frames |
| Transaction costs | SPREADBPS metric (cost raw material) — see NB-2 | code |
| Technical metrics | OHLV bars + VWAP + BETA/PEX/EVX raw material | code |
| Deliberate data errors | 6 error classes, sidecar-registered | LT-021 probes |

All ten §17 named example scenarios map to LT-001..010; controlled scenarios with known
correct behavior are the LT battery itself.

## Blocking findings

**None.**

## Non-blocking recommendations

1. **NB-1 (CI gating):** the leakage battery (~3.5 s) is outside the CI PR-gate marker
   filter and the `leakage-fast` job is `if: false`. Echoing the implementer's A-cand-5:
   flip `leakage-fast` on now that `-m leakage` exists — the suite this job was waiting
   for has landed. (Orchestrator/G016-owner change; not G019's file.)
2. **NB-2 (§17 "transaction costs"):** covered only as raw material (SPREADBPS + volume/ADV).
   Acceptable at this layer (cost MODELS are G027's charter), but the assumption register
   should say so when G027 lands.
3. **NB-3 (LT-011 lag grid):** execution-lag grid is in scenario periods (weeks), not days
   (A-cand-2, documented in the module). Fine for the exercised invariant; the
   day-grid variant should be revisited when the backtester's execution-delay harness
   (CI-014) activates.
4. **NB-4 (registry candidates):** A-cand-1 (multi-vintage rows vs raw PK, vintage
   disambiguation by knowledge_time — binds G020), A-cand-3 (classification history not
   expressible without schema amendment), A-cand-4 (estimate histories via window end)
   should be entered in `assumptions_register.md` by the orchestrator on merge.
5. **NB-5 (CALENDAR grading):** `generate()` grades the CALENDAR dataset SNAPSHOT_STAMPED
   (supports_pit=false path of `grade_dataset`). A derived deterministic grid arguably
   deserves its own grade or the SYNTHETIC_TRUTH mark; current choice is leak-safe and
   harmless, but worth a one-line note in provider_contract.md §4.1 when D-017 is folded in.
6. **NB-6 (doc drift):** system_design.md §3 module map lacks the `lasr.data.synthetic`
   entry (architect-owned; flagged by the implementer in the PR).

## Evidence for the verdict

- Every gate re-run from a fresh locked venv with outputs above; pytest twice with
  identical 1081/22; CI-filter 977/4; PR CI green on all active jobs.
- All four pinned world hashes reproduced exactly from config+seed by me.
- Embedded-IC calibration verified with my OWN panel construction and seeds (0.4σ/1.3σ
  deviations on the positive control; negative control ≈ 0; leak ≈ 0.9; seasonal unbiased
  across 5 seeds).
- §17 structural invariants (membership, delistings, vintages, error registry, ledger)
  re-derived from emitted tables on fresh seeds, not from the implementer's tests.
- Teeth ablations verified to genuinely differ from clean data AND trip the documented
  symptom (LT-009 uplift, LT-010 IC>0.07, LT-013 |IC|>0.5, LT-016 >2 SE, LT-021
  validation trip + clean control passing); ACTIVATION skips fail loudly when un-skipped
  (probed on LT-021 in a scratch copy — production files untouched).
- Flagged shared-file edits each traced to a schema-level necessity and empirically shown
  not to change local_file's contract outcomes (18 passed/1 skipped, same skip reason as
  the G018-verified state).

---

## Round 2 re-verification (post red-team remediation)

- **Goal ID:** G019 — **Issue:** #19 — **PR:** #63
- **Branch:** `agent/implementer/G019-synthetic-provider`
- **Commit reviewed:** `b160f521afac7e298b910c483ad661e5bec421ea` (worktree clean; 8 commits past round-1 report 16d24a6: red-team audit doc a1cc21b + 7 remediation commits)
- **Scope:** narrow re-check of the RT-1..8 remediation per coordinator assignment; round-1 verdict otherwise stands.
- **Date:** 2026-07-24

### Verdict: **PASS** (0 blocking; 1 new non-blocking, NB-7)

### Commands executed (fresh, same venv)

```
uv sync --locked                    -> clean
uv run ruff format --check .        -> 138 files already formatted
uv run ruff check .                 -> All checks passed!
uv run mypy src/lasr                -> Success: no issues found in 78 source files
uv run pytest -q  (run 1)           -> 1122 passed, 22 skipped in 13.26s
uv run pytest -q  (run 2)           -> 1122 passed, 22 skipped in 8.21s  (identical)
uv run pytest -m "unit or integration" -q -> 980 passed, 4 skipped, 160 deselected
uv run pytest -q -rX                -> zero xfails/xpasses reported
gh pr checks 63 (at b160f52)        -> lint/typecheck/test(3.11,3.12,macos) PASS;
                                       e2e-smoke/leakage-fast/determinism `if: false` skips
```

### Diff audit 16d24a6..b160f52 (16 files, +1297/−160) — every hunk mapped

| Hunk group | Maps to | Judgment |
|---|---|---|
| `_emission.py` master/classification/membership emitters: open row carries NO closure; closure = second superseding vintage stamped at closure publication; sort keys gain knowledge_time | RT-1 | Honest fix (FULL_VINTAGES closure vintages), verified by probes below |
| `_emission.py::base()` announcement `max(effective−14d, prior-period publication)`; terminal (delisting/merger) rows stamped at the effective period's own publication | RT-2 | Correct: terminal_return is a realized return, now never pre-disclosed |
| `_emission.py` estimates: absolute fiscal-year labels (`FY2007`), per-year truths/biases, FY1+FY2-horizon series per stamp | RT-3 (generator side) | Series identity now on the raw PK; no cross-year collision |
| `_emission.py::seed_errors` collision-safe `draw()` with `bars_touched`/`funds_touched`, stale anchor re-draw (≥2 real followers), `event_dates` on every registry entry; typed refusal if placement impossible | RT-5 | Both-directions registry/data correspondence, now seed-robust |
| `_emission.py::build_ablations` survivorship ablation adds `raw_corporate_actions` + `raw_classifications` | RT-6 | Teeth integrity restored |
| `_stages.py::_build_action_schedule` alive-at-drawn-period sampling; merger candidates alive at period (with inherent no-other-termination residual, documented); post-merger event truncation | RT-7 | Removes the full-path-survival conditioning |
| `synthetic_provider.py`: estimates dedup key + `period_end` | RT-3 (surface side) | The actual bug fix |
| `synthetic_provider.py`: `_END_OF_DAY` (23:59:59) → `_BAR_CLOSE` (21:00) window-end cutoff for estimates and membership | RT-8 | Post-close data no longer served at "window end" |
| `synthetic_provider.py`: master/classifications serve `latest_vintage_view` (snapshot semantics); membership fetch = knowledge-cutoff THEN latest view (PIT semantics) | RT-1 (surface side) | Closures beyond the window can no longer appear (probed: 0/45) |
| `synthetic_provider.py` + capability notes: vintage='latest' vendor-semantics WARNING | RT-4 | Documentation-level mitigation as recommended; keeper test pins visibility |
| `world.py::latest_vintage_view` + exports; `sidecar.py::SeededErrorTruth.event_dates`; `truncation.py` docstring | RT-1/RT-5 support | Pure additions |
| `eba7dfd` | 3 now-unused `type: ignore` comment removals in `_emission.py` (verified: comments only) | Clean |
| Keeper tests (3 files): `@pytest.mark.xfail(strict=True)` markers REMOVED, docstrings updated, **assertion bodies untouched** (diff-verified) | Ratchet flips | Strengthening: former reproduced defects are now permanent must-pass regressions |
| `test_lt009_delistings.py`: teeth check extended 2→6 tables + explicit no-delisting-rows assertion | RT-6 | Strengthened |
| `test_lt019_future_truncation.py`: new parametrized `test_surviving_rows_reveal_no_post_asof_closures` over all probe scenarios + LT-009 (with a has-closures teeth guard) | RT-1 | LT-019 now covers the field-content leak class it was structurally blind to |
| `test_lt021_seeded_errors.py`: stale-run threshold ≥2→≥3; `event_dates` required; new 26-seed both-directions registry-integrity sweep (includes the six red-team bad seeds) | RT-5 | Strengthened |
| `test_lt016` / `test_synthetic_generator.py`: `latest_vintage_view` at analysis-time full-knowledge points; new interval-vintage validation + closure-stamp tests; `test_fy1_precedes_fy2` → absolute-label test (asserts label==FY{year} and FY1+FY2 horizons per stamp) | RT-1/RT-3 consequences | Semantics-preserving adaptations plus new assertions; PIT-ness assertions (e.g. no membership before inclusion) retained |

**No weakened test found.** All threshold changes move in the strict direction; the two
replaced/adapted tests assert strictly more than their predecessors.

### Re-verification probes (my own code, fresh seeds where applicable)

- **Round-1 §17 spot-probes re-run (touched areas):** fresh-seed baseline (seed 777, 60×8y):
  knowledge-monotonicity per key across ALL multi-vintage tables — fundamentals 488,
  master 16, classifications 32, membership 24, estimates 928 multi-vintage keys,
  **0 non-monotone**; open rows carrying closures: **0**; closure vintages stamped before
  their closure date: **0**; delisting/merger realization unchanged — 15 terminal events,
  0 bars after, 0 realization mismatches, 0 early-announced terminals.
- **Field-level truncation honesty (RT-1):** my own `truncate_tables` run at an
  independent as_of (2009-06-30): **0 post-as_of closure payloads** in surviving rows
  (round 1 could not have passed this; the red-team measured 4/6/8 leaks).
- **RT-2 weekly reproduction:** the red-team's exact config (baseline, seed 7, weekly):
  **0 of 22** terminal events knowable at the prior decision close (was 22/22); no
  announcement precedes the prior period's publication instant.
- **RT-3:** `fetch_estimates` over the full window serves all **7** fiscal-year series
  (was 2 rows); **RT-8:** 0 stamps after the window-end bar close.
- **RT-1 surface:** `fetch_universe_membership` — 0/45 rows with `valid_to` beyond the
  window end; `fetch_security_master` collapses to exactly 1 row/key with closures
  served (snapshot/retrieval semantics — closures knowable by then).
- **RT-6:** LT-009 ablation (fresh seed 555) covers 6 tables; 0 dead-name rows in any.
- **Spot-runs:** all 3 keeper modules + LT-019 field-content probe + LT-009 teeth +
  generator interval/estimates tests + LT-021 26-seed sweep, individually:
  **66 passed, 0 failed, 0 xfail/xpass, 2.80s**.

### Pinned-hash disposition

No pinned world-hash CONSTANTS exist in tests (neither round); determinism tests assert
run-to-run equality, not fixed values, so nothing went stale in code. The PR-body proof
hashes are superseded by the emission fixes — recomputed by me from
`default_config(sid, 20260723)`:

| Scenario | Round-1 hash | Round-2 hash | Changed |
|---|---|---|---|
| baseline | `4efcece09ca17403` | `ac161ea6af603652` | yes (closure vintages, estimates identity, action sampling) |
| LT-005 | `c244bb3a430d860f` | `c244bb3a430d860f` | **no** — consistent: LT-005 has no churn/actions/estimates, so the fixes cannot touch it |
| LT-018 | `b69c96d564062ec8` | `30cc55f7c9205424` | yes (announcement stamps, action sampling) |
| LT-021 | `93a45e3932f3b2da` | `401ec5fe57baca9b` | yes (collision-safe seeder, event_dates) |

Same-seed reruns remain hash-identical; different seeds differ (regression suite + gates).

### New non-blocking finding

7. **NB-7 (generator versioning):** `GENERATOR_VERSION` remains `"1.0.0"` although its
   documented contract is "bumped whenever generated content for a fixed (config, seed)
   changes" and the remediation changed emitted content for 3 of 4 catalog reference
   scenarios. Harmless pre-merge (no consumer has cached 1.0.0 worlds from main), but
   bump to 1.1.0 (or document that versioning starts at first merge) before G037 seed
   sweeps cache worlds keyed by (version, config, seed).

### Evidence for the round-2 verdict

Gates re-run twice with identical 1122/22 and zero xfails; every diff hunk traced to a
stated RT fix with no weakened assertion; all four blocking findings (RT-1/2/3/5)
re-attacked with my own probes on my own seeds/as-ofs and found closed; ratchet flips
verified as marker-removal-only; hash changes explained, localized (LT-005 invariant),
and reproducible; PR CI green at b160f52.
