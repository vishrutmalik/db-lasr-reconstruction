# Verification report — G021

- **Goal ID:** G021 — Data-quality checks (LT-021 battery over L-CANON +
  lineage-reachable L-RAW) + queued G020 integrity hardening
  (R2-N1 / NB-6 / N11 / N3) + LT-021 activation
- **GitHub issue:** #21
- **Branch:** `agent/implementer/G021-data-quality`
- **Commit reviewed:** `c50acad58871bdea1a37d2607532dea11a50a92d`
  (10 commits incl. the origin/main merge `1d9b621`); worktree clean,
  == PR head (`gh pr view 66 --json headRefOid`)
- **Pull request:** #66
- **Verdict:** **PASS** (0 blocking findings; 3 non-blocking)

## Acceptance criteria reviewed

Read before any implementation code: issue #21 + goals.md row (no detail
block exists for G021; the binding charter is the integration-queue items),
`coordination/integration_queue.md` (G020-verification binding: NB-2/NB-3/
NB-4 already closed in G020 round 2; NB-6 queued here; G020-remediation
routing: N3 sort keys, N11 truthfulness, N10 review),
`docs/red_team/G020.md` round 2 (R2-N1 reproduction §"Attacks on the
fixes"; "G021 battery must audit ALL datasets"; data-driven basis check
recommendation), `docs/verification/G020.md` round 2 (NB-6 probe),
`docs/methodology/leakage_tests.md` LT-021 (recall 1.0, quarantine,
inverted-timestamp structural rejection, sidecar-diffable report),
`assumptions_register.md` (A-G019-01, A-002),
`skills/point-in-time-auditing/SKILL.md`.

## Commands executed (all re-run by me, fresh env)

From `.worktrees/G021-implementer`, `UV_PROJECT_ENVIRONMENT=$HOME/.venvs/lasr-verify-g021`:

- `git rev-parse HEAD` = `c50acad…`; `git status` clean; PR #66 head equal. ✓
- `git diff --stat origin/main...HEAD` → 14 files / +3591 −22:
  `data/quality/{__init__,battery,checks,report,truthfulness}.py` (owned),
  `canonical/store.py` (grants a+b: R2-N1 one-liner + NB-6 identity),
  `schemas/raw_fundamentals.py` (grant c: N3 sort key),
  `tests/unit/test_quality_*.py` (owned, 5 files),
  `tests/leakage/test_lt021_seeded_errors.py` (coordinator-directed
  activation). **Nothing out of scope.** ✓
- Pre-existing-test protection, diff-verified: extracted main's and HEAD's
  copies of `test_lt021_seeded_errors.py`; the pre-existing region
  (everything before the activation marker, imports excluded) is
  **byte-identical**; only the `@activation` placeholder was replaced and
  imports extended. Collected counts: main 35 (34 pre-existing + 1
  placeholder), HEAD 38 (34 + 4 activated). ✓
- `uv sync --locked` → exit 0. ✓
- `uv run ruff format --check .` → `169 files already formatted`. ✓
- `uv run ruff check .` → `All checks passed!` ✓
- `uv run mypy src/lasr` → `Success: no issues found in 93 source files`. ✓
- `uv run pytest -q` twice → **1366 passed, 21 skipped** both runs,
  identical (also re-confirmed twice on a later day: 4 identical runs
  total). Matches the PR claim exactly. ✓
- `uv run pytest tests/leakage -q` → **153 passed, 17 skipped**. ✓
- Quality suites + LT-021 file alone → **120 passed**. ✓
- `tests/unit/test_quality_schemas.py` alone → 4 passed (registry-wide N3
  invariant over BOTH schema registries, verified iterating all schemas). ✓
- `gh pr checks 66` → **no checks reported at `c50acad`** (no
  `pull_request` workflow event ever fired for this PR; `gh run list
  --branch agent/implementer/G021-data-quality` is empty). See NB-2 —
  the report push triggers `synchronize`; CI status to be confirmed on the
  post-report head, whose production tree is identical.
- Verifier adversarial probe script (session scratchpad `probe_g021.py`,
  outside tests/): **25/25 probes pass** (sections A–G below), plus 3
  standalone probes (A-G021-04 loud-failure, empty-store battery,
  detector input-order invariance ×10 shuffles).

## Hardening re-verification with the ORIGINAL attacks (all re-executed by me)

### (a) R2-N1 — launder-via-append (red-team round 2 §2c)

Re-ran the reproduction verbatim: write vintaged `fundamentals` v0
(kt = 2025-02-15), rewrite its parquet in place retro-dating kt to
2025-01-05 (deliberately U3-legal so only the integrity net can fire),
read `existing = store.read_records(...)`, append a v1 row, rebuild.

- **Fixed code: REFUSED** — `StoreError` naming the failed integrity
  audit at the predecessor's verified read inside `CanonicalStore.write`
  (store.py:358); nothing new persisted (`dataset_ids` unchanged). The
  tampered predecessor itself audits dirty (3 problems).
- **Teeth (pre-fix simulation):** monkeypatched
  `CanonicalStore.verified_records → read_records` at runtime (no code
  change) and re-ran the identical attack: the laundered successor
  **persisted, audited clean (0 problems), and served the retro-dated row
  at as_of 2025-02-01** — six weeks before the honest knowledge time.
  The one-liner is load-bearing; the committed regression test
  (`test_r2n1_reproduction_tampered_predecessor_refuses_rebuild`) pins
  exactly this attack with a positive twin.

### (b) NB-6 — provenance forgery (verifier G020 round-2 probe)

Re-ran verbatim: legitimately D-015-downgraded `prices_daily` dataset
(1 downgrade event), then rewrote `manifest.json` to
`capability.supports_pit=true` + `pit_grade=FULL_VINTAGES` +
`downgrade_events=[]` — a LEGAL manifest state.

- `verify_manifest_payload` → clean; `read_manifest` parses — the OLD
  nets still pass, exactly as in round 2.
- `audit_dataset` → **dirty**: "directory id … does not match the
  identity … NB-6 manifest self-hash" (and the content-hash check
  correctly does NOT fire — records untouched — pinning that the new
  identity binding is the net that catches it).
- Serve path: `verified_records` **refuses** (`StoreError`); a fresh
  `PitStore.as_of_frame` **refuses**. ✓
- Single-field variant (`provider` rewrite only) also detected. Honest
  twin audits clean; re-run is an idempotent no-op with the same
  dataset id (retrieval_time correctly excluded from identity).

### (c) N11 — retrieval-time lies

Built canonical `fundamentals` from a REAL raw snapshot (lineage anchored):

- **c1 built-before-retrieved:** rewrote the canonical manifest's
  `retrieval_time` to predate the source snapshot's retrieval. The
  artifact audit stays clean **by design** (A-G021-01: retrieval_time is
  identity-excluded) — and `check_raw_lineage_retrieval` **FAILS** with
  "the build cannot have consumed data not yet retrieved". ✓
- **c2 knowledge-postdates-retrieval:** `retrieval_time` rewritten to
  before the payload knowledge times → `check_knowledge_within_retrieval`
  **FAILS**, flagged_indices exact. ✓
- **c3 ghost anchor:** canonical citing a nonexistent snapshot id →
  lineage check FAILS ("unresolvable … CI-006"). ✓
- **c4 raw payload tamper:** rewrote a raw snapshot's `payload.parquet`
  (one value changed) → `check_raw_snapshot_integrity` FAILS on both the
  content re-hash and the directory-id binding (L-RAW previously had no
  post-write audit at all). ✓

### (d) N3 — raw sort-key ties

Two `raw_fundamentals` rows tying on the OLD sort key (Q4-2024 + FY2024,
same period_end — the exact G2 reproduction):

- Canonical order, two fresh stores → **one identical snapshot id** both
  times (`snap-20d98722b882fd49`). ✓
- Shuffled order → **refused loudly** (`IntegrityError`, "U4/CI-043",
  message names `fiscal_period`), no second snapshot minted — the
  pre-fix double-mint is impossible because exactly one canonical order
  now exists. ✓
- Identical sorted re-ingest → idempotent no-op (`created=False`). ✓
- Registry-wide invariant locked by test over BOTH registries
  (`test_n3_every_raw_sort_key_is_pk_superset`,
  `test_n3_every_canonical_sort_key_is_pk_superset`) — verified the loops
  iterate all of `RAW_SCHEMAS`/`SCHEMAS`; 4 passed.

## Detector teeth — MY OWN hand corruption (not the generator's seeder)

Hand-built a clean 24-row `raw_market_daily` panel (3 tickers × 8 days,
varying closes) + 4 clean `raw_fundamentals` rows; applied one corruption
per LT-021 class by hand; each detector must flag EXACTLY my rows
(recall AND precision). Clean batch first: **all six detectors PASS —
zero false positives.**

| detector | my corrupted rows | flagged | verdict |
|---|---|---|---|
| lt021.duplicate_rows | {24} (copy of row 3 appended) | {24} | exact |
| lt021.negative_prices | {5: close −7.25, 11: close NaN, 17: vwap 0.0} | {5, 11, 17} | exact |
| lt021.stale_prices | {9–13} (5-run frozen; threshold 5) | {9, 10, 11, 12, 13} | exact; a 4-run on another ticker correctly NOT flagged |
| lt021.impossible_volumes | {2: −100, 9: inf} | {2, 9} | exact; volume 0.0 correctly legal |
| lt021.missing_mandatory_fields | {7: currency None, 14: ticker key absent} | {7, 14} | exact |
| lt021.inverted_timestamps | {1} (kt < period_end) | {1} | exact |

Cross-detector precision also held: each corruption fired ONLY its own
class's detector. Input-order invariance: 10 random shuffles of a
corrupted batch → stale/duplicate detectors flag the same row set
(one duplicate occurrence) every time.

## LT-021 live integration

- Activated tests re-run (in-suite and standalone): recall 1.0 with
  exact-set equality per class vs the sidecar (stale at entity level per
  the anchor convention, extras asserted empty on the other five);
  quarantine of exactly the flagged indices leaves batches every detector
  passes; the inverted-timestamp row is refused at the canonical write
  path with `SchemaValidationError` (U3), nothing persisted; two
  independently generated worlds → byte-identical reports.
- **Previously-bad seeds probed end-to-end by me** (G019's red-team seeds
  10, 15, 54; my own harness, not the committed test): recall 1.0 and
  exact-set equality on every class on all three seeds (per class:
  3 seeded / 0 missed / 0 extras; stale 15 claimed rows, 0 missed).

## Report artifact

- Two `run_quality_battery` runs over the same store (fundamentals +
  downgraded prices + real raw lineage) → **byte-identical JSON**
  (5719 bytes, 24 results), clean fixture reads clean.
- Empty store → byte-identical, reasoned-skips-only report.
- Every SKIP carries a recorded reason (model-enforced:
  `CheckResult._status_consistent` makes SKIPPED-without-reason and
  FAIL-without-problems unrepresentable).
- Parquet-tampered dataset → `artifact.integrity` FAIL +
  `content.battery` **SKIPPED with reason** for that dataset; zero silent
  content checks for it; the other dataset's content battery unaffected.
- Incidental confirmation with teeth: an earlier draft of my probe
  accidentally reused a directory previously tampered by another probe
  section — the battery flagged every leftover tamper (knowledge-vs-
  retrieval, lineage ordering, raw re-hash) exactly as designed.

## Code paths inspected

- `canonical/store.py`: `_IDENTITY_EXCLUDED_FIELDS` (content_hash +
  retrieval_time only), `dataset_identity_digest` (records digest +
  manifest-sans-volatile through `canonical_json`/sha256), `write`
  (verified predecessor reads line 358; identity-derived directory id),
  `integrity_problems` (identity recomputed from on-disk payload).
  Serve/write path audit: `PitStore._records` → `verified_records`;
  `write`'s vintaged loop → `verified_records`; no production builder
  reads `existing` unverified (grep: builders.py has no `read_records`).
- `schemas/raw_fundamentals.py`: sort key = PK superset (append
  `fiscal_period`); no other schema change.
- `quality/checks.py`: six LT-021 detectors (pure functions, no store/
  clock/env access), conformance re-sweep via `collect_problems`,
  coverage with typo-guard (`QualityCheckError` on undeclared threshold
  columns) and empty-batch loud fail, three reconciliations (delisting
  relist-aware via open-interval override; factor lineage id+date; split
  basis with the B3 ledger signature and unverifiable-gap flagging).
- `quality/truthfulness.py`: the three N11 checks (lag-rule exemption per
  A-002 on the knowledge check; full re-hash incl. identity fields for
  raw snapshots).
- `quality/battery.py`: every dataset of every table artifact-audited
  (`audit_all_datasets` — the R2-N1 companion), content battery only on
  integrity-clean datasets, `content.battery` SKIPPED recording,
  deterministic iteration (registry order / sorted ids / sorted raw
  refs), reconciliation resolution pin/only/reasoned-skip.
- `quality/report.py`: status-consistency model validators;
  `canonical_json` serialization; no wall-clock anywhere (grep: no
  `datetime.now`/`utcnow`/`time.time`/`environ` in `src/lasr/data/quality/`).
- Tests: no mocks anywhere in the quality suites (grep), no circular
  fixtures found; the integrity tests execute the original attack
  mechanics (real parquet rewrites, real manifest rewrites), not
  simulations of them.

## Edge cases attempted

Empty store battery; empty-batch coverage (loud fail direction, per
suite); single-entity panels; NaN/inf/zero prices and volumes; absent
dict keys vs None values (both flagged); U3-legal retro-dating (so only
integrity nets can fire); sub-threshold stale runs (not flagged); relist
semantics in delisting reconciliation (per suite); shuffled input order
×10 (same row set flagged); two fresh stores (identical content-addressed
ids); idempotent re-runs post-NB-6 (identity stable); hand-built
partial-column raw payloads (A-G021-04: loud FAIL, never silent —
probe-confirmed).

## Leakage risks checked

- The manufactured CI-001 violation (inverted timestamp) is refused at
  the canonical write path (U3) — nothing persisted — AND independently
  re-detected on stored data by the detector (write-path trust not
  assumed).
- Retro-dated payloads: refused at serve (verified_records), dirty at
  audit, and can no longer be laundered through an append rebuild (R2-N1).
- Provenance/grade forgery in a legal manifest state: detected via
  identity binding; refused at serve (NB-6).
- Retrieval-time rewrites: identity-excluded by design but cross-checked
  by N11 (knowledge-vs-retrieval, lineage ordering, B4b stamp check for
  market bars). Residuals accurately documented (full re-forgery incl.
  directory rename; non-PIT raw retrieval_time — the accepted G020
  round-2 trust boundary).
- Quarantine surface (`flagged_indices`) drops exactly the seeded rows;
  requarantined batches pass every detector — no silent consumption path.
- Detectors are pure (no clock/env/store access) — no hidden state.

## Quantitative invariants checked

- LT-021 pass/fail: recall = 1.0 on all six seeded classes, exact-set
  equality (no extras), on the battery seed AND seeds 10/15/54 (my own
  harness). Sidecar-diffable: byte-identical reports across independent
  generations.
- Detector precision: exact flagged-index sets on my hand corruptions;
  zero false positives on clean batches.
- Determinism: 4 identical full-suite runs; battery reports
  byte-identical across runs and across fresh stores; content-addressed
  ids stable across stores and input re-ingests.
- Idempotency: identical re-ingest/rebuild → `created=False`, same id
  (raw and canonical, post-NB-6).
- N3: one row set ⇒ one snapshot id, all orders (accept-or-refuse, never
  two ids).
- Split-basis ledger: the B3 numbers (50.2 vs 100.4 across a 2:1 split)
  pinned in the suite; reconciliation flags continuity-through-split as
  the pre-adjusted signature.

## Assumption candidates A-G021-01..04 vs code

- **A-G021-01** (identity excludes retrieval_time; N11 nets catch
  manifest-side rewrites; non-PIT raw residual): ACCURATE —
  `_IDENTITY_EXCLUDED_FIELDS`, probes B3/C1/C2; the residual is real
  (non-PIT raw snapshots carry no knowledge column to cross-check).
- **A-G021-02** (visible config defaults 5 / 0.05 / empty; activation
  uses 3): ACCURATE — `QualityCheckConfig` defaults + validation;
  activation harness passes `stale_run_length=3` matching the generator's
  anchor+≥2 seeding.
- **A-G021-03** (U3 exemptions enumerated; reasoned SKIPs): ACCURATE —
  `U3_EXEMPT_TABLES` + battery SKIP branch.
- **A-G021-04** (full-column re-hash assumption; loud failure):
  ACCURATE and probe-confirmed (partial-column payload → FAIL with
  re-hash + directory-id problems; never a silent pass).
- **Not yet registered** in `assumptions_register.md` — see NB-1.

## Blocking findings

None.

## Non-blocking findings

1. **NB-1** — A-G021-01..04 exist only as PR-description "candidates";
   they are not registered in `assumptions_register.md` (precedent:
   A-G019-01 was registered by the implementer in a dedicated commit).
   Content verified accurate against the code; register at/before merge.
2. **NB-2** — CI never ran on PR #66 at `c50acad`: zero workflow runs on
   the branch, zero check runs at the SHA (the `pull_request` event
   evidently never fired — an infra hiccup, not a code property; G022's
   and G023's concurrent PRs did run). The verification-report push
   triggers `synchronize`; the run on the post-report head (identical
   production tree) is the CI evidence for this PR.
   **CONFIRMED post-push** (`gh pr checks 66` at `4e80cc7`, run
   30910363346): lint / typecheck / test ×3 (ubuntu 3.11, 3.12; macos
   3.12) / **leakage-fast** all pass; determinism + e2e-smoke design-gated
   skips. Resolved.
3. **NB-3** — the newly-activated LT-021 leakage tests are not selected
   by ANY CI job: `test` runs `-m "unit or integration"`, and
   `leakage-fast` filters `-k "lt005 or lt010 or lt013 or lt019 or
   lt020"`. They run locally and in the verifier gates, but a regression
   would not trip CI. `ci.yml` is orchestrator-owned (correctly untouched
   by this branch); recommend extending the `-k` filter with `lt021` at
   merge.

Observation (not a finding): `run_quality_battery` applies the LT-021
content detectors to canonical tables and audits lineage-reachable raw
snapshots for integrity/N11 only; the raw-payload detector pass is
exercised through the LT-021 leakage harness. Consistent with the L-CANON
charter (canonical row models make negative prices/inverted stamps
unrepresentable at write; detectors re-check stored data against
tampering) — worth remembering when G029 wires the battery into the CLI.

## Recommendations

- Register A-G021-01..04 in `assumptions_register.md` (NB-1).
- Confirm the post-report CI run green, including leakage-fast (NB-2).
- Extend leakage-fast's `-k` filter with `lt021` (NB-3, orchestrator).
- G029: wire `run_quality_battery` into the CLI slice and persist the
  report JSON as a run artifact (the PR's own stated limitation).

## Evidence

- Gate outputs above, all re-run by me (twice for the full suite, on two
  separate days — four identical runs).
- Probe script `probe_g021.py` (session scratchpad, outside tests/):
  25/25 probes, sections A–G, with the pre-fix monkeypatch simulation
  proving the R2-N1 one-liner load-bearing (laundered successor audits
  clean and serves retro-dated rows without it).
- Standalone probes: A-G021-04 loud failure; empty-store battery
  determinism; 10-shuffle detector order invariance.
- Diff extractions proving the 34 pre-existing G019 tests byte-identical
  (collected-count reconciliation 35 = 34 + placeholder → 38 = 34 + 4).
