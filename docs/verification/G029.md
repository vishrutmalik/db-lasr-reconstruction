# Verification report — G029

- **Goal ID:** G029 — End-to-end synthetic vertical slice (CLI, reproducible)
  + accumulated integration charter (grants 4a–e, 5, 6)
- **GitHub issue:** #29
- **Branch:** `agent/implementer/G029-vertical-slice`
- **Commit reviewed:** `0c89a08cc6298860429a666c5502e561effd5672` (6 goal
  commits on main @ 393a603; production code unchanged by the later
  additive red-team/verifier docs+test commits on the shared branch)
- **Pull request:** #74
- **Verdict:** **PASS** (0 blocking findings; 8 non-blocking)
- Verified from fresh context in a dedicated locked venv
  (`UV_PROJECT_ENVIRONMENT=$HOME/.venvs/lasr-verify-g029`,
  `uv sync --locked`, `TEST_SEED=1729`). Criteria, integration-queue
  bindings and docs/verification/G027.md M-1..M-6 read BEFORE the diff.

## Acceptance criteria reviewed

- PR #74 acceptance record (goals.md row → PR body): CLI slice, CI-042
  double-run byte identity, CI-045/CI-055 end-to-end re-assertion
  (correctness_criteria.md §"G029 vertical slice"), A-003 banner
  discipline, A-G029-01..08, 10 ratchet flips, gate items 4a–e/5/6,
  5 found-not-fixed out-of-lane items.
- Every G029-binding section of coordination/integration_queue.md
  (RT-G026-2/3 · G034-r2-NB-4 · N8 · zero-test-row folds · RT-G028
  block + seam notes · G025 NB-3/NB-4 + knob promotion · G028 wiring
  items · RT-G035-3 gate + G035 interface notes · G027 adapter contract
  M-1..M-6/RT-G027-8/-5/-3 · CT-16).

## Diff enumeration & scope (vs merge-base 393a603)

38 files, +4825/−219. Owned paths: `src/lasr/pipeline/**` (6 new),
`src/lasr/cli/__init__.py`, `tests/end_to_end/`, `tests/unit/
test_pipeline*/test_cli*/test_config_g029_knobs*`. Grants: 4a
`reporting/panel.py`+`portfolio_metrics.py`; 4b `models/ensembles/
combine.py`+`selectors.py`; 4c `portfolio/level3_optimizer.py`; 4d
`validation/folds.py`+`runner.py`+`__init__.py`; 4e `config/ensemble.py`
+`experiment.py`+`sections.py`; ratchet flips `portfolio/accounting.py`
(RT-G027-5 + the owner-routed CI-045 tolerance) + 5 `tests/leakage/`
files; item 5 CT-16 in `tests/integration/test_provider_contract.py`;
item 6 `.github/workflows/ci.yml`; `configs/experiments/
smoke_synthetic.yaml`. Accompanying minimal test edits verified line-by-
line: `test_import_rules.py`/`test_package_imports.py` (register the new
package only), `test_reporting_panel.py` (match-string update),
`test_validation_folds.py` (pre-fix sub-horizon test flipped to pin the
refusal, old behavior preserved in the docstring). One scope NOTE (N-3):
`configs/models/nlasr_2012.yaml` — new unowned directory, byte-identical
to the merged `tests/fixtures/config/nlasr_2012.yaml`.

**Found-not-fixed honesty:** `data/canonical/builders.py`, schemas,
`features/library.py`, `docs/architecture/system_design.md` all
UNTOUCHED by the diff — the 5 out-of-lane items are reported, not
stealth-fixed. A-G029-01..08 registration correctly left to the
orchestrator merge duty (precedent: A-G027 registered by coordination
commit 4b7032d).

## Commands executed (all re-run by the verifier)

| Command | Result |
|---|---|
| `uv sync --locked` (fresh venv) | OK |
| `uv run ruff format --check .` | `291 files already formatted` |
| `uv run ruff check .` | `All checks passed!` |
| `uv run mypy src/lasr` | `Success: no issues found in 153 source files` |
| `uv run pytest -q` run 1 | `2566 passed, 23 skipped, 18 xfailed` (78.4s) |
| `uv run pytest -q` run 2 | `2566 passed, 23 skipped, 18 xfailed` — identical |
| `uv run pytest -m e2e -k "not TestDeterminism"` | `7 passed` (4.9s) |
| `uv run pytest -m e2e -k "TestDeterminism"` | `3 passed` (4.2s) |
| `uv run pytest -m leakage -k "lt005 or …"` (leakage-fast filter) | `78 passed, 1 skipped` |
| CT-16 subset (`-k ct16 -rs`) | `3 passed, 3 skipped` — skips carry reasons (non-PIT provider families); matches the +3-skips claim |
| **`lasr run` into TWO fresh roots** (shipped smoke config) | both: `run-288f8ed46e0439d7`, 989 predictions, 40 periods, `passed: False`; **48 files, 48/48 byte-identical (cmp per file), 0 differing** |
| `lasr verify-run <run_dir>` | `verified:` exit 0 |
| rerun into existing root | no-op; full sha256 sweep of the root unchanged |
| seed 2024 run (fresh root) | different run_id/config_hash; predictions 971; ledger content differs (CI-042 seed sensitivity) |
| seed probe at 16×4 (`build_data_stage`) | seeds 2,3,5,7,11,13,42,123 ALL fail `IdentityError` (LT-018, successor ticker); 99/1729/2024 clean — reproduces the PR claim exactly |
| verifier hand tests through `lasr.costs` + adapter | all pass (below) |
| tamper probes on a copied run | both caught (below) |
| `gh pr checks 74` @ 0c89a08 | 7 active jobs pass; determinism 29s, e2e-smoke 26s, lint 15s, typecheck 20s, tests 41/42s, leakage-fast 37s; test-macos `skipping` (push-only, preserved) |

Xfail arithmetic cross-check: baseline 28 xfails → 18 = exactly the 10
removed strict-xfail markers counted in the tests/leakage diffs.

## Tests passed / failed

Passed: full suite 2566 twice (identical counts); all job-filter subsets
above; CI green at 0c89a08. Failed: none.

## Ratchet-flip integrity (10/10 — each marker removed, fix pinned, pre-fix values preserved in comments)

| Ratchet | Fix inspected in production code | Flip judgement |
|---|---|---|
| RT-G026-2 | `select_training_records` refuses mixed `horizon_steps` (folds.py) | genuine; test still runs the original leak oracle if no refusal fires |
| RT-G026-3 | `0<e<1` refused for overlapping families at BOTH `generate_folds` and selection; docstring exactness claim scoped to symmetric close-to-close, RT-G026-1 explicitly still open/ratcheted | genuine; old sub-horizon unit test flipped honestly with pre-fix zone in docstring |
| RT-G027-5 | engine refuses negative charges (typed, `run_accounting`) | genuine |
| RT-G028-1 | `cost_borrow_drag` refuses negative recorded charges; `portfolio_summary` routes through it (line 299) | **SPECIAL RULING: restructure SOUND.** The engine now refuses the original premise, so the test (1) pins the engine refusal AND (2) rebuilds the poisoned ledger via `dataclasses.replace` — the ORIGINAL attack shape (hostile ledger reaching the reporting layer) is preserved, and the pre-fix accepted arm (compute-with-nonneg-drag) is REPLACED by a strict refusal expectation. Strengthened, not weakened. Pre-fix values (−2.35 drag, Sharpe 48) in comments. |
| RT-G028-2a | panel outcome identity `(security_id, target_end)`; as_of+1s cannot double-count | genuine; old `(as_of, security)` under-key removed |
| RT-G028-2b | per-date `(target_start, target_end)` heterogeneity refused | genuine |
| RT-G025-1 | duplicate `ComponentICRecord` keys refused in `seasonal_rank_ic_weights` | genuine |
| RT-G025-2 | optional `label_date` validated at construction (month cross-check + `label_date < target_end`) AND unstamped-bucket single-calendar-offset check | genuine; the all-forged-same-offset residual is inherent without stamps and the code says so; pipeline producer always stamps (`model_stage` sets `label_date=signal_time`) |
| RT-G025-5 | only `weight > 0` components gate composite coverage | genuine; verifier probe: zero-weight EMPTY component is inert |
| RT-G035-3 | eigenvalue rank/PD guard (`n·eps·max|λ|` cutoff) on any covariance consumed by the vol cap / risk_aversion; typed `RiskModelInputError` | **SPECIAL RULING: dual resolution SOUND.** (1) The guard — the substance of the gate item — is fixed on the shared surface and tested both ways (collinear T≤N/δ=0 refuses naming the deficiency; a full-rank covariance still builds with predicted vol > 0). The replaced live-bug teeth test documents its pre-fix pins in the docstring. (2) `portfolio_level=3` → typed `PipelineConfigError` naming G038 — probed LIVE by the verifier through the CLI. NOT scope-dodging: running L3 half-wired (no alpha-scale/NAV/attribute wiring; RT-G035-4 decomposition still ratcheted) would be worse than refusing, and the guard means no future L3 leg can consume a rank-deficient covariance. Red-team keeper now also pins refusal + no-blessed-artifacts. |

## G027↔G034 adapter audit (M-1..M-6, RT-G027-8) — hand-recomputed by the verifier through `lasr.costs` directly

- **Rate base (RT-G027-8):** establishment NAV 1000 / gross-2 book at
  20 bps → cost **2.0 = rate × Σ|trade| = rate × gross × NAV** (the G/2
  reading would give 1.0); G027-P1-shape rebalance Σ|trade| 2164.8 →
  4.3296. Matches the branch's `TestRateBasePin` (4.0 vs 2000 two-way)
  AND the live smoke ledger: `cost == 0.002 × turnover_two_way ×
  nav_start` exact on all 40 periods. Per-side/per-dollar-traded pinned
  end-to-end (A-G035-10 agreement).
- **M-2/M-3 accrual:** borrow = fee_pa × short × days/365 recomputed by
  hand (0.01 × 500 × 31/365) — exact; dcf×denominator must invert to
  integer days ≥ 1 (dcf=0.5/365 refused; dcf=0.0 = no accrual window, no
  mark); one aggregate `__SHORT_BOOK__` mark per period.
- **M-1:** one `Trade` per security per rebalance by construction
  (sorted ids); attributes side channel typed. **Zero-notional
  unreachability:** engine filters `delta != 0.0` (accounting.py:397);
  adapter refuses a zero row (probed).
- **M-4:** collapses to `(totals.trading_total, totals.borrow)`; full
  `CostRunResult` ledgered per period (`period_records` → persisted
  cost_ledger.json); zero-borrow banner logged + surfaced (live in CLI
  stdout, manifest `zero_borrow_banners`, cost ledger rows).
- **M-5:** `RunContext.aum` == the hook's pre-trade NAV (spy probe
  1234.5 → 1234.5). **M-6:** spy adapter proves ONLY `run()` is called —
  `price_trade`/`price_trades`/`accrue_borrow` untouched.
- **Output guard:** negative and non-finite (cost, borrow) both refused
  at the adapter (4 hostile-model probes) — RT-G027-5 defense in depth.
- **Two-way seam exactness:** Σ|trade| vs hook two_way mismatch refused.
- **CI-045 tolerance (RT-G027-3/N-1):** `RECONCILIATION_TOLERANCE`
  tightened to the criterion's 1e-10 with the divergence-regime
  justification documented at the constant; live smoke worst persisted
  residual **5.55e-17**; e2e re-asserts 1e-10 from disk. Honest
  direction (extreme-gross books refuse rather than bless).

## CI bindings re-asserted end-to-end (against LIVE artifacts, not fixtures)

- **CI-042:** e2e `TestDeterminism` runs the REAL CLI into tmp roots and
  byte-compares FULL trees (>20 files asserted; 48 in the shipped
  smoke); seed change asserted to change ledger hashes; rerun-no-op
  asserted via mtimes. Verifier independently reproduced all three.
- **CI-045/CI-048/NAV chain:** re-asserted from the PERSISTED ledger by
  `verify_run` AND by e2e at the written tolerances. Verifier's
  forged-hash tamper probe (edit ledger cost −500, recompute its
  manifest hash) is still caught by the CI-048 identity re-assertion —
  the accounting re-checks are genuinely independent of the hash sweep.
  Plain tampering caught by hash mismatch (both probed live).
- **CI-055:** acceptance targets evaluated as bands (`band > 0`
  asserted), measured values recorded, synthetic run records
  `passed: false` honestly (live manifest confirms; no crash, no fake
  pass); unmeasurable keys typed `not_evaluated_at_g029`.

## ci.yml audit (item 6)

Diff has exactly 3 hunks: header comment, e2e-smoke activation,
determinism activation. Both jobs ubuntu-only; `pytest -m e2e` split
disjointly by `-k TestDeterminism`. `paths-ignore` blocks (pull_request
AND push) byte-identical to main; `test-macos` push-only guard
untouched; test matrix unchanged (2 ubuntu legs). Measured wall times on
the PR's run: determinism 29s, e2e-smoke 26s — matches the ~30s claim,
well under budget.

## Bindings closed (item-by-item)

- **CT-16** both arms: (a) closure rows stamped ≥ closure event,
  parameterized over both interval tables × ALL registered providers;
  (b) pre-closure membership window fetch serves intervals OPEN (no
  vanish, no early closure). 3 pass on the synthetic provider; 3
  skip-with-reason on non-PIT families.
- **N8:** `FEATURE_VALUES` PK excludes `knowledge_time` (schema-pinned
  test); stamps are batch properties; persisted feature_values.json
  keys ONE stamp per batch beside unstamped rows (live artifact
  inspected).
- **factor_selection_stability:** wired to G024's per-fit
  `selected_factor_ids` union via the bridge ledger; live report shows
  real values (n_fits 41, mean_jaccard 1.0), not NOT_AVAILABLE.
- **LT-004 home:** manifest `leakage_audit` (per-feature mean |rank IC|
  vs config threshold; live: 3 features, 0.157–0.170, clean at 0.30) +
  `acceptance_verdict` blocks `passed` on any suspected leak
  (unit-pinned: in-band + leak ⇒ False; nothing-evaluated ⇒ False).
- **Zero-test-row FoldSkip:** typed `FoldSkip(ZERO_TEST_ROWS)` ledgered
  on `PredictionSet` (additive default); live run ledgers fold_0040;
  e2e asserts the manifest/fold-ledger identity.
- **G028 NB-3** (empty panel → typed refusal, no fabricated
  horizon default) and **G025 NB-4** (empty weighted component map →
  typed refusal): both LIVE — probed directly by the verifier (see N-2).
- **RT-G023-2 skip-ledger shape:** target skips (calendar_exhausted,
  missing_end_price) flow into coverage accounting; live coverage block
  `fully_accounted: true, total_unaccounted: 0` over 40 dates.

## Merger-successor probe (item 8) — ruling

Reproduced EXACTLY at the claimed 16×4 world: seeds 2,3,5,7,11,13,42,123
all fail the canonical build with the typed
`IdentityError: successor ticker … (exchange …) does not resolve to a
minted security_id (LT-018 identity chain)`; 99/1729/2024 clean. The
failure is honest (typed, named, no partial blessed run — red-team
keeper also pins this) and the fix is genuinely NOT in the diff. Seed
choice documented in the e2e determinism test comment + PR body; the
shipped smoke seed (1729) is clean at its own 30×6 world (verifier ran
it end-to-end repeatedly). **Nuance (N-5):** clean-seed lists are
world-size-specific — seed 99 is clean at 16×4 but FAILS at the smoke
config's 30×6. The canonical-owner fix stays REQUIRED before
arbitrary-seed runs (G038 gate, already routed).

## Code paths inspected

`pipeline/experiment.py` (runner, run-id = sha256(resolved identity)
[:16], idempotent-rerun verify-first no-op, `_apply_overrides` tagged
leaves, acceptance evaluation, `_persist` canonical-JSON hashing,
`verify_run` independent re-assertions); `pipeline/data_stage.py`
(composition-only; config-derived retrieval instant — no wall-clock
reads anywhere in pipeline/cli, grep-swept; PK-unique vintage waves,
input-order invariant; interval-vintage merge with LT-016 refusal;
quality gate refusal); `pipeline/feature_stage.py` (per-run eligibility
discipline with drop ledger; monthly-native kernels, 40-day staleness in
evidence strings; typed all-dropped refusal); `pipeline/model_stage.py`
(CI-016 ±1 labels only; block stamps = max(feature batch stamp, record
knowledge cutoffs); IC realization protocol — records appended at
scoring with true `target_end` + `label_date`, CI-007 strict filter
internal to weighting, weights taken at `fit_as_of`: the LT-005
protocol holds); `pipeline/cost_adapter.py` (above); `cli/__init__.py`
(banner-first stdout; typed refusals; scenarios catalog); grant files
(above); `configs/experiments/smoke_synthetic.yaml` (all overrides
tagged; every run knob explicit — A-G029-08).

No stub cores, no hardcoded paths, no mocked-assertion-only tests, no
circular fixtures; e2e asserts against live CLI artifacts. Performance
is plausible for the world size (~2.8s CPU per smoke run; 41 fits).

## Edge cases attempted (verifier probes beyond the branch's tests)

- `portfolio_level=3` live through the CLI → typed refusal naming G038
  (no artifacts left). Missing `pipeline:` section → typed. Unknown
  `universe_instance` → typed naming the served universe. Seed 99 at
  30×6 → typed IdentityError (honest failure).
- Tamper probes: score edit → hash mismatch; ledger cost hole WITH
  forged manifest hash → CI-048 identity break. Both fail `verify-run`.
- Adapter: fractional accrual days, zero-notional row, two-way mismatch,
  negative/NaN/inf outputs, spy on protocol surface, aum probe — all as
  documented.
- Combine: zero-weight EMPTY component inert (composite from the
  weighted component); order-shuffled component maps identical output;
  empty weighted map refused.
- Empty `PredictionSet` → `PanelConstructionError` (no fabricated
  horizon).
- `embargo_horizons=0.0` on an overlapping family still selects
  (embargo off) — legal per CI-015(b) "configurable … defaults ON";
  see N-7.

## Leakage risks checked

- Interval-merge stamps the FINAL interval at the OPEN instant —
  PIT-sound for containment; delayed closure publication REFUSED
  (probed by branch unit test); residual `valid_to` direct-read hazard
  honestly documented as A-G029-02 with a schema-owner follow-up (N-4
  notes the +1-day guard nuance).
- IC-weighting look-ahead: realized ICs enter weighting only via the
  CI-007 strict `target_end < fit_as_of` filter; records stamped with
  true window ends by the producer (code-inspected).
- LT-004 audit live on every run + acceptance gate blocks `passed`.
- Survivorship: coverage accounting is two-sided per date (universe vs
  predicted/unscored/skipped, `unaccounted` empty); delisting realized
  through the G027 engine (terminations ledgered — 40-period live run
  carries termination rows).
- CT-16 closes the RT-9 contract-level interval-leak hole.

## Quantitative invariants checked

| Invariant | How probed | Result |
|---|---|---|
| CI-042 byte identity (48 artifacts) | two fresh-root CLI runs, per-file cmp | 48/48 identical |
| seed sensitivity | seed 2024 run | content + run_id differ |
| idempotent rerun | sha256 sweep before/after | unchanged |
| CI-045 two-path ≤ 1e-10 | recomputed from persisted ledger rows | worst 5.55e-17 |
| CI-048 net = gross − cost − borrow | persisted rows + forged-hash tamper probe | holds; tamper caught |
| NAV chain + final_nav | recomputed across 40 periods | exact |
| rate base = per-side per-dollar-traded | hand tests through lasr.costs + live ledger identity | exact on all periods |
| charges ≥ 0 and finite | engine + adapter + reporting refusals probed | all typed |
| accrual integer-day inversion | hand (28d, 31d) + refusal (0.5d) | holds |
| rank/PD guard | collinear T≤N refuses; healthy builds | holds |
| LT-004 gate | unit pin + live manifest | blocks `passed` |

## Blocking findings

None.

## Non-blocking findings / recommendations

1. **N-1 (late L3 refusal; untested level-2 arm):** `portfolio_level=3`
   refuses only at `_build_book` — AFTER the full data/feature/model
   stages ran (~minutes wasted on big worlds; fail-closed, no blessed
   artifacts — red-team keeper pins this). Recommend an up-front config
   check in `run_experiment`. The pipeline's level-2 arm
   (`build_signal_weighted_portfolio` path incl. the beta-producer
   refusal) is never driven by any test. Owner: G030+/G038.
2. **N-2 (refusals live but unpinned in-branch):** G028 NB-3
   (empty panel) and G025 NB-4 (empty weighted component) have no
   dedicated unit tests on the branch (verifier probed both directly).
   Recommend two small pins. Owner: reporting/ensembles suites.
3. **N-3 (config duplication):** `configs/models/nlasr_2012.yaml` is a
   byte-copy of `tests/fixtures/config/nlasr_2012.yaml` in a new
   unowned directory (a hair outside the enumerated
   `configs/experiments/` grant). Drift hazard between fixture and
   runtime copy — single-source one from the other. Owner: config owner.
4. **N-4 (guard vs docstring):** the interval-merge refusal fires only
   when the closure was published > interval_end **+1 day**; the
   docstring says "no later than the interval's own end". Benign on the
   monthly grid; document the grace day or drop it. Owner: pipeline /
   A-G029-02 schema follow-up.
5. **N-5 (seed-list scope):** merger-successor clean seeds are
   world-size-specific (99 clean at 16×4, FAILS at 30×6). The G038
   arbitrary-seed gate should screen per configuration until the
   canonical fix lands. Owner: canonical builders (already routed).
6. **N-6 (oos_coverage semantics):** `coverage_fraction` 1.0 is measured
   from FIT test windows over the planned grid; the final grid day is
   "covered" by fold_0040's fit though it produced zero predictions
   (honest only in combination with `fold_skips` + coverage
   accounting). Consider keying covered days on predictions or
   documenting the fit-based semantics. Owner: reporting/G030+.
7. **N-7 (embargo off-switch):** `embargo_horizons=0.0` on overlapping
   families disables the embargo entirely while 0<e<1 now refuses —
   defensible per CI-015(b) "configurable", but a doc note (and maybe a
   warning) would prevent the off-switch reading it as a loophole.
   Owner: validation owner.
8. **N-8 (CLI ergonomics):** pipeline refusals surface as raw tracebacks
   (typed exceptions, nonzero exit). Cosmetic; a caught-and-printed
   typed error line would be friendlier. Owner: CLI polish, G030+.

## Evidence for the verdict

- Every gate re-run twice from a fresh locked venv with identical counts
  matching the PR claim exactly (2566/23/18); all four CI job filters
  re-run locally; PR CI green at 0c89a08 with claimed wall times.
- The headline invariant reproduced INDEPENDENTLY: two fresh-root CLI
  runs byte-identical across all 48 artifacts; verify-run clean;
  rerun no-op; different seed changes content.
- All 10 ratchet flips inspected marker-by-marker with their production
  fixes; both special cases (RT-G028-1 restructure, RT-G035-3 dual
  resolution) ruled sound with live probes.
- The adapter's every pinned convention re-derived by hand through
  `lasr.costs` directly, and the rate base re-verified against the live
  persisted ledger of the verifier's own run.
- The merger-successor claim reproduced seed-for-seed (8 fail typed /
  3 clean) at the probe world size, plus the world-size nuance found.
- Two tamper probes (including a forged-manifest-hash variant) defeated
  by the persisted-identity re-assertions.
