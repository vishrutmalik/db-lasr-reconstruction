# Verification report — G039

- **Goal ID:** G039 — Real-data (AlphaSense) integration guide + onboarding
  runbook (adapter spec against the verified provider contract)
- **GitHub issue:** #38
- **Branch:** `agent/data-researcher/G039-real-data-guide`
- **Commit reviewed:** `5ba8eea30c9b6ae97db6de7078631fff42fcc8a6`
  (docs commits `2c3e8c1`, `b4fa015`; merge of main `3b5984a`; marker sweep
  `5ba8eea`)
- **Pull request:** #59 (head `5ba8eea…` confirmed via
  `gh pr view 59 --json headRefOid`)
- **Verdict:** **PASS** (0 blocking findings against the deliverables;
  1 URGENT repo-level escalation — input-file drift — discovered during
  verification, not caused by this branch; 5 non-blocking findings)

## Acceptance criteria reviewed

Read before the deliverables: issue #38 + comments (drift-check-after-G018
sequencing), `coordination/agent_assignments.yaml` G039 entry (owned paths:
exactly the two docs), `goals.md` row, `skills/pr-verification/SKILL.md`,
`.claude/agents/verifier.md`. Ground-truth sources read independently:
`docs/architecture/provider_contract.md` (incl. main's post-branch D-013
annotation, commit `870cbd4`), `decisions.md` D-009/D-011/D-012/D-013/D-015
(incl. main's corrected D-012 citation), `docs/data/{gap_list, field_mapping,
feature_coverage, pit_assessment, data_dictionary, evidence_rows}.md`,
`docs/data/workbook_schema/*`, `assumptions_register.md`,
`docs/architecture/{canonical_schemas, system_design}.md`, merged G018 code.

## Commands executed

All from `.worktrees/G039-data-researcher` (absolute paths), `gh` at
`~/.local/bin`:

- `git status` → clean; `git rev-parse HEAD` → `5ba8eea…`. ✓
- `git merge-base origin/main HEAD` → `8455ac6`;
  `git diff --stat origin/main...HEAD` → **exactly 2 files, both new**:
  `docs/data/real_data_integration.md` (+473),
  `docs/runbooks/real_data_onboarding.md` (+204). The two-dot
  `git diff origin/main HEAD` noise is main advancing after the branch's
  merge (G043 merge `d4c5c8a`, docs fix `870cbd4`, progress `e9597b1`) —
  no file on the branch is modified relative to the merge-base other than
  the two owned files. Diff scope exact. ✓
- Marker sweep: `grep -rn ADAPTER_PATH_PENDING_G018` over both files →
  **zero occurrences** (exit 1). ✓
- `uv run pytest -q` (fresh worktree venv) → **527 passed, 1 skipped**
  (CT-11 skip-with-reason, matching the guide's crosswalk row verbatim). ✓
- `uv run pytest tests/integration/test_provider_contract.py -q` →
  18 passed, 1 skipped. ✓
- openpyxl re-verification of every workbook cell the guide claims to have
  spot re-verified (script in session scratchpad, read-only): results below.
- `shasum -a 256` on both live workbooks vs `input_manifest.md`: see
  escalation E-1.
- Quote-length scan (regex over both files, all `"…"` spans): **no quote
  exceeds 15 words**. ✓
- Fabrication greps: vendor names (Bloomberg/Refinitiv/FactSet/CRSP/
  Compustat/IBES/WRDS/…), `https?://`, endpoint paths → zero vendor
  endorsements, zero invented endpoints/URLs (single regex hit was
  "descr**ibes**" matching /ibes/ — false positive). ✓

## Marker-sweep verification (every swept reference vs merged code)

| Swept reference in docs | Verified against | Result |
|---|---|---|
| `LocalFileProvider._build_capabilities()` | `src/lasr/data/providers/local_file.py:436` | ✓ exists; record matches guide §1.2 line-for-line |
| `_PRICE_FIELD_CODES` (`close→CLOSE`, `market_cap→MCAP`) | `local_file.py:96` | ✓ exact mapping |
| `_CLASSIFICATION_SCHEME_CODES`, `_SECURITY_MASTER_FIELDS` | `local_file.py:99/106` | ✓ |
| `UnknownProviderIdError` in `base.py`; D-015 closed-set addition | `base.py:194`; contract §3 amendment (line 100) | ✓ raised for unresolvable ids (CT-12 test asserts it) |
| `DEFAULT_PRICE_FIELDS` / `LISTED_ONLY_PRICE_FIELDS` | `base.py:208/213` | ✓ `("close","market_cap")` / OHLV+vwap+shares |
| `grade_dataset()` (guide §2.4) | `base.py:249` | ✓ D-011 split + D-015 downgrade implemented exactly as §2.4 states |
| `tests/integration/test_provider_contract.py` (CT-01..15) | file exists; suite ran green | ✓ |
| Four `.env.example` names (runbook §1.2) | committed `.env.example` | ✓ `LASR_LOCAL_TEMPLATE_ROOT`, `LASR_API_BASE_URL`, `LASR_API_KEY`, `LASR_API_SECRET` — names match exactly; CT-14 test uses the same names as canaries |

## Content spot-checks (all passed; 20 performed, ≥12 required)

1. Guide §1.2 capability table = `provider_contract.md` §4.2 **verbatim**
   (all 14 rows, values and sources).
2. Guide §1.2 vs `_build_capabilities()` code: 8 cross-family booleans all
   `False`; MARKET_DAILY `LATEST_ONLY`/`history_start=None`/basis `UNKNOWN`;
   FUNDAMENTALS `LATEST_ONLY` + FY-5..FY+2 note; ESTIMATES/SECURITY_MASTER/
   CLASSIFICATIONS revision `NONE`; FX unavailable; CALENDAR derived-with-note.
   Structural note (10 gap-list flags → 8 booleans + 2 structural) verified
   against gap_list "Consequences" (exactly 10 named) and the dataclass.
3. Guide §1.3 absence table vs `gap_list.md` §§1–8: identifiers, delistings,
   corporate actions/basis, estimate history, index membership, universe
   enumeration, borrow/bid-ask, publication timestamps, FX history,
   consolidation basis, statistic type — all match section-for-section.
4. FM-18(b) formula `r_t = (P_t + D_t)/P_{t−1} − 1` + dividend-timing quote —
   verbatim from `field_mapping.md` FM-18.
5. FM-20 derivation chain (weekly returns → 5y-std scale → sector×region
   de-mean → re-rank) — verbatim from FM-20; depth caveat carried.
6. FM-21 260-week window (E-P4-08) — matches FM-21 and `nlasr_2020.md` §4
   (line 51: "5-year (260-week) rolling std").
7. Growth/Quality formulas (asset growth, EPS growth, percent accruals,
   Sloan cash-flow form, CAPEX/assets, B/P) — all verbatim from
   `field_mapping.md` §5.2/§5.5/§5.1, incl. "zero precomputed growth" fact.
8. §2.1 table: all 21 cited dictionary rows re-checked against
   `data_dictionary.md` (r32/38/42/117/202/222/231/232/317/404/413/418/419/
   424/427/438/451/469/485/502/511) — codes and NOT_ESTABLISHED tags match.
9. VP-01..07 expected-if-true/false vs G013 findings: FM-12/13/14
   LISTED_ONLY (fiscal-period values only, daily never demonstrated);
   VP-02's r451–r454 codes NOT_ESTABLISHED; VP-03 depth caveat + no shown
   FY-anchor mechanism (pit §FY grid); VP-04 legs match pit_assessment
   (`Data!N2:O3` single option; 11-word quote verbatim; A-001 false-branch =
   "additional positive evidence" matches pit verdict); VP-07 basis-per-
   demonstrated-window matches FM-17/CT-15 guard semantics.
10. Go/no-go ladder vs `feature_coverage.md`: P1 16 groups 4D·4V·2A·5U·1N ✓;
    P2 +8 groups 1D·2V·2A·2U·1N ✓; P3 ≥13/70 + 2 named blocked, ~50–55
    plausible ✓; HC identical ✓; HF least coverable, VP-01-gated ✓; P4 16/16
    named (9 direct, 7 derivable), Growth MEDIUM, 9 groups 1D·3V·3U·2N ✓;
    modernized definitionally un-runnable ✓. §9 headline table: **uniform
    "no" for all seven** — guide §4 and runbook §7 state exactly this;
    runbook's synthetic-primary claim matches §8. Shopping lists item-for-item.
11. D-011/D-012/D-013/D-015 vs `decisions.md`: guide §2.4 resolution matches
    D-011 + D-015(b) (manifest-recorded downgrade binding G020/G021); guide
    §5 resolution cites FM-11/FM-25 — **agrees with main's corrected D-012**
    (`870cbd4`), not the superseded FM-11/31; D-013 descope wording matches
    the decision and main's provider_contract annotation (Protocol +
    records + suite + `.env.example` = the generic interface; replay = design
    requirement on future adapters, not shipped code).
12. CT crosswalk vs merged suite (6 CTs spot-checked): CT-03
    (`vintage="as_reported"/"all"` → `CapabilityError`, test line 298),
    CT-07 (OHLV-not-in-coverage → `FieldUnavailableError`, D-012 comment in
    test), CT-10 (`supports_pit=false` ⇒ **no knowledge_time column**),
    CT-12 (empty conformant frame + `UnknownProviderIdError` for unknown
    entity), CT-14 (canary env `LASR_API_KEY`/`LASR_API_SECRET`), CT-15
    (basis declared; `grade_dataset` UNKNOWN-basis downgrade asserted).
    CT-11's skip-with-reason reproduced in the live run.
13. Evidence IDs E-G012-01..15 all resolve in `evidence_rows.md` with
    matching content (513 rows/freq split, 176 consensus, 258+35 M&A/funding,
    FY grid formula, `Data!N2:O3`, J8=393594.53, 375/254 obs, holes, VL40,
    single-ticker controls, units, identifier absence).
14. Workbook-schema citations: `P_TO_BV` header-only pair at TM **AS,AT**
    (catalog line 604) ✓; FP J3:J4 support contact ✓; W1 dims 514×9 / 177×3 ✓.
15. `assumptions_register.md`: A-001 "On real data" field contains the exact
    6-word quote; A-002/A-003/A-004/A-ARCH-01 (`hash(ticker, exchange,
    first_seen)`) all as cited.
16. `canonical_schemas.md`: §1.3 `delisting_return` (CI-049/LT-009) ✓,
    §2.1 `adjustment_factors` ✓, §6.3 `universe_membership_intervals` ✓.
17. `system_design.md`: §2 names the TM panel as the RETRO_WINDOW example ✓;
    §4 rule "only `config` (and `cli`) read env" ✓ (line 246); §5 L-RAW
    `raw/<provider>/<family>/<snapshot_id>/` ✓.
18. `provider_contract.md`: §1 "nothing downstream may upgrade the grade" ✓;
    §2 ProviderId = ticker+exchange ✓; §7/Principle 3 "testable once, not per
    provider" ✓; §5 "must pass CT-01..15 unmodified" ✓.
19. MASTER_PROMPT line 1466: "Never create fake production endpoints." ✓.
20. `.gitignore`: `inputs/data_templates/*` ignored (README excepted);
    `.env`, `.env.*`, `!.env.example` — exactly as runbook §1.2 describes;
    `docs/runbooks/dev_setup.md` exists.

## Live-workbook re-verification (guide line 12–15 claim)

Re-ran the guide's claimed openpyxl spot re-verification against
`inputs/data_templates/` (main checkout): `Data!N2:O3` = Version Type /
Latest restatement → latest_filing ✓; TM C3=NVDA, C4:C5 =
2025-01-01..2026-06-21 ✓; CLOSE obs = 375, unadjusted-LTM obs = 254 ✓;
FS D8 = 16675 ✓; FS J8 = 393594.5296 ✓; FP D3 = NVDA ✓; W1 sheet dims ✓.
**FP D37 does NOT reproduce**: it now caches `Please Login to Continue`
(as do FP D29:D44); the VL40 string survives only at FS D148:K148
(`PAYABLES`). See escalation E-1 — the files on disk were modified AFTER
the guide was committed.

## Fabrication hunt (deadliest-failure check)

Sampled aggressively across both docs: no vendor endorsement anywhere
(§4.2 is explicitly "REQUIREMENTS, not vendors" and stays that way); no
invented API endpoint, URL, or HTTP surface (all "endpoint" mentions are
negative or hypothetical-evidence statements); no history-depth claim
beyond the demonstrated 18-month/FY-grid windows (every depth statement is
either NOT_ESTABLISHED or a requirement on an external source); no
capability upgrade anywhere — the record is all-false exactly per evidence,
probes are the only upgrade path, and §3's preamble forbids force-fitting.
The guide's only "available=true" claims (close/MCAP/EV/multiples window,
current snapshots, FY grid) are each tied to a workbook cell that I
re-verified live. **Clean.**

## Runbook operability walk (dry read)

Followed §0→§8 as an operator: preconditions checkable; file destinations
and git policy exact (verified against `.gitignore`/`input_manifest.md`);
§2's hash-first discipline is executable (and, run today, correctly detects
escalation E-1 — the runbook catches real drift); §3 probe order is
concrete (controls, window, save-as, per-probe artifacts); §4 recording
steps name the exact function/file/table to amend and the same-PR rule;
§5 flip-authority table is complete and each row has trigger + vehicle,
with the catch-all routing absent-list items to a new goal; §6 archive
procedure names layout, manifest stamping, and honest labelling; §7
verified against feature_coverage; §8 failure modes actionable. Gaps found
are non-blocking (N-4, N-5 below).

## Edge cases attempted

- Marker sweep with case-insensitive grep and partial-token variants
  (`PENDING_G018`, `ADAPTER_PATH`) → zero hits beyond the resolved prose.
- Citation resolution probed on 6+ hard targets (dictionary row numbers,
  workbook cells, MP line, contract section text, spec-quote wording,
  evidence IDs) — all resolve; two imprecisions found (N-2, and field_mapping's
  own TM AT/AV vs catalog AS,AT — pre-existing in merged G013, guide is
  actually the more accurate of the two).
- Drift timeline forensics: G039 docs committed 2026-07-21 09:38/09:40
  (+0400); workbook mtimes 2026-07-21 16:31/16:52 — the guide's "all match"
  claim predates the file modification and is consistent with the manifested
  catalogs; 6/7 items still reproduce even on the drifted files.
- Cross-checked guide against main's post-branch doc fixes (`870cbd4`):
  the guide already cites the corrected FM-11/FM-25 and the D-013 shipped
  reality — no drift on merge.

## Leakage risks checked

Doc-level: §2.4 grading table matches D-011/D-015 and `grade_dataset()` —
the downgrade path is stamped strictly later than bar close (leak-safe
direction) and manifest-recorded; CT-10's no-knowledge_time rule correctly
assigns stamping to ingestion; runbook §6 archive procedure sets
knowledge_time = retrieval_time (D-009), never backfills, and forbids
historical claims from go-forward archives. No doc statement licenses a
PIT upgrade without a recorded probe.

## Quantitative invariants checked

- Capability record: 8 booleans false + 2 structural = gap-list's 10. ✓
- Scorecard arithmetic: 4+4+2+5+1=16 (P1); 1+2+2+2+1=8 (P2); 13+2 blocked
  of 70, 50–55 plausible (P3); 9+7=16 named (P4); 1+3+0+3+2=9 groups (P4). ✓
- TM obs: 375 (CLOSE/MCAP/EV/_ADJ), 254 (unadjusted LTM), 6 empty pairs,
  two ~3-month holes — re-derived from the live workbook. ✓
- FS D8=16675, J8≈393594.53 re-read live. ✓
- 46 FM rows, 823 fields, 513/176/306/104/34/26-pair counts all cross-match
  dictionary/catalogs. ✓

## Findings

**Blocking (against G039):** none.

**Escalation (URGENT, repo-level, not caused by this branch):**

- **E-1 — Input-file drift vs manifest.** Both git-ignored evidence
  workbooks on disk no longer match `input_manifest.md`:
  W2 `ebffbda477d6…` / 333,181 B (manifest `40973092c8a3…` / 343,811 B);
  W1 `9a57a90e9b8e…` / 70,584 B (manifest `9bf1cdeb4bfb…` / 70,136 B).
  mtimes 2026-07-21 16:31:05 / 16:52:09 (+0400) — AFTER the G039 commits
  (09:38/09:40), consistent with the workbooks being opened and re-saved in
  Excel without an AlphaSense session (volatile add-in formulas recalculated:
  FP D29:D44 now cache `Please Login to Continue`; E-G012-12's cited VL40
  error no longer sits at FP D37, though the VL40 pattern itself survives at
  FS D148:K148). Consequences: the G012 catalogs describe the *manifested*
  files, not the current disk files; G040's clean-clone audit and any future
  probe interpretation will trip on this. Orchestrator must rule: restore
  the manifested originals from a pristine copy (preferred) or add new
  manifest rows + a G012 catalog erratum. The runbook's own §2 step 1 is the
  procedure that detects exactly this — it worked.

**Non-blocking:**

- **N-1** — Guide §1.2 "Two flags may change **only** … `history_start` …
  `supports_vintages` … No other mechanism upgrades this record" understates
  its own §3 gate table and runbook §5: VP-01 (MARKET_DAILY field-coverage
  additions) and VP-07 (`corporate_action_basis`) also change the record.
  Internal wording inconsistency; the operative tables are correct.
- **N-2** — Guide §2.2 Technical row cites "P1/P3 formulas.md §5": correct
  for P3 (`docs/evidence/p3_lasr_2014/formulas.md` §5), but P1's technical
  formulas are §9 of its formulas.md. Inherited verbatim from merged
  `field_mapping.md` §5.6 (same imprecision).
- **N-3** — `base.py:207` comment still cites "FM-11/FM-25/FM-31" for
  `DEFAULT_PRICE_FIELDS`; main's D-012 correction (`870cbd4`) removed FM-31
  (float-adjusted cap, needs-additional-data). G018 code-comment nit, not a
  G039 file; queue with the G019/G020 doc-drift items.
- **N-4** — Runbook §0 lacks an explicit "AlphaSense Excel add-in installed
  + authenticated session" precondition for §3 probes. E-1's
  `Please Login to Continue` strings are precisely the unauthenticated
  outcome; §3's neither-branch rule handles it, but the precondition should
  be stated.
- **N-5** — Runbook §4 step 2 is slightly vague on which file receives new
  evidence rows (`evidence_rows.md` vs "the integration guide's gate table
  context"); orchestrator merge covers numbering but the target file should
  be named.

## Recommendations

1. Orchestrator: rule on E-1 before G040 and before any real probe run;
   record the ruling in `decisions.md` and fix/annotate `input_manifest.md`
   and (if files are not restored) E-G012-12 + the W2 catalog FP table.
2. Fold N-1/N-2/N-4/N-5 into the existing doc-drift cleanup queue (with the
   G043-noted `config_system.md` item); none needs to gate PR #59.
3. N-3 belongs to the G018 follow-up list (comment-only change).

## Evidence

- Branch/PR: `git rev-parse HEAD` = `5ba8eea…` = PR #59 head; worktree clean;
  triple-dot diff = 2 new files only.
- Marker sweep: grep exit 1 on `ADAPTER_PATH_PENDING_G018` across both docs.
- Tests: `uv run pytest -q` → 527 passed, 1 skipped (CT-11 documented skip).
- Live workbook reads: openpyxl (read-only) outputs recorded in the
  "Live-workbook re-verification" section; hashes via `shasum -a 256`.
- All quoted doc phrases located verbatim in their cited sources; quote scan
  found no span >15 words.

Verifier: fresh-context verifier agent, 2026-07-23.
