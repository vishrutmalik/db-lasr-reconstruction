# Verification report — G041

- **Goal ID:** G041 — Quote-length compliance pass on merged evidence (P1/P3/P4)
- **GitHub issue:** #46
- **Branch:** `agent/paper-researcher/G041-quote-compliance`
- **Commit reviewed:** `096a098e5d78eb7195c398f63e50c797445c6c01`
- **Pull request:** #47 (metadata not independently confirmable — `gh`
  unavailable in verifier environment; branch and SHA verified locally;
  goals.md row on origin/main confirms issue #46 / PR #47 linkage)
- **Verdict:** **PASS**

## Acceptance criteria reviewed

From goals.md (origin/main row G041), MASTER_PROMPT copyright discipline, and
the compliance headers the evidence files themselves assert:

1. Every double-quoted verbatim span in `docs/evidence/p1_nlasr_2012/`,
   `docs/evidence/p3_lasr_2014/`, `docs/evidence/p4_nlasr_2020/` is ≤15 words.
2. The change is a pure trim pass: no citation, item-numbering, classification,
   or other content changes — each hunk only shortens a quoted span, moves
   words out of quotation marks into paraphrase, or adjusts a compliance
   header.
3. Retained quote cores remain verbatim to the source PDFs at the cited pages;
   moved-out paraphrases do not distort meaning.
4. Compliance headers claiming "Quotes ≤15 words" are true after the change.
5. Semantic invariants (numbers, formulas, parameters) unchanged.

Scope note: the underlying evidence content was already verified in
G007/G009/G010; this verification is deliberately narrow to the trim pass.

## Commands executed

- `git log --oneline` / `git status --short` / `git rev-parse HEAD` in the
  worktree → HEAD = 096a098 on the assigned branch; worktree clean. ✓
- `git diff --stat main...HEAD` → exactly 5 files, 34 insertions / 34
  deletions, all inside the three target directories:
  `p1_nlasr_2012/extraction.md` (13/13), `p1_nlasr_2012/formulas.md` (6/6),
  `p3_lasr_2014/extraction.md` (5/5), `p3_lasr_2014/formulas.md` (2/2),
  `p4_nlasr_2020/extraction.md` (8/8). Matches the assignment exactly. ✓
- `git diff main...HEAD` → full hunk-by-hunk inspection: 20 hunks containing
  23 distinct quote trims (some hunks carry two). Every hunk either shortens
  a quoted span or moves words out of quotation marks into paraphrase; all
  page citations (p.50, p.58, p.29, p.13, p.20, p.25, p.56, p.17, p.18, p.4,
  p.5, p.6, p.19 …), item numbers, and classification tags are byte-identical
  across each hunk. No header hunks present (headers already claimed ≤15
  words; see criterion 4). ✓
- Verifier's own multi-line-aware quote scanner (written fresh for this
  verification, straight `"…"` and curly `“…”` pairs, quotes may span line
  breaks, fenced code blocks excluded, word = whitespace token containing an
  alphanumeric; raw token count also tracked):
  `scratchpad/g041_verifier_quote_scan.py` run over all .md files in the
  three directories.
- Same scanner run over the pre-change tree (merge-base 5867506 via
  `git archive`) as a scanner self-test.
- Same scanner run over `docs/evidence/p2_nlasr2_2013/` extracted from
  origin/main (directory does not exist on this branch — created on main
  after the branch point).
- `pypdf` (6.14.2) verbatim spot-checks of 6 of the 23 trimmed quotes against
  the source PDFs in `inputs/papers/` (normalized for curly quotes, dash
  variants, hyphen-spacing extraction artifacts, whitespace).
- Numeric-invariant probe: multiset of all numeric tokens
  (`\d+(?:[./-]\d+)*%?`) in removed vs added diff lines compared per file.

## Tests passed

- **Post-change scan:** p1_nlasr_2012 — 174 quotes, max 15 words;
  p3_lasr_2014 — 221 quotes, max 15 words; p4_nlasr_2020 — 208 quotes, max
  15 words. Zero violations. Max raw token count (including non-word tokens
  such as "…") is also 15 in all three directories.
- **Scanner self-test (pre-change tree):** 18 violations detected, max 26
  words (p1 formulas.md rank-IC definition), confirming the scanner catches
  the class of breach and that the trim was genuinely needed. All 18
  pre-change violations lie inside the 23 trimmed spans; the remaining 5
  trims were borderline quotes trimmed conservatively.
- **p2 state (report-only, remediated on another branch):**
  `docs/evidence/p2_nlasr2_2013/` is absent from this branch; on origin/main
  (835ceea) it scans clean — 170 quotes, max 15 words, zero violations.
- **Verbatim spot-checks (6 of 23, two per paper):**
  1. P1 §4 (p.50 cited; PDF p.50): retained "we get the factor score at the
     end of the month" — source reads "we **always** get the factor score at
     the end of the month …"; core verbatim except the pre-existing omission
     of "always" (present before this PR — see non-blocking #1). Moved-out
     paraphrase "assume rebalance on the same day" matches source verbatim —
     no distortion. ~PASS (inherited inexactness, not introduced here).
  2. P1 §19 (p.29): "uses the trailing 12 years … in the same month" — both
     fragments verbatim on PDF p.29; the elided parenthetical matches the new
     paraphrase ("falling back to all the available years when there is less
     than 12 years of historical data") exactly. PASS.
  3. P3 turnover metric (p.25): "the correlation between the previous and
     current month model predicted alpha scores" verbatim on PDF p.25;
     moving "Serial correlation is defined as" outside the quote is faithful.
     PASS.
  4. P3 weight update (p.8): "weight of each incorrectly classified stock is
     increased" verbatim on PDF p.8; paraphrase "with each correctly
     classified stock's weight decreased" matches the source clause. PASS.
  5. P4 §21 hedge sample (p.4): both retained fragments ("the worst 50% of
     the weeks in the previous 10 years"; "ranked according to the P&L of the
     aggregate of the other 3 models") verbatim on PDF p.4. PASS.
  6. P4 §37 validation periods (p.5): both retained fragments verbatim on PDF
     p.5 ("out-of-sample" appears as "out -of-sample" in pypdf extraction — a
     known hyphen-spacing artifact; the surrounding context confirms the
     exact wording). PASS.
- **Numeric-invariant probe:** removed-line and added-line numeric-token
  multisets identical for all 5 files — every number in every trimmed passage
  survives unchanged.
- **Targeted numeric items (3, as assigned):**
  1. P1 §19 seasonal samples: trailing **12 years** / same calendar month —
     unchanged and verbatim in source. ✓
  2. P4 §21 hedge sample: worst **50%** of weeks, previous **10 years**,
     aggregate of the other **3 models** — unchanged and verbatim in source. ✓
  3. P4 §37 validation: hyperparameters trained **1996–2002**, posterior
     results **2003–2020** out-of-sample — unchanged and verbatim in source. ✓
  (Additionally, P1 weak-learner constants ε = 1/N and Q = 5 pass through
  their hunks unaltered.)
- **Compliance headers (criterion 4):** no header lines were edited by the
  diff; the pre-existing headers asserting ≤15-word quotes
  (`p1_nlasr_2012/extraction.md` l.11, `p1_nlasr_2012/evidence_rows.md` l.5,
  `p4_nlasr_2020/extraction.md` l.7, `p4_nlasr_2020/formulas.md` l.5) were
  false before this PR (18 violations) and are **now true** per the scan.
  p3_lasr_2014 files carry no such header — nothing to falsify. ✓

## Tests failed

None blocking. Spot-check 1 noted an inherited (pre-PR) one-word quote
inexactness; recorded as non-blocking #1.

## Code paths inspected

- All 20 hunks of `git diff main...HEAD` (the entire change surface).
- Headers/front matter of all 5 changed files plus the 10 unchanged sibling
  .md files in the three directories (scanned in full by the quote tool).

## Edge cases attempted

- Multi-line quotes (scanner joins across line breaks) — none exceed 15 words.
- Curly vs straight quote mixing — both handled; zero curly-quote violations.
- Straight-quote parity — no file has an odd quote count (no unbalanced
  spans that could hide a long quote from pairing).
- Fenced code blocks excluded, then the three directories' formulas files
  re-checked — the LaTeX blocks contain no double-quoted prose.
- Ellipsis tokens inside quotes ("…") — counted as non-word tokens; even
  counting them as words, the max is 15.
- Adjacent-quote splitting — checked for spans reassembled from two quotes
  (see non-blocking #2).

## Leakage risks checked

Not applicable (documentation-only trim pass; no code, data, or
methodology-content changes — confirmed by hunk inspection and by the
citation/number identity checks above).

## Quantitative invariants checked

- Numeric-token multiset identity per file across the diff (see above). ✓
- Three assigned numeric items re-verified against source PDFs. ✓
- Quote-count deltas consistent with pure splitting/trimming: p1 174→174,
  p3 220→221, p4 202→208 (net increases arise from splitting long quotes
  into shorter retained fragments). ✓

## Blocking findings

None.

## Non-blocking recommendations

1. **P1 §4 quote fidelity (inherited from G007, not introduced here):** the
   retained quote "we get the factor score at the end of the month" omits
   "always" from the source ("we always get the factor score …") without an
   ellipsis. Recommend a follow-up one-word fix ("we … get" or include
   "always", keeping ≤15 words) in `docs/evidence/p1_nlasr_2012/extraction.md`
   item 4.
2. **Compliance-in-letter vs. spirit:** several trims satisfy the per-quote
   ≤15-word rule by moving verbatim source wording just outside the quotation
   marks (e.g. P1 §4 "assume rebalance on the same day"; p1 formulas.md "so
   that the nominator and denominator won't be 0") or by splitting one long
   span into two adjacent short quotes (e.g. P4 §37). Total verbatim
   reproduction per passage is roughly unchanged. If the compliance owner's
   intent is to bound verbatim reproduction rather than quote length per se,
   a genuine-paraphrase pass on these spots would strengthen the position.
   No individual quote breaches the stated rule, so this does not block.
3. `docs/evidence/p2_nlasr2_2013/` does not exist on this branch (created on
   main after the branch point); origin/main's copy scans clean (max 15
   words). No rebase needed for correctness, but the orchestrator should be
   aware the merge will be trivial (disjoint paths).

## Evidence for the verdict

- Independent scanner output (post-change): zero quotes >15 words across
  174 + 221 + 208 = 603 quotes in the three target directories; max word and
  max raw-token counts both exactly 15.
- Scanner self-test on the pre-change tree: 18 violations, max 26 words —
  the tool demonstrably detects the breach class this goal remediates.
- Six PDF spot-checks: all retained cores verbatim at the cited pages
  (one pre-existing one-word omission inherited from G007, outside this PR's
  introduced changes); all six moved-out paraphrases faithful to source.
- Numeric-token multiset identity across every hunk; citations, item
  numbers, and classification tags byte-identical.
- Diff scope exactly as assigned: 5 files, 34/34 lines, three directories,
  20 hunks / 23 quote trims, no other content touched.
