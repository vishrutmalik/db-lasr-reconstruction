# Verification report — G042

- **Goal ID:** G042 — Methodology/doc cleanup: CI-046 unit reconcile +
  verifier nits
- **GitHub issue:** #55
- **Branch:** `agent/quant-reviewer/G042-doc-cleanup`
- **Commit reviewed:** `91e9e20b971f5fa7d7b9bd4be745b46883cbd7c9`
  (branch commits: `c02df27` methodology files, `91e9e20` skills sweep)
- **Pull request:** #56 (confirmed via `gh pr view 56`: head
  `agent/quant-reviewer/G042-doc-cleanup @ 91e9e20…` → base `main`, OPEN)
- **Verdict:** **PASS**

## Acceptance criteria reviewed

From issue #55 (the 9-item list) plus the goals.md G042 row and the
integration-queue deferred list:

1. CI-046 turnover-unit reconciliation (G015 finding N-10, blocker for
   G027/G028) resolved from paper evidence with citation.
2. Sweep of 8 `PENDING_G011` markers in `skills/`.
3. `clipped` → `clamp` tail-mode enum alignment between skills and the
   lasr_2014 spec.
4. Embargo quantified in walk-forward-validation skill (import CI-015).
5. Equal-count binning restated in nlasr-weak-learner skill.
6. Stale inherited-tally note in `lasr_hc_2014.md` fixed (G011 finding 1).
7. CC-06 → CC-03 cross-ref typo in P2 extraction (single line).
8. CI-009 "Tested by:" field added (G014 finding 1).
9. CI-015(d) provenance wording EXPLICIT → ASSUMED per OQ-P4-06 (G014
   finding 2).

Global: each methodological fix cited; scripted re-scan shows no
`PENDING_G011` markers; **no other content changed**.

## Commands executed

- `git status` / `git log` / `git rev-parse HEAD` in
  `.worktrees/G042-quant-reviewer` → HEAD = `91e9e20`, tree clean.
- `git fetch origin` + `git diff --stat origin/main...HEAD` → exactly 11
  files (8 skills + `correctness_criteria.md` + `versions/lasr_hc_2014.md` +
  `docs/evidence/p2_nlasr2_2013/extraction.md`); merge-base `fa4b7a4`.
- Full `git diff origin/main...HEAD` read hunk-by-hunk (byte-level review;
  hunk→item mapping below).
- **Independent PDF extraction (pypdf)** of P1
  `inputs/papers/20120605_Rise of the Machines.pdf`, full text of paper
  pages 36 and 39 (pdf indices 35, 38) — from the main checkout, not any
  repo transcription.
- Scripted re-scans (my own scripts, not the implementer's):
  `grep -rn PENDING_G011 skills/` (0 hits; `git grep` on `origin/main`
  shows exactly 8, one per swept file); `grep -rni clipped skills/`
  (0 hits); `grep -rn clamp` in skills + `lasr_2014.md` (skill's 3 uses +
  spec's `tail_mode ∈ {literal, clamp}` line 63, A-G011-31);
  `grep -c '^### CI-'` on branch **and** on `origin/main:` (55 = 55,
  sequence ends CI-055); Python quote-length scanner (double/curly quotes,
  >15 words) over all 11 touched files.
- Independent recount of the `lasr_2014.md` §12 parameter table (Python:
  29 rows, primary provenance class per row).
- Repo-wide stale-string scans: `250%/yr|250% per year`, `250%`,
  `17 EXPLICIT`.
- Cross-reference resolution: `gh issue view 55`, `gh pr view 56`,
  `docs/verification/G014.md` findings 1–2, `docs/verification/G011.md`
  finding 1, `coordination/integration_queue.md` N-10, existence and range
  of `docs/methodology/contradiction_register.md` and `leakage_tests.md`
  (LT-001..LT-021), `docs/methodology/versions/` contents,
  `assumptions_register.md` A-G011 block.
- No Python test suite exists in the repo yet (docs-only state); the
  scripted re-scans above are the executable checks.

## Item 1 — CI-046 evidence check (load-bearing)

Independent extraction, P1 paper p.36 (pdf idx 35), verbatim:

> "Figure 53 shows the monthly turnover of our model, the average is over
> 250% (notice this is a two-way turnover for a market neutral portfolio,
> so the maximum turnover in a given period is 400%)."

Both quotes embedded in the corrected CI-046 row ("monthly turnover of our
model, the average is over 250%"; "the maximum turnover in a given period")
are exact substrings of the paper text. The Figure 53 series axis runs
0–400% with a 12-month moving average overlaid — a monthly series.

P1 paper p.39 (pdf idx 38), verbatim corroboration of the per-month
convention: "Turnover constrained at 30% one-way per month (or 360% one-way
per year)".

Verdict on the resolution direction: **/mo is correct; the criteria file's
">250%/yr" was the error** — exactly what the fix implements. Confirmed:

- Corrected row now reads "P1 decile L/S two-way >250%/mo" and carries the
  citations (P1 p.36 Fig 53; P1 extraction §33) plus the reconciliation
  rationale naming G042 / N-10.
- P1 extraction §33 (line 416) does record "over 250%" two-way monthly, max
  400% — the row's secondary citation is accurate; evidence row P1-37
  agrees.
- `versions/nlasr_2012.md` is NOT in the diff (empty `git diff` for that
  path); its §11 line 127 still reads ">250%/mo" — the spec band stands
  unchanged, as claimed.
- `docs/methodology/contradiction_register.md` (CR-014 block, line 399)
  already said ">250% monthly for P1" — now consistent repo-wide.

## Hunk → item mapping (out-of-scope check)

| Hunk | Item |
|------|------|
| p2 extraction §26, CC-06→CC-03 (1 line) | 7 |
| correctness_criteria CI-009 "Tested by:" | 8 |
| correctness_criteria CI-015 Basis (OQ-P4-06 ASSUMED) | 9 |
| correctness_criteria CI-046 band + reconciliation note | 1 |
| lasr_hc_2014.md inherited tally 17/6/3/6 → 19/6/0/4 | 6 |
| documentation-evidence-traceability (marker → contradiction_register.md) | 2 |
| lasr-linearized: precondition marker | 2 |
| lasr-linearized: 3 × `clipped`→`clamp` (step 5, failure mode, test 5) | 3 |
| leakage-survivorship (marker → leakage_tests.md LT-001..021) | 2 |
| nlasr-weak-learner: precondition marker | 2 |
| nlasr-weak-learner: step 2 EQUAL-COUNT restatement | 5 |
| quantitative-test-design (marker → CI-001..055) | 2 |
| synthetic-financial-data (marker → LT-001..021) | 2 |
| temporal-ensemble (marker → versions/, merged) | 2 |
| walk-forward: precondition marker | 2 |
| walk-forward: 3M-family embargo quantified per CI-015(b) | 4 |

Every hunk maps to exactly one assigned item; **no out-of-scope edits**.
All replacement targets exist and are accurate:
`docs/methodology/contradiction_register.md` present;
`leakage_tests.md` headings run LT-001..LT-021; CI headings 55;
`docs/methodology/versions/` holds all 7 specs.

## Scripted re-scan results

- `PENDING_G011` in `skills/`: **0** (main had exactly 8, one per file —
  the issue's count is exact and the sweep is complete). Remaining
  mentions repo-wide are only historical records
  (`coordination/integration_queue.md`, `docs/verification/G006.md`) —
  correctly untouched.
- `clipped` (any case) in `skills/`: **0**. `clamp` present at the 3
  expected skill sites and matches the authoritative enum in
  `lasr_2014.md` §5 (`tail_mode ∈ {literal, clamp}`, ASSUMED A-G011-31,
  echoed in its §12 table).
- CI heading count: **55 on branch = 55 on main** (no criterion added,
  dropped, or renumbered).
- Quote length ≤15 words: **all quotes introduced by G042 pass** (longest
  new quote = 10 words). The scanner's single >15 flag
  (p2 extraction line 69, 16 tokens incl. an ellipsis) is byte-identical
  to `origin/main` and outside every G042 hunk — pre-existing, not a G042
  finding.
- Stale-unit sweep: no ">250%/yr" remains outside historical
  queue/verification records and CI-046's own before/after note; no
  "17 EXPLICIT" echo remains outside verification records.

## Spot-checks, items 4–9

- **Item 4 (embargo):** CI-015(b) states "an embargo of at least one full
  horizon after the test period is configurable and defaults ON for
  overlapping families"; the walk-forward skill's new 3M text mirrors it
  ("at least one full target horizon … configurable and defaulting ON for
  overlapping families (CI-015(b))") while preserving the P3 p.59 ~80%/6M
  serial-correlation stat. Match.
- **Item 5 (equal-count binning):** skill now: Q equal-count bins of the
  cross-sectional normalized rank, "count EXPLICIT P1-10/11, equal-count
  scheme ASSUMED per OQ-P1-01 — as nlasr_2012.md §5 records".
  `nlasr_2012.md` §5: "Q=5 equal-count bins of the normalized rank
  (P1-10/11; binning convention ASSUMED per OQ-P1-01)". OQ-P1-01 tags
  equal-count quintiles ASSUMED; P1-10/11 are EXPLICIT rows for the
  quantile learner and "we set Q=5". Fully consistent.
- **Item 6 (tally):** independent recount of `lasr_2014.md` §12 table:
  29 parameter rows → primary classes {EXPLICIT: 19, IMPORTED_FROM_P1: 6,
  INFERRED: 0, ASSUMED: 4} — matches both `lasr_2014.md`'s own tally
  (line 171) and the corrected lasr_hc echo (19/6/0/4, 29 parameters).
  G011 verification finding 1 asked for exactly this fix (old echo
  17/6/3/6). Match.
- **Item 7 (CC ref):** diff in p2 extraction is exactly one line (§26).
  p2 `contradiction_candidates.md` CC-03 = "Boosting parameters absent
  from P2 (rounds, quantiles, smoothing)" and itself quotes P1 p.20
  "30 layers" — the correct target for §26's rounds cross-ref. CC-06 =
  "'In sample' boundary for v1 OOS test" (unrelated), confirming the old
  ref was a typo. Correct.
- **Item 8 (CI-009 Tested by):** new field "G026, G038" mirrors
  correctness_criteria.md's own coverage map (G026 backtester row and
  G038 full-experiment row both list CI-009; no other row does), which is
  the literal instruction of G014 verification finding 1. See non-blocking
  finding 1 for a residual cross-doc inconsistency.
- **Item 9 (CI-015(d) provenance):** OQ-P4-06 in
  `docs/evidence/p4_nlasr_2020/open_questions.md` tags the pooled
  weekly-rows reading "ASSUMED: use all weekly rows … (the N example in
  Step 4 … supporting this reading)". The corrected Basis wording
  ("ASSUMED per the OQ's own tag, supported by the Step-4 N example")
  matches the source exactly; the old "EXPLICITly implied" overstatement
  is gone, per G014 finding 2's suggested phrasing. Match.

## Edge cases / adversarial probes attempted

Docs-only change; the adversarial equivalents executed:

- Independent PDF re-extraction of both cited pages (not trusting any repo
  transcription) — quotes verified as exact substrings.
- Independent tally recount from the primary table rather than trusting
  either file's arithmetic.
- Both-sides marker count (main vs branch) to confirm "8 swept" is neither
  under- nor over-claimed.
- Repo-wide stale-token sweeps (`250%/yr`, `clipped`, `17 EXPLICIT`) to
  catch inconsistencies the diff itself would hide.
- CI heading count compared branch-vs-main to detect silent
  addition/renumbering.
- Resolution of every newly cited artifact/ID (contradiction_register.md,
  LT-001..021, CI-001..055, A-G011-31, OQ-P1-01, OQ-P4-06, P1-10/11,
  P1-37, CI-015(b)) to catch fabricated references — all resolve.

## Leakage risks checked

No pipeline semantics changed. The two leakage-relevant edits strengthen
constraints rather than relax them: CI-015(d) provenance is now honestly
ASSUMED (an implementer can no longer cite it as EXPLICIT license to pool
overlapping rows without the overlap-robust-inference rider, which is
retained), and the walk-forward skill's embargo is now quantified (≥1 full
target horizon, default ON) instead of the vague "add embargo". CI-046's
band change tightens the acceptance check G027 will consume (>250% per
month is the stricter, paper-true bound; the wrong per-year band would have
passed grossly under-turning reconstructions).

## Quantitative invariants checked

| Invariant | Probe | Result |
|-----------|-------|--------|
| CI-046 band unit = paper's unit | pypdf extraction p.36; Fig 53 axis; p.39 corroboration | /mo confirmed |
| Spec/criteria band agreement | nlasr_2012.md §11 vs CI-046 vs CR-014 | all >250%/mo |
| 2× one-way/two-way relation intact | CI-046 statement + P3-27 basis unchanged | intact |
| lasr_2014 tally arithmetic | recount 29 rows | 19/6/0/4 exact |
| CI census stable | heading count branch vs main | 55 = 55 |
| PENDING_G011 = 0 in skills | grep branch; git grep main | 0 (was 8) |
| Enum single-token | `clipped` 0 hits; `clamp` = spec token | aligned |
| Quote discipline | scripted ≤15-word scan, 11 files | no new violations |

## Blocking findings

None.

## Non-blocking findings

1. **CI-009 goal-coverage disagreement across docs (pre-existing, now more
   visible).** The new "Tested by: G026, G038" mirrors
   correctness_criteria.md's own coverage map, exactly as G014 finding 1
   prescribed — but CI-009's own Scope line says "(G026, G028, G038)" and
   `docs/architecture/testing_strategy.md` line 110 maps CI-009 →
   "G026,G028,G038". Whether G028 (metrics) must independently test the
   HP-window-rejection invariant should be reconciled in a follow-up
   one-liner (either add G028 to both coverage maps or drop it from
   Scope/testing_strategy). Not a G042 defect: the field matches its
   assigned source of truth and says so.
2. **Minor rhetorical looseness in the CI-046 note (cosmetic).** The
   parenthetical "an annual reading would also contradict the 400%
   per-period ceiling" is supportive hand-waving: strictly, the
   contradiction only follows because average and maximum describe the
   same per-period series that the paper labels monthly. The load-bearing
   citation (the paper's explicit "monthly turnover … the average is over
   250%") is airtight on its own, so no change required.

## Evidence for the verdict

- Diff scope: `git diff origin/main...HEAD` = 11 files / 16 hunks, each
  mapped 1:1 to the 9 assigned items (table above); worktree clean at
  `91e9e20`; remote branch tip = local HEAD; PR #56 head SHA matches.
- Item 1 verified against the primary source (independent pypdf
  extraction), not against any repo file.
- Items 2–9 each verified against their originating finding (G011 finding
  1, G014 findings 1–2, G015 N-10, G006-deferred skill nits) and against
  the authoritative cross-referenced file.
- All scripted re-scans pass; all new citations resolve; zero out-of-scope
  content change (every unchanged neighbor line inspected in the hunks;
  the single quote-length flag is byte-identical to main).
