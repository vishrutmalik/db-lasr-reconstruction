# Verification report — G043

- **Goal ID:** G043 — Config schema slice: VersionSpec, guards, CI-044 completeness
- **GitHub issue:** #58
- **Branch:** `agent/implementer/G043-config-schemas`
- **Commit reviewed:** `6a41c08d46dc58ba5c7048a0422ba5897bf1f888` (3 commits:
  `582b424` pyproject grant, `8c426a5` schemas/guards/loader/fixture,
  `6a41c08` test suites)
- **Pull request:** #61 (head `6a41c08…` confirmed via `gh pr view 61 --json headRefOid`)
- **Verdict:** **PASS**

## Acceptance criteria reviewed

Specs of record: `docs/architecture/config_system.md` (§§1–9),
`docs/methodology/versions/*.md` (provenance tables; nlasr_2012.md §12 in
full), `docs/methodology/contradiction_register.md` (all 31 CRs; §7 knob
index as reproduced in config_system.md §7),
`docs/methodology/correctness_criteria.md` (CI-044, CI-013, CI-016, CI-055),
decisions.md D-014, issue #58 acceptance block, G015 verification findings
N-3/N-9. Verifier assignment checks 1–8 all executed.

## Commands executed

All from `.worktrees/G043-implementer` with a fresh verifier environment
`UV_PROJECT_ENVIRONMENT=$HOME/.venvs/lasr-verify-g043` (implementer env
untouched):

- `git status` → clean; `git rev-parse HEAD` → `6a41c08…`; merge base with
  `origin/main` = `8c0bee7` (G017 merge).
- `git diff --name-status origin/main...HEAD` → exactly **20 files**, all in
  owned paths: 10 × `src/lasr/config/**` (`__init__.py` M + 9 A: ensemble,
  errors, experiment, guards, kernel, loader, provenance, sections,
  selection, version_spec), 6 × `tests/unit/test_config*.py` (bindings,
  ci044_completeness, fixture_nlasr_2012, guards, loader, provenance),
  `tests/fixtures/config/nlasr_2012.yaml`, `pyproject.toml`, `uv.lock`.
  Nothing else touched. ✓
- `uv sync --locked` → exit 0, fresh venv (adds pandas-stubs 3.0.3.260530,
  types-pyyaml 6.0.12.20260518). ✓
- `uv run ruff format --check .` → `78 files already formatted`. ✓
- `uv run ruff check .` → `All checks passed!` ✓
- `uv run mypy src/lasr` → `Success: no issues found in 53 source files`
  (strict). ✓
- `uv run pytest -q` twice → **702 passed** both runs (4.53s / 1.97s),
  identical. ✓ (374 pre-existing + 328 new config tests.)
- `gh pr view 61 --json statusCheckRollup` → lint ✓, typecheck ✓,
  test ubuntu 3.11/3.12 + macos 3.12 ✓; e2e-smoke/leakage-fast/determinism
  SKIPPED (placeholder stages, expected). CI green at the reviewed SHA. ✓
- Two CI-044 mutation runs (test-table edits, reverted; worktree clean
  after — `git status --porcelain | wc -l` → 0).
- ~15 adversarial probe scripts run against the installed package (guard
  probes, loader-safety probes, provenance probes, edge battery — detailed
  below).

## Pyproject grant audit (check 1)

Claimed vs actual (`git diff origin/main...HEAD -- pyproject.toml uv.lock`):

| Granted item | Claimed | Actual diff | Match |
|---|---|---|---|
| `pandas-stubs>=2.2` (dev group) | yes | added, with G043-grant comment | ✓ |
| `types-PyYAML>=6` (dev group) | yes | added (mypy-strict on loader's yaml import) | ✓ |
| TID251 numpy.random additions | 37 | 37 counted: `default_rng` + 34 samplers (`binomial`…`bytes`) + `get_state`/`set_state` | ✓ |
| Anything else | none | none — only the two deps, the 37 entries, and two explanatory comment blocks | ✓ |
| uv.lock | refresh | adds only `pandas-stubs` + `types-pyyaml` package/dep-group entries | ✓ |

D-014 records the grant (dated 2026-07-22, orchestrator ratifies). The G017
PR body records the recommendation generically ("pandas-stubs dev dep +
TID251 additions recommended"); the specific 37-entry enumeration is the
implementer's, but every entry is a numpy.random global-state API consistent
with the existing training_and_artifacts.md §6.1 ban — within grant scope.
No out-of-grant edits. ✓

## Worked-fixture audit — 39/39 (check 3)

Manual cross-off of every row of nlasr_2012.md §12 against
`tests/fixtures/config/nlasr_2012.yaml` (value + provenance class +
assumption id): **39/39 match.** Highlights: train_universe russell3000
EXPLICIT/P1-31; eligibility_screens [] EXPLICIT_ABSENCE/A-G011-01;
membership_vintage ASSUMED/A-G011-02; return_type total INFERRED/A-G011-08;
fractions 30/40/30 EXPLICIT/P1-04; Q=5 EXPLICIT + bin_scheme
ASSUMED/A-G011-06; ε one_over_n EXPLICIT/P1-13; n_definition
INFERRED/A-G011-10; smooth_z false INFERRED/A-G011-11; n_rounds 30
EXPLICIT/P1-17; windows 12m/12y/1m EXPLICIT/P1-19/20/21; risk_model
ASSUMED/A-004 + internal_cost_bps ASSUMED/A-G011-18; borrow null
EXPLICIT_ABSENCE/A-G011-19. Class tally reproduces the spec's 22 E / 4 INF /
13 A (EXPLICIT_ABSENCE under EXPLICIT). Three rows are mechanism-covered
exactly as in the §6 block: kernel (discriminator `piecewise_constant`),
selection objective (discriminator `min_z` + tagged `allow_repeats`),
global/ultra equal weighting (variant mechanism, §6 comment).

Mechanical semantic diff of the fixture vs the config_system.md §6 YAML
block (yaml.safe_load both, recursive compare): **the only differences are
the three `costs.base_bps` fields** (prov EXPLICIT→ASSUMED, src reworded,
assumption A-G043-01 added) — precisely the one ruled exception.

**base_bps ruling verified:** P1-38 gives the grid {5,10,15,20,25,30} and
names no base; the grid midpoint is 17.5, not 20; 20 bps is P2/P3's base
(CR-013 forbids cross-paper import as EXPLICIT). G015 verification N-3
flagged exactly this mis-tag. Re-tagging ASSUMED with register candidate
A-G043-01 is correct; ruling documented in the fixture header and pinned by
`test_base_bps_n3_ruling`. ✓

## CI-044 completeness audit + mutation test (check 4)

Independent recount of the id domains from source documents:

- CR ids: `grep -c "^## CR-"` on contradiction_register.md → **31**
  (CR-001..031, no gaps). Table has all 31. ✓
- OQ ids: p1 open_questions.md → OQ-P1-01..17 (**17**); p2 → Q1..Q14
  (**14**); p3 → Q1..Q12 (**12**); p4 → OQ-P4-01..17 (**17**). Total **60**;
  table domain check pins exactly this set. ✓
- Assumption ids: distinct `A-G011-*` across the seven version specs →
  **66** (01..66, no gaps) + shared `A-004` = **67**. ✓
- Totals: **158 ids**, path rows **53 (CR) + 63 (OQ) + 71 (assumption) =
  187** over 103 distinct schema targets — matches the PR claim exactly.

Documented exclusions all verified against their cited sources:

- 8 CR n/a rows = {CR-001, 009, 020, 021, 023, 026, 028, 031} — exactly the
  n/a rows of config_system.md §7 (dating decision / no-knob-by-design /
  errata / citation hygiene / documentation / cosmetic). CR-009's absence is
  additionally asserted structurally (`test_cr009_no_weight_update_knob_anywhere`
  walks the whole model tree, >20 classes). ✓
- 6 OQ exclusions: P3-Q2 (resolved by the P1 update formula —
  lasr_2014.md §7 lines 80–82 say so verbatim); P3-Q10 (no disclosed
  minimum-bin-size rule; CR-012 Q∈{3,5} region override is the documented
  handle); P3-Q11 (masses over labeled stocks = structural kernel behavior,
  per the OQ's own text "implied by the y_i=±1 summation"); OQ-P4-04
  (nlasr_2020.md §8: "No convergence criterion defined (OQ-P4-04) → none
  implemented"); OQ-P4-08 (arithmetic typo, not a knob); OQ-P4-10
  (paper-trading wording, reporting caveat). All legitimate. ✓
- A-G011-33 (Z on fractional masses): lasr_2014.md line 75 — "Z's inputs are
  whatever W±ⱼ the kernel defines"; derived from kernel type, no independent
  knob. Legitimate. ✓

Path rows resolve against schema **classes** (`model_fields`), not built
instances, so renames break the test. CR path rows spot-checked against
config_system.md §7 row-by-row (CR-002..CR-030): match (CR-012 adds
`bin_centers` for P4, a superset — fine).

**Mutation tests (both reverted, worktree clean after):**

1. Pointed CR-010's row at `boosting.n_rounds_renamed` (simulates a schema
   rename) → `test_cr_knob_reachable[CR-010-…]` **FAILED** ("broken at
   'n_rounds_renamed'"), 196 others passed. Resolver has teeth. ✓
2. Silently deleted the OQ-P4-13 entry from the table →
   `test_oq_domains_complete` **FAILED**. Ids cannot be dropped silently. ✓

The suite also self-tests the mechanism
(`test_resolver_rejects_misplaced_knob`).

## Guard probes (check 5) — all my own, via the real code paths

All 7 versions first dumped to real YAML files in scratch and loaded through
`load_version_spec` (file-based inheritance resolution for hc/hf/modernized):
**7/7 build, 0 guard violations**, correct kernel/selection per version.

| Probe | Result |
|---|---|
| nlasr_2012 + hedge component | **rejected**, SpecGuardError rules `{no_hedge_component, component_count}`, basis CR-002 ✓ |
| lasr_2014 with `max_weighted_corr` | **rejected**, `selection_type`, basis "CR-008 (G015 N-9 pin)" ✓ |
| lasr_hc/lasr_hf resolved specs with `max_weighted_corr` | **rejected** by the same guard; via the delta path the deep-merge produces a min_z/max_wcorr hybrid rejected even earlier by the schema (`extra_forbidden`) — both layers hold ✓ |
| nlasr_2020 with turnover cap 0.30 | **rejected**, `no_turnover_cap`, basis CR-014 ✓ (modernized with cap 0.10 also rejected) |
| P3 kernel dict carrying `beta_negative_action` | **rejected by the schema itself** (`extra_forbidden`), before guards — cross-version kernel blend unrepresentable ✓; M-08 kernel delta re-parented onto lasr_2014 likewise ValidationError ✓ |
| lasr_hc delta without the 3M horizon | **rejected**, `target_horizon`, basis "CR-006 (P3-02)" — inherited 1M violates the child guard on the RESOLVED spec, per config_system.md §8 ✓ |

Guard registry inspected against config_system.md §4 table: every row
present (nlasr_2012 3-components/no-hedge/no-neutralization/kernel/selection;
nlasr2_2013 4-with-ic-threshold-hedge; lasr_2014-family kernel; nlasr_2020
kernel/selection/turnover/roster; universal fractions-sum + horizon/grid;
CI-055 enforced at schema level via `AcceptanceConfig` validator — stronger
than a guard). Registry goes beyond §4 with documented bases: N-9 min_z pin
for the lasr family, CR-006 horizon pins, CR-003 hedge metric+grain pins,
§8 inherits-legality, CR-018 execution pins (hf next_open, 2020 t+2 MOC).

## Loader safety (check 6)

- `grep -rn "yaml\." src/lasr/` → only `yaml.safe_load` + `yaml.YAMLError`;
  no `yaml.load`/`unsafe_load` anywhere. A `!!python/object/apply:os.system`
  payload is rejected as invalid YAML (safe loader has no such constructor). ✓
- Unknown keys: top-level unknown section, misspelled nested key, unknown
  kernel discriminator (`gradient_boosting`) all → ValidationError
  (`extra_forbidden` / `union_tag_invalid`); `extra="forbid"` + `frozen=True`
  on the shared `ConfigModel` base. ✓
- Inheritance cycles: real 2-file cycle (lasr_2014.yaml ↔ lasr_hc_2014.yaml
  in scratch) → `ConfigLoadError: inheritance cycle involving 'lasr_2014'`;
  true self-cycle (modernized.yaml inherits modernized) → cycle error;
  missing parent file → load error. ✓
- `config_hash`: same dict validated twice → identical SHA-256; fixture
  loaded in **two separate processes** → identical
  (`14a784dd56763df9…b1db429e`); one leaf changed (n_rounds 30→29) →
  different hash; input dict key order shuffled → identical hash (canonical
  JSON sorts keys, compact separators, JSON-mode dump). Round-trip
  dump→revalidate→hash identical (suite test, re-run). ✓

## Provenance type audit (check 7)

- `Provenance` = exactly the 6 classes of config_system.md §2 (probed:
  {ASSUMED, EXPLICIT, EXPLICIT_ABSENCE, IMPORTED_FROM_P1, INFERRED,
  MODERNIZED}); unknown class (`STATED`) rejected. ✓
- `Param`: `src` mandatory non-empty, `prov` mandatory closed, unknown key
  rejected, frozen (mutation raises), typed value enforced,
  `Param[float | None]` supports tagged absences (CR-014). ✓
- `Override.prov`: all four evidence classes (EXPLICIT, EXPLICIT_ABSENCE,
  IMPORTED_FROM_P1, INFERRED) **rejected**; only MODERNIZED/ASSUMED accepted;
  rationale mandatory (missing AND empty both rejected). Overrides cannot
  masquerade as paper evidence. ✓
- `ExperimentConfig.faithful`: True with zero overrides, False with one —
  probed directly. ✓

## Evidence sampling — 8 non-fixture claims (check 8)

1. CR-002 register text: "nlasr_2012 = exactly 3 components … Never import
   the hedge component into the P1-era spec … must fail to build if a hedge
   selector is supplied" → `_guard_p1_roster` implements verbatim. ✓
2. CR-003: three metric literals `{backcast_ic_threshold,
   bottom_half_model_ic, bottom_half_aggregate_pnl}` + month/week grain +
   0.075/144m for 2013 → `HedgeBackcastComponent` vocabulary + per-version
   metric/grain pins match. ✓
3. CR-008: "lasr_2014 family = argmin-Z … never substitute one for the
   other" → confirms the G015 N-9 guard pin (G015.md finding N-9 verified
   verbatim). ✓
4. CR-014: "must NOT add a turnover cap … null for nlasr_2020" →
   `Param[float | None]` + `_guard_no_turnover_cap`. ✓
5. CR-030: `beta_negative_action ∈ {stop_training, skip_alpha}`, default
   literal `stop_training`, alternative required → kernel Literal + factory
   default + modernized M-08 flip test. ✓
6. `KernelType` in `src/lasr/data/schemas/ensemble.py` = {piecewise_constant,
   piecewise_linear_interp, linear_fit_nonneg} — binding test asserts
   equality with the KernelConfig discriminators; config layer does NOT
   import the schemas layer (binding lives in tests, per system_design §4). ✓
7. hedge_backcast-vs-doc drift: config_system.md §3 writes
   `HedgeSelector(type: Literal["hedge"])`; canonical
   `HedgeBackcastSelectorSpec` (schemas/ensemble.py line 97) uses
   `"hedge_backcast"`. Config follows the canonical schema (G017: ExpertSpec
   canonical); drift documented in ensemble.py docstring + PR report for a
   docs-owning goal. Correct precedence. ✓
8. modernized.md M-01 ("Purged … default ON") and M-08 ("skip_alpha …
   the only place a CR default differs") → delta factory + guards +
   `test_modernized_delta_flips_only_documented_defaults`. ExecutionMode =
   the CR-018 four-mode set in `lasr.core.timing` (shared enum, CI-014). ✓

## Edge cases attempted

Empty ensemble roster (rejected, min_length=1); empty acceptance section
(rejected); bare-equality acceptance target (rejected, CI-055); band=0
(rejected, gt=0); `*_min` one-sided bound (accepted, as designed); empty
validation windows (rejected); inverted date window/range (rejected);
inverted DateRange in ExperimentConfig (rejected); portfolio_level=4
(rejected); unknown version_id `nlasr_2025` (rejected at schema); input
key-order shuffle (hash-invariant); fixture loaded across processes
(hash-stable); test suite run twice (identical). Also: labels 50/0/50
accepted (guard checks sum only — matches §4's stated rule; the 30/40/30
values are YAML facts) and `n_rounds=-5`/`n_bins=-1` accepted (no numeric
range validation on `Param[int]`) — see non-blocking notes.

## Leakage risks checked

Config layer holds no data flow, so PIT leakage is out of scope here; the
version-boundary analogue (evidence leakage across papers, CR-002/007/008
"never conflate") is enforced structurally and was attacked directly (guard
probes above; cross-version kernel/selection blends unrepresentable at the
schema layer). `ExecutionMode.SAME_CLOSE` keeps P1's acknowledged look-ahead
as an explicit mode rather than falsified knowledge times, consistent with
system_design §1.

## Quantitative invariants checked

- CI-044: 158/158 ids present, 187 path rows resolve, exclusions pinned with
  rationale, mutation-tested both directions (path break + id drop). ✓
- CI-013: horizon/grid legality {1M,3M}×month_end, {1W,4W}×weekly enforced;
  illegal pair probed. ✓
- CI-016: fractions sum to 1.0 ± 1e-9 enforced; probed. ✓
- CI-055: bands/dual-references/one-sided bounds only; bare equality and
  zero band probed and rejected; CR-019 dual reference keeps both 0.0654 and
  0.0756. ✓
- Config-hash determinism (feeds CI-042): stable across loads, processes,
  and key order; sensitive to any leaf value. ✓
- 39/39 provenance rows; 69 tagged leaves counted by the suite. ✓

## Blocking findings

None.

## Non-blocking findings / recommendations

1. **Numeric Params carry no range validation** (`n_rounds=-5`, `n_bins=-1`
   accepted). Version-correct values are pinned in YAMLs and the coming
   CI-029 config-diff test; still, ge/gt bounds on structurally-positive
   ints would be cheap hardening for the model goals (G024+) to add.
2. **CR-003 lookbacks/threshold not guard-pinned** (144m/120m/156w/520w and
   0.075 live as YAML values, not guards). §4 does not require them, and the
   version YAMLs land with model goals — acceptable, but worth a CI-029
   assertion then.
3. **Doc drift (not this goal's fault):** config_system.md §3 still shows
   `HedgeSelector(type="hedge")` and `ComponentSpec`; superseded by the
   canonical `hedge_backcast` selector set. Already flagged in the PR for a
   docs-owning goal — should be tracked so §3 is corrected.
4. **A-G043-01** exists only in the fixture/PR report so far; it must reach
   the assumption register when that artifact lands (register owner).
5. The G017-recommended TID251 list was never enumerated anywhere before
   D-014; the 37 entries are all legitimately in-scope numpy.random
   global-state APIs, but future grants should enumerate in the
   recommendation itself.

## Evidence

- Gates: this report's "Commands executed" (all re-run by the verifier;
  nothing taken from pasted output).
- Fixture diff: recursive semantic YAML compare, three base_bps fields only.
- Mutation logs: CR-010 path break → 1 failed/196 passed; OQ-P4-13 drop →
  `test_oq_domains_complete` failed; both reverted (`git status` clean).
- Probe transcripts: guard probes 1–5 PASS, loader-safety probes PASS
  (cycle/self-cycle/evil-yaml/unknown-key/hash ×4), provenance probes PASS
  (4 rejected classes, 2 accepted, rationale ×2, faithful ×2), edge battery
  9 PASS + 2 documented NOTEs.
- Recounts: CR=31, OQ=17+14+12+17=60, A-G011=66+A-004=67; rows 53+63+71=187;
  distinct targets=103.
